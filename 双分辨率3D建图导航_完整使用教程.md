# 双分辨率 3D 建图与导航完整使用教程

适用环境：

- Ubuntu 22.04
- ROS 2 Humble
- Gemini2 深度相机
- 2D 激光雷达
- STM32 下位机
- NVIDIA RTX 3060

## 1. 三个主脚本的区别

| 脚本 | 功能 | 是否控制小车 |
|---|---|---|
| `START_DUAL_2D_3D_MAPPING.sh` | 2D 建图、彩色 RGB-D 关键帧、OctoMap、实时高分辨率点云 | 否 |
| `START_DUAL_2D_3D_NAVIGATION.sh` | 上述全部功能，加 SmacPlanner2D、Regulated Pure Pursuit、3D 动态避障和受控脱困 | 是 |
| `START_DUAL_2D_3D_SLAM.sh` | 兼容入口，等同于 `MAPPING` | 否 |

第一次测试必须先用 `MAPPING`，完成传感器方向与外参标定后再运行
`NAVIGATION`。

## 2. 第一次安装

进入项目根目录：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
```

赋予脚本执行权限：

```bash
chmod +x ./*.sh
chmod +x visual_laser_slam/*.sh
```

安装依赖：

```bash
./STEP0_INSTALL_VISUAL_SLAM_DEPS.sh
```

依赖脚本应安装或检查：

- `depth_image_proc`
- RTAB-Map
- OctoMap
- Cartographer
- Nav2
- Regulated Pure Pursuit controller
- RViz2
- `sensor_msgs_py`

安装完成后确认：

```bash
source /opt/ros/humble/setup.bash
ros2 pkg prefix depth_image_proc
ros2 pkg prefix rtabmap_slam
ros2 pkg prefix octomap_server
ros2 pkg prefix nav2_regulated_pure_pursuit_controller
```

每条命令都应输出安装路径，不能显示 `package not found`。

## 3. 串口和相机检查

插好 Gemini2、2D 雷达和 STM32 后执行：

```bash
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
lsusb
```

主启动脚本会自动检测串口并申请权限。当前预期波特率：

```text
2D LiDAR: 115200
STM32:    115200
```

若自动检测失败，在下面文件中填写固定端口：

```text
visual_laser_slam/dual_resolution_3d.env
```

例如：

```bash
CHASSIS_PORT=/dev/ttyUSB0
LIDAR_PORT=/dev/ttyACM0
```

不要仅按 USB0/USB1 的数字猜设备，应同时检查 USB VID/PID 和启动日志。

## 4. 第一次编译和只建图启动

执行：

```bash
./START_DUAL_2D_3D_MAPPING.sh
```

脚本会自动：

1. 打开 RViz。
2. 在缓存工作区编译 `lidar_py` 和 `local_depth_cloud_cpp`。
3. 启动 Gemini2。
4. 启动 STM32 和 LiDAR。
5. 启动 Cartographer 2D。
6. 启动过滤后的实时深度点云。
7. 启动 RTAB-Map 彩色低分辨率关键帧。
8. 启动 OctoMap。

首次编译时间比后续启动长。出现以下结果才算编译成功：

```text
Summary: ... packages finished
```

不能含有：

```text
Failed <<< local_depth_cloud_cpp
```

## 5. RViz 检查

启动后依次检查：

### Cartographer 2D Global Map

- 应显示稳定的二维墙体地图。
- 原地旋转时地图不能螺旋扩散。
- 车体实际旋转方向必须与 RViz 一致。

### Filtered 2D LiDAR

- 显示经过时间戳处理的 2D 雷达扫描。
- 点云方向必须与车体和实际墙面一致。

### STEP10V2.1 Local High Resolution

- 实时高分辨率过滤点云。
- 这是 Nav2 3 cm 本地 STVL 动态层的主要实时避障输入。
- 人离开后重新看到原位置时，障碍应被射线清除。

### RTAB-Map Persistent Low Resolution 3D

- 显示低频保存的真实彩色 RGB-D 关键帧。
- RTAB-Map 不发布主 TF，不得拖动 Cartographer 地图。
- 当前取消勾选一段时间后会暂停 RTAB-Map，以降低负载。

### OctoMap 3D Occupied

- 显示长期低分辨率占用体素。
- 适合保存环境结构，不是 20 Hz 实时避障源。

### AGV URDF Collision Model

- 显示 `0.665 x 0.665 x 0.321 m` 车体。
- 相机和雷达模型位置应与实车一致。
- URDF 负责显示与 TF；Nav2 实际碰撞边界由 footprint 参数负责。

## 6. 相机自动外参标定

标定前必须保持 `START_DUAL_2D_3D_MAPPING.sh` 正在运行。

### 6.1 地面标定

另开终端，进入项目：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
./CALIBRATE_CAMERA_EXTRINSIC.sh
```

要求：

- 车完全静止。
- 选择平整、哑光或反光较弱、没有明显坡度的地面。
- 深度画面的下半部分必须主要是地面；RGB 画面不要求完全没有墙。
- 墙可以出现在远处或画面上半部分，但不能占据深度画面下半部分的主要面积。
- 不要有人脚、箱子、台阶、坑洞或强反光缺深度区域占据大部分画面。
- 标定使用深度点云拟合地面，不使用 RGB 颜色，也不依赖相机加速度计。

通过后自动写入：

```text
CAMERA_ROLL_DEG
CAMERA_PITCH_DEG
CAMERA_Z
CAMERA_GROUND_CALIBRATED=true
```

并生成：

```text
visual_laser_slam/dual_resolution_3d.env.bak.时间戳
```

看到 `AUTO-WRITE REJECTED` 表示质量不足，配置没有被修改。

地面标定完成后，先在主终端按一次 `Ctrl+C`，等待程序完全退出，再重新运行：

```bash
./START_DUAL_2D_3D_MAPPING.sh
```

### 6.2 相机和 LiDAR yaw 标定

让车停在距离大平墙 1 到 3 米的位置，确保 Gemini2 和 2D LiDAR
都能看到同一面墙。另开终端执行：

- 最推荐让车头大致垂直朝向墙，误差在约 15 度内，方便两种传感器看到同一片墙。
- 数学上不要求车与墙绝对垂直；算法比较的是同一面墙在两种传感器中的方向差。
- 相机不必看到整面墙，但墙应覆盖相机前方主要区域，宽度最好超过 1 米。
- 墙前不要有桌腿、箱子、门框、玻璃或另一面更显眼的斜墙。
- 车辆必须全程静止。

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
./CALIBRATE_CAMERA_YAW.sh
```

通过后自动写入：

```text
CAMERA_YAW_DEG
CAMERA_EXTRINSIC_CALIBRATED=true
```

再次结束并重启 `START_DUAL_2D_3D_MAPPING.sh`。

YAW 成功后不要再次运行地面标定。再次运行地面标定会把
`CAMERA_EXTRINSIC_CALIBRATED` 重新置为 `false`，此时必须重启后再跑一遍 YAW。

## 7. 标定后的检查

执行：

```bash
grep -E '^CAMERA_(ROLL|PITCH|YAW)_DEG=|^CAMERA_Z=|^CAMERA_(GROUND|EXTRINSIC)_CALIBRATED=' \
  visual_laser_slam/dual_resolution_3d.env
```

确认最后一行为：

```text
CAMERA_EXTRINSIC_CALIBRATED=true
```

然后在 RViz 检查：

- 地面点云应落在 `z=0` 附近。
- 墙面不能整体倾斜或旋转 45/90/180 度。
- 彩色点云、实时深度点云、2D LiDAR 的方向应一致。
- URDF 相机模型方向应与点云视锥一致。

注意：RViz 中车体相对房间墙面的朝向由 `map -> base_link`（Cartographer）
决定，不由 `CAMERA_YAW_DEG` 决定。若 2D 地图、车体箭头和雷达边界一起突然
转动，应检查 `CARTOGRAPHER_POSE_JUMP`、`NAVI_YAW_RATE_LIMIT` 和下位机 yaw；
若只有彩色/深度点云相对 2D 墙线歪，而车体与 2D 地图稳定，才是相机外参问题。

相机向下倾角不需要再用三轴加速度重复标定。地面深度平面直接求出
`CAMERA_ROLL_DEG`、`CAMERA_PITCH_DEG` 和 `CAMERA_Z`；静止加速度只能给
roll/pitch 初值，不能得到 yaw，而且容易受相机 IMU 轴定义和安装结构影响。

## 8. 启动导航和实时避障

完成标定后结束只建图版本，执行：

```bash
./START_DUAL_2D_3D_NAVIGATION.sh
```

该版本的数据链是：

```text
Cartographer 2D 地图/位姿
        |
SmacPlanner2D 全局路径（内部平滑）
        |
Regulated Pure Pursuit 20 Hz 跟踪与碰撞前视
        |
/cmd_vel_nav
        |
velocity_smoother
        |
safety_fusion + C++ 前/后/旋转碰撞门
        |
/cmd_vel_safe
        |
STM32
```

过滤后的 Gemini2 点云同时进入：

- 3 cm 局部 STVL：立即减速、停车，并参与 RPP 的碰撞前视。
- 5 cm 全局 STVL：持续堵路时使全局路径失效，并触发 SmacPlanner2D 重新选路。

## 9. 第一次导航验证

不要第一次就高速或在狭窄走廊测试。

1. 在宽阔区域启动导航版。
2. 保持物理急停随时可按。
3. 先选择前方 2 到 3 米的空目标。
4. 检查规划线是否出现。
5. 检查车辆是否平滑起步。
6. 把纸箱从侧面缓慢放入路线。
7. 检查车辆是否减速、停车或绕开。
8. 持续堵住路线，检查全局路径是否重新生成。
9. 移走纸箱，检查 costmap 障碍是否清除并恢复导航。

调试命令：

```bash
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 topic hz /local_highres_cloud_v21/sensor
ros2 topic hz /local_costmap/costmap
ros2 topic hz /global_costmap/costmap
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
```

`controller_server` 和 `planner_server` 应为 `active`。

## 10. 正确结束方法

在启动主程序的终端按一次：

```text
Ctrl+C
```

不要连续快速按很多次。脚本需要时间：

1. 停止运动输出。
2. 结束 Nav2、RTAB-Map、OctoMap 和 Cartographer。
3. 保存 RTAB-Map 数据库。
4. 保存 OctoMap。
5. 关闭 RViz。

等待出现：

```text
[stop] Complete.
```

如果等待较长仍未退出，再检查：

```bash
ps -ef | grep -E 'ros2|rviz2|cartographer|rtabmap|octomap' | grep -v grep
```

## 11. 日志位置

每次运行日志位于：

```text
SLAM_Log/dual_3d_日期_时间/runtime.log
```

出现问题时至少保存：

- 完整 `runtime.log`
- RViz 截图
- 执行的脚本名称
- 问题发生时间
- 当时是静止、直行还是原地旋转

## 12. 本次 3.txt 报错说明

### `AMENT_TRACE_SETUP_FILES: 未绑定的变量`

原因是标定脚本在 `set -u` 模式下加载 Jazzy setup。脚本已经改为加载
ROS 环境时临时关闭 `nounset`。

### `Missing ROS package: depth_image_proc`

当时尚未安装依赖。日志显示后续已经成功安装：

```text
ros-humble-depth-image-proc
```

### `no matching function for call to max(int, long int)`

ROS 2 Humble 整数参数返回长整型，GCC 13 不允许和 `int` 隐式混合推导。
代码已经增加显式整数转换。

更新代码后重新运行 `START_DUAL_2D_3D_MAPPING.sh`，脚本会重新编译缓存工作区。

## 13. 更新代码后的推荐命令

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
git pull
chmod +x ./*.sh visual_laser_slam/*.sh
./STEP0_INSTALL_VISUAL_SLAM_DEPS.sh
./START_DUAL_2D_3D_MAPPING.sh
```

确认建图正常后再执行自动标定和导航测试。

## 14. V5.2 旋转漂移复测与标定

### 14.1 先重新编译并启动

终端 A：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
chmod +x ./*.sh visual_laser_slam/*.sh
./START_DUAL_2D_3D_MAPPING.sh
```

等待 RViz、相机、雷达和 Cartographer 全部启动。终端中不应再出现
`RELIABILITY_QOS_POLICY`，RTAB-Map 也不应持续报告
`Did not receive data since 5 seconds`。

### 14.2 正确的两阶段标定

主程序必须继续运行。另开终端 B，在平整地面保持车辆和相机完全静止：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
./CALIBRATE_CAMERA_EXTRINSIC.sh
```

成功后先结束并重启终端 A 的 MAPPING，让新的 roll、pitch、z 生效。

车辆停在距离大平墙 1 至 3 米处，保证相机和 2D 雷达都能看到同一面墙。
另开终端 C：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
./CALIBRATE_CAMERA_YAW.sh
```

两个脚本都会自动备份并写入
`visual_laser_slam/dual_resolution_3d.env`。不要在主程序未运行时执行标定。

### 14.3 原地旋转复测

先在空旷区域缓慢旋转约 90 度，再停止 5 秒。观察：

```bash
grep -E 'NAVI |NAVI_YAW_RATE_LIMIT|CARTOGRAPHER_POSE_JUMP|incompatible QoS|Did not receive data' \
  SLAM_Log/dual_3d_*/runtime.log | tail -n 120
```

预期结果：

- `NAVI_YAW_RATE_LIMIT` 可以在下位机 yaw 阶跃时出现；
- 每个 20 ms 周期接受的 yaw 不超过约 2.4 度；
- `used_wz` 在姿态变化时不再保持 0；
- 不再连续出现 40 至 89 度的 `CARTOGRAPHER_POSE_JUMP`；
- 地图不会在原地旋转时生成一圈重复墙。

如果保护持续触发，保留完整 `runtime.log`，并记录真实车辆在该时刻是否转动。
参数 `NAVI_MAX_YAW_RATE_DEG_S` 默认 120，首次复测不要修改。

## 15. V5.3 当前完整操作顺序

### 15.1 建图版

终端 A：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
chmod +x ./*.sh visual_laser_slam/*.sh
./START_DUAL_2D_3D_MAPPING.sh
```

首次运行会在隔离缓存工作区编译，等待终端打印全部 `[ready]`。不要在同一个
终端继续输入标定命令。

### 15.2 自动标定

保持终端 A 运行，另开终端 B。地面标定时车辆必须静止在平整地面：

```bash
cd ~/视频/huichuan-agv-ros2-foxy-visual-lidar-slam-step/huichuan-agv-ros2-foxy-main
./CALIBRATE_CAMERA_EXTRINSIC.sh
```

脚本会自动使用主程序的 `ROS_DOMAIN_ID=88`，等待深度话题最多 10 秒，并在
质量检查通过后备份、写入 `dual_resolution_3d.env`。成功后重启终端 A。

相机和 2D 雷达同时面对 1 至 3 米外的一面大平墙，再运行：

```bash
./CALIBRATE_CAMERA_YAW.sh
```

成功后再次重启建图程序。标定不通过时不会覆盖现有参数。

### 15.3 导航版

```bash
./START_DUAL_2D_3D_NAVIGATION.sh
```

导航版首次运行会额外编译 `frontier_exploration_ros2`。启动后检查：

```bash
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 topic hz /local_highres_cloud_v21/sensor
ros2 topic hz /local_costmap/costmap
```

两个 lifecycle 节点应为 `active`，本地点云约 14 至 15 Hz。首次运动测试应
架空驱动轮或在空旷低速区域进行。

### 15.4 如何判断“实时障碍是否清除”

不要用 RTAB 历史彩色点云或全局 OctoMap 判断实时碰撞清除。导航时观察
`local_costmap` 的 STVL 动态层：放入箱子应快速标记，移走并让相机重新看到
该区域后应清除。RTAB 持久地图保留历史人物/物体不代表 Nav2 仍把它当作
当前障碍。

### 15.5 本轮复测必须查看的日志

```bash
grep -E 'NAVI_YAW_RATE_LIMIT|CARTOGRAPHER_POSE_JUMP|Errno 5|No such file|RTAB-Map.*Loop|local_highres' \
  SLAM_Log/dual_3d_*/runtime.log | tail -n 200
```

新的 yaw 保护日志应包含 `action=drop_and_rebase`。如果仍出现明显双墙，请
记录发生时间并保留整个运行目录；如果同时出现 `/dev/ttyACM0` 的
`Errno 5` 或设备消失，应优先检查雷达 USB 线、供电、接口和接触。
