# CAR 项目修改记录

## 修改时间：2026-07-07

## 目标

将成熟的 ROS2 建图导航技术栈（slam_toolbox + Nav2 + AMCL）融合进现有项目，统一串口通信，将视觉避障和雷达建图导航合并为一套完整系统。

---

## 一、核心发现：slam_toolbox 在 Foxy 上可以工作

**之前认知**：slam_toolbox lifecycle 在 Foxy + FastDDS 下完全不可达。

**实际情况**：`online_async_launch.py` 启动的 `async_slam_toolbox_node` 是**普通节点**，不是 lifecycle 节点。之前用 `ros2 lifecycle set` 命令管理它当然找不到。

**解决方案**：
- launch 文件中用 `Node` 替代 `LifecycleNode`
- 删除 lifecycle configure/activate 事件管理代码
- 设置 `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`（已安装）

---

## 二、修改的文件

### 2.1 launch/slam_launch.py

**改动**：
- `LifecycleNode` → 普通 `Node`
- 删除 `register_activate_handler`、`emit_configure_event` 等 lifecycle 管理代码
- 删除 rviz2 启动（用户按需手动开）

**原因**：Foxy 下 lifecycle service 不可达，普通 Node 直接启动即可。

### 2.2 launch/slam_nav_fusion_launch.py

**改动**：
- `LifecycleNode` → 普通 `Node`
- 删除 `activate_slam`、`configure_slam` 事件
- 清理无用 import（`EmitEvent`、`RegisterEventHandler`、`LifecycleNode`、`OnStateTransition`、`ChangeState`、`Transition`）

**原因**：同上。

### 2.3 launch/nav2_launch.py

**改动**：
- `lifecycle_nodes` 列表加入 `'amcl'`

**原因**：AMCL 也是 lifecycle 节点，之前漏了，需要由 `lifecycle_manager` 统一管理。

### 2.4 lidar_py/vision_obstacle_node.py（新建）

**功能**：将 Gemini2 深度相机的近地面避障逻辑包装成 ROS2 节点。

**数据流**：
- 直接用 pyorbbecsdk 读深度帧（复用视觉代码模块）
- 9 区域障碍检测（L/C/R × RED/YELLOW/GREEN）
- 深度时间滤波 + 区域防抖 + 避障状态机
- 发布 `/depth_obstacle`（Int32MultiArray, 8 个 int32）
- 发布 `/depth/front_danger`（Bool）
- 发布 `/depth/min_distance`（Float32, m）

**不再直接发串口**，由 safety_fusion_node 统一仲裁。

### 2.5 launch/full_system_launch.py（新建）

**功能**：一条命令启动完整系统。

**启动的节点**：
- `chassis_node` — 底盘串口通信（唯一串口通信点）
- `lidar_node` — LD14P 雷达驱动
- `static_transform_publisher` — base_link → laser TF
- `slam_toolbox` — 2D 建图
- `vision_obstacle_node` — 视觉避障（可选，`enable_vision:=true`）
- `safety_fusion_node` — 安全融合
- Nav2 定位 + 导航（可选，`enable_nav2:=true`）

**用法**：
```bash
# 只建图
ros2 launch lidar_py full_system_launch.py

# 建图 + 视觉避障
ros2 launch lidar_py full_system_launch.py enable_vision:=true

# 建图 + 视觉避障 + Nav2 导航
ros2 launch lidar_py full_system_launch.py enable_vision:=true enable_nav2:=true map:=~/maps/lab_map.yaml
```

### 2.6 setup.py

**改动**：
- entry_points 加入 `vision_obstacle_node = lidar_py.vision_obstacle_node:main`
- data_files 加入 `full_system_launch.py`

---

## 三、STM32 通信协议确认（不需要改）

### 下行帧（上位机 → STM32，20 字节）

```
AA 55 cmd spd[0] spd[1] spd[2] spd[3] checksum

cmd:
  0x01 MOVE       四轮速度直接下发
  0x02 STOP       软停
  0x03 ESTOP      急停
  0x04 PS2        归还手柄控制权
  0x05 ECHO_ON    开启帧回传
  0x06 ECHO_OFF   关闭帧回传

spd[0..3]: int32 LE, 单位 cnt/s
符号约定: 电机1,3 负值=前进, 电机2,4 正值=前进
```

### 上行帧（STM32 → 上位机）

**NAVI 帧（20 字节，cmd=0x07，20Hz）— 当前唯一在发的上行帧**：
```
AA 55 0x07 yaw vx vz [unused] checksum

spd[0] = yaw × 100    → 偏航角 (0.01°/unit, 陀螺仪积分)
spd[1] = vx  × 1000   → 线速度 (mm/s, 编码器算的)
spd[2] = vz  × 100    → 角速度 (0.01°/s, 陀螺仪原始 gz)
spd[3] = 未使用
```

编码器帧（AA 55 34 字节）和 IMU 帧（AA 56 23 字节）在当前 STM32 代码中被 `#if 0` 注释掉，没有发送。

---

## 四、最终系统架构

```
Gemini2 深度相机
    ↓
vision_obstacle_node.py
    ↓ /depth_obstacle
    ↓
safety_fusion_node.py ← /cmd_vel_nav (Nav2)
    ↓ /cmd_vel_safe
    ↓
chassis_node.py ──AA55──→ STM32
    ↑ NAVI帧(0x07)
    ↓ /odom + TF
    ↓
slam_toolbox ← /scan ← lidar_node ← LD14P
    ↓ /map
Nav2 + AMCL (可选)
```

**串口通信统一由 chassis_node.py 负责**，视觉代码不再直接发串口。

---

## 五、未修改/保留的文件

| 文件 | 状态 | 说明 |
|------|------|------|
| lidar_node.py | 保留 | 只发布 /scan，不需要改 |
| chassis_node.py | 保留 | 已有完整 odom + TF + 串口通信 |
| safety_fusion_node.py | 保留 | 已有 /depth_obstacle 融合逻辑 |
| hector_slam.py | 保留 | 备用方案（slam_toolbox 不可用时） |
| laser_odometry.py | 保留 | 备用方案 |
| nav2_params.yaml | 保留 | 已有完整 Nav2 配置 |
| mapper_params_online_async.yaml | 保留 | 已有 slam_toolbox 配置 |
| 视觉代码 (road_v5.../) | 保留 | 不修改原代码，通过 vision_obstacle_node 包装 |

---

## 六、环境要求

```bash
# Foxy + CycloneDDS (必须)
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp

# 已安装的包
ros-foxy-slam-toolbox
ros-foxy-navigation2
ros-foxy-nav2-bringup
ros-foxy-rmw-cyclonedds-cpp
```

---

## 七、复查补充（2026-07-07）

本次复查了 `AGV_2D雷达建图导航完整改动步骤.txt`、`修改.md` 和实际源码，确认主链路应为：

```text
Nav2 /cmd_vel_nav + Gemini2 /depth_obstacle
    -> safety_fusion_node
    -> /cmd_vel_safe
    -> chassis_node
    -> AA55 20B 四轮速度
    -> STM32

STM32 0x07 NAVI 20B
    -> chassis_node
    -> /odom + odom->base_link

LiDAR /scan + /odom + TF
    -> slam_toolbox 或 Nav2/AMCL
    -> /map
```

补充修正：

1. `vision_obstacle_node.py` 修正视觉代码目录查找逻辑，会从源码/安装目录向上查找 `CAR/road_v5_5_640_modular_v7_unified_io`，也支持 `VISION_CODE_DIR` 手动指定。
2. `full_system_launch.py` 修正 `base_link -> laser_frame`，和 `lidar_node` 的 `LaserScan.header.frame_id=laser_frame` 一致。
3. `full_system_launch.py` 在 `enable_nav2:=false` 时启动在线建图 `slam_toolbox`；在 `enable_nav2:=true` 时不再同时启动 `slam_toolbox`，避免和 AMCL 同时发布 `map->odom`。
4. `full_system_launch.py` 的导航模式改为 include 项目自己的 `nav2_launch.py`，保证 Nav2 的 `/cmd_vel` 被 remap 到 `/cmd_vel_nav`，再经过 `safety_fusion_node` 输出 `/cmd_vel_safe`。
5. `full_system_launch.py` 的视觉节点默认分辨率调整为 `1280x720`。
6. `start_robot_debug_all.sh`、`slam_launch.py`、`slam_nav_fusion_launch.py`、`nav2_launch.py`、`full_system_launch.py` 都补上了 `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`。
7. 已对关键 Python/launch 文件执行 `python -m py_compile`，语法检查通过。

当前推荐启动：

```bash
cd ~/CAR
./start_robot_debug_all.sh
```

手动测试：

```bash
# 在线建图 + 视觉避障
ros2 launch lidar_py full_system_launch.py enable_vision:=true

# 已有地图导航 + 视觉避障
ros2 launch lidar_py full_system_launch.py enable_vision:=true enable_nav2:=true map:=~/maps/lab_map.yaml
```

---

## 八、网页地图漂移修正与点击导航（2026-07-07）

问题：

- 网页原来只订阅 `/map` 和 `/robot_pose`，但系统侧没有稳定统一地发布 `/robot_pose`。
- 网页如果拿不到 `map -> base_link` 的真实 TF 位姿，显示箭头容易看起来“飘”或不准。

修正：

1. 新增 `lidar_py/robot_pose_publisher.py`
   - 从 TF 查询 `map -> base_link`。
   - 发布 `/robot_pose`，类型 `geometry_msgs/PoseStamped`。
   - 建图模式下由 `slam_toolbox` 提供 `map -> odom`。
   - 导航模式下由 AMCL 提供 `map -> odom`。
   - `chassis_node` 继续提供 `odom -> base_link`。

2. 启动文件已接入：
   - `slam_launch.py`
   - `nav2_launch.py`
   - `slam_nav_fusion_launch.py`
   - `full_system_launch.py`

3. 网页新增点击地图导航：
   - 单击地图选择目标点，地图显示黄色目标标记。
   - 点击“确定导航”后发布 `/web/nav_goal`。
   - `/web/nav_goal` 类型是 `geometry_msgs/PoseStamped`，`frame_id=map`，由 `web_goal_nav_node` 转成 Nav2 `NavigateToPose` Action。
   - 拖拽地图只平移视图，不会误发送目标。

验证：

```bash
python -m py_compile robot_pose_publisher.py ...
npm install
npm run build
```

Python 语法检查通过，网页 `vite build` 通过。

---

## 九、底盘尺寸参数微调（2026-07-07）

按当前实车尺寸更新底盘运动学参数：

```text
轮径 = 152mm -> wheel_radius = 0.076m
车长 = 480mm -> wheel_base_h = 0.24m
车宽 = 520mm -> wheel_track_w = 0.26m
```

已同步修改：

- `chassis_node.py` 默认参数
- `safety_fusion_node.py` 默认参数
- `slam_launch.py`
- `nav2_launch.py`
- `slam_nav_fusion_launch.py`
- `full_system_launch.py`

当前代码使用：

```text
turn_radius = wheel_base_h + wheel_track_w = 0.50m
track_width = 2 * wheel_base_h + 2 * wheel_track_w = 1.00m
```

---

## 十、网页运行开关与 Nav2 路径预览（2026-07-07）

新增网页实时调试能力：

- `串口运动指令`：通过 `/robot/web_control` 控制 `chassis_node` 是否真正向 STM32 发送 MOVE 帧。关闭时仍接收 `/cmd_vel_safe` 并更新规划状态，但不写串口，并尝试发送一次 `0x04 PS2` 归还控制权。
- `障碍红点`：实时控制 RGB/网页视频中的障碍红色填充。
- `避障框`：实时控制 RGB/网页视频中的 ROI 框。
- `RGB调试信息`：实时控制 RGB/网页视频中的障碍计数、深度阈值等调试文字。

新增路径预览链路：

```text
网页点击地图目标点
    -> /web/preview_goal
    -> web_path_preview_node
    -> Nav2 ComputePathToPose
    -> /web/preview_path
    -> 网页地图绿色路径线
```

这个预览链路不会启动导航，也不会让车动；只有点击“确定导航”后才发布 `/web/nav_goal` 并由 `web_goal_nav_node` 触发 Nav2 Action。关闭“串口运动指令”时，路径预览和 Nav2 规划仍然运行，方便手柄人工开车时观察规划路径。

新增 STM32 串口协议网页调试：

- 新增 `/robot/serial_debug`，由 `chassis_node.py` 发布结构化 JSON。
- 网页新增 `STM32 AA55` 按钮：`enable_move`、`zero_move`、`stop`、`estop`、`ps2`、`echo_on`、`echo_off`。
- `zero_move` 发送 `cmd=0x01` 且四轮速度为 0。
- `stop` 发送 `cmd=0x02`。
- `estop` 发送 `cmd=0x03`。
- `ps2` 发送 `cmd=0x04` 且四轮速度为 0，并关闭上位机 MOVE 输出。
- `echo_on` / `echo_off` 分别发送 `cmd=0x05` / `cmd=0x06`。
- 网页新增 TX、ECHO、NAVI 三个窗口；ECHO 校验通过显示绿色，失败显示红色。
- `chassis_node.py` 在同一个串口读缓冲中同时解析 20B AA55 回响帧和 `cmd=0x07` NAVI 帧。

验证：

```bash
cd ~/CAR/网页 && npm run build
python -m py_compile chassis_node.py web_path_preview_node.py depth_obstacle_node.py display_debug.py
```

实车调参判断：

- 直走距离不准：优先检查 `wheel_radius`、`pulse_per_rev`、`gear_ratio`。
- 原地旋转角度不准：优先微调 `wheel_base_h + wheel_track_w`。

---

## 十一、网页箭头姿态、Nav2 差速车参数与一键启动修正（2026-07-08）

### 11.1 网页小车箭头抽搐修正（旧方案，已被 11.8 覆盖）

问题：
- 网页小车箭头订阅 `/robot_pose`。
- 原 `/robot_pose` 直接使用 `map -> base_link` 的姿态。
- 建图或 AMCL 定位时，`map -> odom` 会被 SLAM/定位模块修正，手柄转弯时网页箭头容易出现不跟随、抽搐或方向不准。

旧修正：
- 修改 `lidar_py/robot_pose_publisher.py`。
- 位置仍然来自 `map -> base_link`，保证小车在网页地图上的位置是地图坐标。
- 朝向优先来自 `odom -> base_link`，保证手柄转弯和底盘实时转向能立刻反映到网页箭头。
- 新增参数：`odom_frame: odom`、`orientation_source: odom`。

后续复查发现：
- 这个旧方案会混用 `map` 坐标下的位置和 `odom` 坐标下的朝向。
- 建图一圈后 `slam_toolbox` 会修正 `map -> odom`，此时网页图标容易出现“位置在 map 上是对的，但车头方向相对地图歪掉”的问题。
- 最新修正见 `11.8 WEB_ROBOT_ICON_MAP_HEADING_2026-07-09`。

已同步启动文件：
- `launch/full_system_launch.py`
- `launch/slam_launch.py`
- `launch/nav2_launch.py`
- `launch/slam_nav_fusion_launch.py`

验证命令：
```bash
ros2 run tf2_ros tf2_echo odom base_link
ros2 topic echo /robot_pose
```

如果手柄转弯时 `odom -> base_link` 的 yaw 不变，则问题不在网页，而在 STM32 `0x07 NAVI` 回传或 `chassis_node.py` 的 `/odom` 更新链路。

### 11.2 Nav2 差速四轮小车参数优化

当前仍默认使用 Foxy 标配 `DWBLocalPlanner`，没有强制切换到 RPP，避免现场缺少 `nav2_regulated_pure_pursuit_controller` 插件导致 Nav2 起不来。

修改文件：
- `config/nav2_params.yaml`

主要优化：
- AMCL 更新更频繁：`update_min_d: 0.10`、`update_min_a: 0.10`
- 差速车局部规划更稳定：`vy_samples: 1`、`vtheta_samples: 32`
- 转向速度范围：`min_vel_theta: -1.4`、`max_vel_theta: 1.4`、`min_speed_theta: 0.05`
- 降低急冲和抖动：`acc_lim_x: 0.9`、`decel_lim_x: -1.2`、`acc_lim_theta: 2.2`、`decel_lim_theta: -2.8`
- 小车 footprint 按实车 480mm x 520mm 设置：`[[-0.24, -0.26], [-0.24, 0.26], [0.24, 0.26], [0.24, -0.26]]`
- 局部代价地图扩大：`width: 4`、`height: 4`
- 2D 激光障碍层补充：`obstacle_max_range: 8.0`、`raytrace_max_range: 9.0`、`obstacle_min_range: 0.05`、`raytrace_min_range: 0.05`
- planner 频率降低：`expected_planner_frequency: 5.0`

判断方式：
```bash
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
ros2 topic echo /odom
```

- `/cmd_vel_nav` 抖而 `/odom` 稳：继续调 Nav2 控制器参数。
- `/odom` 本身抖：优先检查 STM32 回传 yaw/vx/vz、轮径、底盘尺寸和串口帧解析。
- `/cmd_vel_nav` 有速度但 `/cmd_vel_safe` 为 0：检查 `safety_fusion_node.py` 是否因视觉避障或运行开关拦截。

### 11.3 视觉基线不再默认锁死 Nav2

问题：
- 外部视觉节点 `depth_obstacle_node.py` 会发布 `/depth/baseline_ready`。
- 刚启动时该值可能为 `false`。
- 之前融合层可能因此把 Nav2 的速度清零，导致“Nav2 有规划但车不动”。

修正：
- 修改 `lidar_py/safety_fusion_node.py`。
- 新增参数：`require_depth_baseline`。
- 默认值为 `False`。
- 只有显式设置 `require_depth_baseline:=true` 时，才会因为视觉基线未 ready 而拦停底盘。

已同步启动文件：
- `launch/nav2_launch.py`
- `launch/slam_nav_fusion_launch.py`
- `launch/full_system_launch.py`

### 11.4 一键启动脚本 open_all.sh 修正

修改文件：
- `open_all.sh`

修正内容：
- 修复脚本中乱码路径，正确使用 `lidar/chapt1_ws`、`web_ctrl`、`road_v5_5_640_modular_v7_unified_io`。
- 默认串口：`LIDAR_PORT=/dev/ttyUSB1`、`LIDAR_BAUD=115200`、`CHASSIS_PORT=/dev/ttyUSB0`。
- 启动 `slam_nav_fusion_launch.py` 时传入 `enable_vision:=false`。
- 原因：一键脚本已经单独启动 `depth_obstacle_node.py`，它负责 Gemini2 SDK 读取、`/depth_obstacle`、`/depth/baseline_ready` 和 MJPEG 视频流 `:8080`。
- 如果 launch 里再启动 `vision_obstacle_node`，会两个节点抢同一个相机，并重复发布 `/depth_obstacle`。

一键启动流程：
```text
open_all.sh
  -> rosbridge_server :9090
  -> slam_nav_fusion_launch.py enable_vision:=false
  -> depth_obstacle_node.py :8080
  -> web_ctrl/start_web.py :5173
```

使用命令：
```bash
cd ~/CCCCAAAA
chmod +x open_all.sh
./open_all.sh
```

首次或代码修改后需要先编译：
```bash
cd ~/CCCCAAAA/lidar/chapt1_ws
colcon build --packages-select lidar_py
source install/setup.bash
cd ~/CCCCAAAA
./open_all.sh
```

启动后网页：
```text
本机网页：http://127.0.0.1:5173
局域网网页：http://<树莓派或Ubuntu主机IP>:5173
视频流：http://<树莓派或Ubuntu主机IP>:8080/video_feed
```

### 11.5 本次静态检查

已在 Windows 侧完成：
```bash
python -m py_compile safety_fusion_node.py nav2_launch.py slam_nav_fusion_launch.py full_system_launch.py
```

结果：
- Python 语法检查通过。
- `package.xml` XML 解析通过。

未完成：
- Windows 本机没有 ROS2 Foxy 运行环境，因此未实际 `ros2 launch`。
- 需要到 Ubuntu/树莓派上执行 `colcon build` 后实车测试。

### 11.6 网页点击目标导航只原地抽搐的修正

问题现象：
- 网页地图上点击目标点，再点“确定导航”后，小车没有稳定前进到目标点，只在原地抽搐或转动。

判断原因：
- 网页原来直接发布 `/goal_pose`，在 Foxy/Nav2/RViz 同时存在时，目标入口不够明确，可能和 Nav2 自带的 `/goal_pose` 监听产生重复或不稳定行为。
- 网页选点只带 x/y，目标姿态默认一直是 `yaw=0`，DWB 控制器可能先强行调整朝向，看起来像原地抖动。

本次修正：
- 新增 `lidar_py/web_goal_nav_node.py`：
  - 订阅 `/web/nav_goal`。
  - 将网页目标转换为 Nav2 `NavigateToPose` Action。
  - 新目标到来时取消旧目标，避免多个目标叠在一起。
  - 根据 `/robot_pose` 自动把目标 yaw 调成“小车当前位置指向目标点”的方向。
  - 收到网页目标后重新打 ROS 主机当前时间戳，避免浏览器/树莓派时间不同步导致 Nav2 TF 抖动。
  - 收到网页清除预览命令时同步取消当前 Nav2 目标，避免标记清掉但车继续导航。
- 修改网页：
  - “确定导航”不再直接发 `/goal_pose`，改为发 `/web/nav_goal`。
  - 地图选点时，如果已有机器人位置，会给目标附带合理 orientation。
- 修改 launch：
  - `nav2_launch.py` 和 `slam_nav_fusion_launch.py` 都启动 `web_goal_nav_node`。
- 修改 `setup.py`：
  - 增加 `web_goal_nav_node = lidar_py.web_goal_nav_node:main` 入口。

现场验证：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
colcon build --packages-select lidar_py
source install/setup.bash
cd ~/CCCCAAAARRRR
./open_all.sh
```

网页点目标后看终端应出现：
```text
web_goal_nav_node: Sending web nav goal: x=... y=... frame=map
web_goal_nav_node: Web nav goal accepted
```

同时检查速度链：
```bash
ros2 topic echo /web/nav_goal
ros2 action list | grep navigate
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
ros2 topic echo /odom
```

排查结论：
- `/web/nav_goal` 有数据，但没有 `Web nav goal accepted`：Nav2 Action 没起来，检查 `bt_navigator` 生命周期。
- `Web nav goal accepted` 有，但 `/cmd_vel_nav` 一直只有角速度：继续调 Nav2/DWB 参数或检查机器人朝向、地图坐标是否还反。
- `/cmd_vel_nav` 有前进速度，但 `/cmd_vel_safe` 没有：检查视觉避障是否拦截，或网页“串口运动指令”开关是否关闭。
- `/cmd_vel_safe` 有速度但车不动：检查 `chassis_node` 到 STM32 的串口发送和下位机控制权。

### 11.7 最新复查标签：WEB_NAV2_GOAL_BRIDGE_RECHECK_2026-07-09

本次复查目的：
- 对网页点击目标导航、路径预览、Nav2 Action 桥接、launch 启动入口、ROS topic 配置、文档说明重新做了一轮静态审查。
- 重点避免“网页显示已发送目标，但 Nav2 没有真正接管”“清除目标后车还继续导航”“浏览器时间戳导致 TF 抖动”这类不必要的现场问题。

本次新增修正：
- `lidar_py/web_goal_nav_node.py`
  - 收到 `/web/nav_goal` 后，不再信任浏览器传来的时间戳，统一重写为 ROS 主机当前时间。
  - 原因：浏览器电脑和 Ubuntu/树莓派时间不同步时，Nav2/TF 可能因为目标时间戳异常而抖动、拒绝变换或表现为原地调整。
  - 收到 `/robot/web_control` 的 `clear_preview_goal` 时，也会取消当前 Nav2 目标。
  - 原因：网页“清除”按钮原本只清预览目标和绿色路径，如果 Nav2 目标已经 accepted，理论上可能出现地图标记没了但车还继续跑。
  - `_on_goal_response()` 增加异常保护，Action 发送失败时会打印 `Web nav goal send failed`，避免异常静默。

文档同步：
- `修改.md`
  - 旧说明中的“点击确定后发布 `/goal_pose`”已经同步为 `/web/nav_goal`。
  - 明确 `/web/nav_goal` 会由 `web_goal_nav_node` 转成 Nav2 `NavigateToPose` Action。
- `CAR_使用教程_注意事项.md`
  - 网页点击地图导航说明同步为 `/web/nav_goal -> web_goal_nav_node -> NavigateToPose`。

当前确认的最终链路：
```text
网页点击地图目标点
    -> /web/preview_goal
    -> web_path_preview_node
    -> Nav2 ComputePathToPose
    -> /web/preview_path
    -> 网页绿色预览路径

网页点击“确定导航”
    -> /web/nav_goal
    -> web_goal_nav_node
    -> Nav2 NavigateToPose Action
    -> /cmd_vel_nav
    -> safety_fusion_node
    -> /cmd_vel_safe
    -> chassis_node
    -> AA55 20B
    -> STM32

网页点击“清除”
    -> /robot/web_control: clear_preview_goal
    -> web_path_preview_node 清空 /web/preview_path
    -> web_goal_nav_node 取消当前 Nav2 目标
```

本次未修改的部分：
- 未修改 `open_all.sh` 一键启动脚本。
- 未修改 STM32 AA55 协议解析和串口发送逻辑。
- 未修改视觉避障阈值、避障优先级和原地转向策略。
- 未修改 Nav2/DWB 参数。
- 未修改底盘轮速、轮径、车宽、车长相关参数。

本次 Windows 侧静态验证：
```bash
# lidar_py 包内所有 Python 源码语法检查
Get-ChildItem -Path 'lidar/chapt1_ws/src/lidar_py/lidar_py' -Filter '*.py' | ForEach-Object { python -m py_compile $_.FullName }

# launch/setup 语法检查
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py \
  lidar/chapt1_ws/src/lidar_py/setup.py

# package.xml 解析检查
python -c "import xml.etree.ElementTree as ET; ET.parse('lidar/chapt1_ws/src/lidar_py/package.xml')"

# 网页构建检查
cd web
npm run build
```

验证结果：
- `lidar_py/lidar_py` 下所有 Python 文件语法检查通过。
- `nav2_launch.py`、`slam_nav_fusion_launch.py`、`full_system_launch.py`、`setup.py` 语法检查通过。
- `package.xml` XML 解析通过。
- 网页 `npm run build` 通过。

Ubuntu/树莓派侧仍需执行：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
colcon build --packages-select lidar_py
source install/setup.bash
cd ~/CCCCAAAARRRR
./open_all.sh
```

现场确认点：
```bash
ros2 topic echo /web/nav_goal
ros2 action list | grep navigate
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
ros2 topic echo /odom
```

网页点击“确定导航”后，终端应看到：
```text
web_goal_nav_node: Sending web nav goal: x=... y=... frame=map
web_goal_nav_node: Web nav goal accepted
```

### 11.8 最新复查标签：WEB_ROBOT_ICON_MAP_HEADING_2026-07-09

问题现象：
- 建图绕一圈后，网页地图上的小车图标朝向歪掉。
- 表现为网页图标看起来“斜着直走”。
- 用户现场不确定网页图标朝向是否和 RViz 中雷达红色扫描线方向一致。

判断原因：
- RViz 在 `map` 固定坐标下显示 LaserScan 时，雷达点云方向来自完整 TF：
```text
map -> odom -> base_link -> laser_frame
```
- 网页图标位置来自 `map -> base_link`。
- 旧方案中网页图标朝向来自 `odom -> base_link`。
- 这会导致网页图标“位置在 map 坐标系，朝向却在 odom 坐标系”，当 `slam_toolbox` 修正 `map -> odom` 后，网页图标朝向就可能相对地图歪掉。

本次修正：
- `lidar_py/robot_pose_publisher.py`
  - 默认 `orientation_source` 从 `odom` 改为 `map`。
  - `orientation_source=map` 时，位置和朝向都来自 `map -> base_link`。
  - 保留 `orientation_source=odom` 兼容分支，但启动文件不再默认使用。
- 启动文件同步改为 `orientation_source: map`：
  - `launch/full_system_launch.py`
  - `launch/slam_launch.py`
  - `launch/nav2_launch.py`
  - `launch/slam_nav_fusion_launch.py`

修正后的关系：
```text
网页图标位置
    -> /robot_pose.position
    -> TF map -> base_link

网页图标朝向
    -> /robot_pose.orientation
    -> TF map -> base_link

RViz 雷达红色扫描线
    -> LaserScan /scan in laser_frame
    -> TF map -> odom -> base_link -> laser_frame
```

因此：
- 如果 `base_link -> laser_frame` 外参正确，网页图标车头方向应与 RViz 中雷达扫描方向保持一致。
- 如果网页图标和 RViz 雷达红线仍明显不一致，优先检查 `lidar_node.py` 的 `laser_yaw_deg`、`scan_angle_sign`、`scan_angle_offset_deg` 和雷达实际安装方向。

现场验证命令：
```bash
ros2 topic echo /robot_pose --once
ros2 run tf2_ros tf2_echo map base_link
ros2 run tf2_ros tf2_echo base_link laser_frame
```

Windows 侧静态验证：
```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/robot_pose_publisher.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py
```

验证结果：
- Python 语法检查通过。

### 11.9 最新复查标签：NAV2_MOVES_THEN_STOPS_DEPTH_BASELINE_2026-07-09

问题现象：
- 网页地图上点目标并点击“确定导航”后，小车只往前动一点，然后停止，没有到达目标点。

判断原因：
- 当前一键启动脚本 `open_all.sh` 使用的是 `slam_nav_fusion_launch.py`。
- `slam_nav_fusion_launch.py` 中 `safety_fusion_node` 之前设置了：
```text
require_depth_baseline: True
```
- 视觉节点 `depth_obstacle_node.py` 启动后会持续发布 `/depth/baseline_ready`。
- 如果视觉基线还没有采集完成，`/depth/baseline_ready=false` 会使 `safety_fusion_node` 直接把 `/cmd_vel_safe` 清零。
- 因此会出现 Nav2 目标已经 accepted，`/cmd_vel_nav` 也可能有速度，但融合层把速度拦停，车只动一下就不动。

本次修正：
- 修改 `launch/slam_nav_fusion_launch.py`：
```text
require_depth_baseline: False
```
- 与 `nav2_launch.py`、`full_system_launch.py` 保持一致。

修正后的行为：
- 视觉基线未 ready 时，不再直接锁死 Nav2 运动。
- 视觉避障仍然根据 `/depth_obstacle` 的 `level` 工作：
  - `level=0`：放行 Nav2。
  - `level=1`：减速并叠加避障转向。
  - `level=2`：停车，随后按原逻辑原地避障转向。

现场验证命令：
```bash
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
ros2 topic echo /depth/baseline_ready
ros2 topic echo /depth_obstacle
```

判断方式：
- `/cmd_vel_nav` 有速度，但 `/cmd_vel_safe` 为 0：问题在融合层或视觉避障状态。
- `/cmd_vel_nav` 本身变 0：问题在 Nav2 控制器、路径、costmap、progress checker 或目标状态。
- `/cmd_vel_safe` 有速度但车不动：问题在 `chassis_node` 串口发送、网页“串口运动指令”开关或 STM32 控制权。

Windows 侧验证：
```bash
python -m py_compile lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py
```

Ubuntu/树莓派侧生效方式：
```bash
cd ~/CCCCAAAARRRR
./open_all.sh
```

说明：
- `open_all.sh` 会自动执行 `colcon build --packages-select lidar_py`，所以重启一键脚本后该参数会安装到 ROS2 工作空间中。

### 11.10 最新复查标签：NAV2_DWB_REAL_CAR_STABILITY_2026-07-09

问题现象：
- 用户反馈：网页点击目标并确认导航后，小车仍可能只动一下、往前走一点后停止，没有到达目标点。
- 需要排除“Nav2 自己输出停了”的情况，而不仅仅检查视觉融合层。

判断：
- 当前系统使用 ROS2 Foxy 标准 Nav2：
  - `NavfnPlanner`
  - `DWBLocalPlanner`
  - 标准 `bt_navigator`
- 没有自研 Nav2 核心算法。
- 但之前的 Nav2 参数对真实四轮差速车偏硬：
  - `progress_checker` 要求 8 秒内移动 0.20m，实车起步慢、避障减速、地面打滑或 odom 抖动时容易误判无进展。
  - DWB 允许零速度候选，且 `PathAlign`、`GoalAlign`、`RotateToGoal` 权重偏大，可能更倾向原地调姿或停顿。
  - 局部/全局 costmap 膨胀半径 0.50m，对 520mm 宽车体和走廊环境偏保守，容易让局部规划觉得可行空间不足。

本次修改 `config/nav2_params.yaml`：
- `progress_checker`
  - `required_movement_radius: 0.20 -> 0.06`
  - `movement_time_allowance: 8.0 -> 15.0`
- `goal_checker`
  - `xy_goal_tolerance: 0.18 -> 0.22`
  - `yaw_goal_tolerance: 0.22 -> 0.70`
- `DWBLocalPlanner`
  - 降低角速度范围：`max_vel_theta: 1.4 -> 1.2`
  - 降低加速度/减速度，减少急停急转导致的打滑：
    - `acc_lim_x: 0.9 -> 0.55`
    - `acc_lim_theta: 2.2 -> 1.5`
    - `decel_lim_x: -1.2 -> -0.8`
    - `decel_lim_theta: -2.8 -> -1.8`
  - 增加采样：
    - `vx_samples: 14 -> 18`
    - `vtheta_samples: 32 -> 36`
  - 增大预测时间：`sim_time: 1.25 -> 1.6`
  - 放宽 TF 容忍：`transform_tolerance: 0.2 -> 0.4`
  - 关闭短路评分：`short_circuit_trajectory_evaluation: True -> False`
  - 降低对完美贴线/完美朝向的执着：
    - `PathAlign.scale: 28.0 -> 12.0`
    - `GoalAlign.scale: 20.0 -> 8.0`
    - `RotateToGoal.scale: 24.0 -> 8.0`
  - 增大前视点：`forward_point_distance: 0.24 -> 0.36`
- costmap
  - 局部膨胀：`inflation_radius: 0.50 -> 0.32`
  - 全局膨胀：`inflation_radius: 0.50 -> 0.35`
  - `cost_scaling_factor: 3.0 -> 4.5`
- planner
  - `NavfnPlanner tolerance: 0.25 -> 0.45`

本次修改 `lidar_py/safety_fusion_node.py`：
- 新增低频状态日志参数：
```text
status_log_period_sec: 1.0
```
- 每秒打印一次：
```text
FUSION_STATUS nav_alive=... nav=(vx,wz) safe=(vx,wz) depth_alive=... level=... baseline_ready=...
```
- 用途：现场能直接判断是 Nav2 自己停了，还是融合层/视觉层把速度清零。

现场判断：
```bash
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
```

或者看终端：
```text
FUSION_STATUS nav=(+0.120,+0.050) safe=(+0.000,+0.000)
```
说明 Nav2 还在给速度，但融合层/视觉状态把输出清零。

```text
FUSION_STATUS nav=(+0.000,+0.000) safe=(+0.000,+0.000)
```
说明 Nav2 自己已经停止输出，需要继续查 costmap、controller、目标状态或 TF/odom。

Windows 侧验证：
```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py
```

验证结果：
- Python 语法检查通过。
- Windows 本机没有 PyYAML，未做 YAML 库解析；已人工检查 `nav2_params.yaml` 关键缩进和参数段。

### 11.11 最新修改标签：WEB_PLAN_AND_8WAY_REMOTE_2026-07-09

问题现象：
- 网页地图只能显示目标点/预览状态，确认导航后看不到 Nav2 实际规划路线。
- 网页前进/后退/旋转按钮体感仍然偏慢。
- 手动遥控只有 5 向：前、后、左旋、右旋、停止，缺少左前/右前/左后/右后的差速弧线控制。

原因判断：
- 前端原来只订阅 `/web/preview_path`，没有订阅 RViz/Nav2 使用的 `/plan`。
- 网页按钮虽然发布到 `/cmd_vel_nav`，但 `safety_fusion_node` 的显式参数 `max_v=0.26` 会把手动速度夹到很低，所以之前改前端速度不明显。
- STM32 PS2 手柄侧四档倍率为 `1/2/5/30`，基础速度 `15,000,000 cnt/s`，原地旋转基础速度 `10,000,000 cnt/s`；网页旧配置没有按这个逻辑映射。

本次修改：
- 网页新增订阅：
```text
/plan                 nav_msgs/Path   Nav2 实际全局规划路线，网页画蓝色线
/web/preview_path     nav_msgs/Path   未确认目标时的预览路线，网页画绿色线
```
- `MapRenderer` 新增 `navPath`，清除目标时会同时清除目标点、绿色预览线和蓝色 Nav2 路线。
- 地图路径绘制新增深色描边，蓝线/绿线在灰度地图上更容易看清。
- 遥控按钮改成 3x3 八向：
```text
左前   前进   右前
左旋   停止   右旋
左后   后退   右后
```
- 前端 `/cmd_vel_nav` 映射：
  - `forward/backward`：只给 `linear.x`
  - `turn_left/turn_right`：只给 `angular.z`
  - `forward_left/forward_right/backward_left/backward_right`：同时给 `linear.x` 和 `angular.z`，形成差速弧线转弯
  - 斜向按钮按 STM32 PS2 摇杆满转向的等效运动学处理：外侧轮约等于当前档速度，内侧轮约为 0，避免外侧轮速度翻倍后被 100M 上限硬削顶。
- 网页四档速度按 STM32 PS2 参数换算：
```text
1挡 15M cnt/s
2挡 30M cnt/s
3挡 75M cnt/s
4挡 450M cnt/s
```
- 注意：网页通过 `/cmd_vel` 运动学进入上位机安全层，实际轮速仍受 `max_wheel_cnt=100000000` 保护，所以 4 挡不会把 450M 原样直接写给电机。
- 放宽 `safety_fusion_node` 手动/融合输出上限：
```text
max_v: 0.26 -> 1.8
max_w: 1.6  -> 2.8
```
- Nav2 自身 `nav2_params.yaml` 的自动导航速度未提高，自动导航仍保持之前的低速稳定参数；本次主要解决网页手动遥控被融合层夹死的问题。

修改文件：
- `web/index.html`
- `web/src/style.css`
- `web/src/config/gearConfig.js`
- `web/src/control/motionController.js`
- `web/src/ros/publishers.js`
- `web/src/ros/topics.js`
- `web/src/ros/subscribers.js`
- `web/src/main.js`
- `web/src/map/mapRenderer.js`
- `web/src/map/poseRenderer.js`
- `lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py`

验证结果：
```bash
cd C:/Users/Administrator/Desktop/CCCCAAAARRRR/web
npm run build
```
结果：Vite 构建通过。

```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py
```
结果：Python 语法检查通过。

### 11.16 最新修改标签：NAVI_VX_SCALE_AND_SKID_COMPENSATION_2026-07-09

本次目标：
- 解释并处理“伺服编码器算出来的 vx 为什么仍然可能导致建图重叠/走廊延伸”。
- 给上位机增加可调的 `vx` 标定和四轮差速转弯打滑补偿参数。

#### 1. 为什么编码器 vx 也可能不等于真实车体位移

编码器本身可以很准，但它测的是：
```text
轮子/电机转了多少
```

SLAM 需要的是：
```text
车体相对于地面真实移动了多少
```

四轮差速车在以下情况会出现明显偏差：
- 原地转弯或小半径转弯时，四个轮子会横向搓地。
- 地面材质、载重、轮胎压缩会让有效轮径变化。
- 左右/前后轮实际半径和摩擦不完全一致。
- 伺服编码器在电机侧，经过减速箱、轮胎、地面接触后才是车体真实位移。
- `四轮速度相加/4` 只适合无打滑直线运动，转弯时会高估或低估车体前进位移。

所以结论是：
```text
编码器 vx 可以作为短中期预测，但不能直接当长期真值。
长期地图闭合仍然应该交给 LiDAR scan matching / loop closure 修正。
```

#### 2. 新增 vx 标定参数

修改文件：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py`

新增参数：
```text
navi_vx_scale: 1.0
navi_vx_deadband_mps: 0.003
navi_turn_vx_scale: 0.75
navi_turn_wz_threshold_rad_s: 0.25
```

含义：
- `navi_vx_scale`：直线速度整体比例标定。
- `navi_vx_deadband_mps`：极小速度置零，降低静止抖动。
- `navi_turn_vx_scale`：转弯时前进位移折减，补偿四轮差速横向打滑。
- `navi_turn_wz_threshold_rad_s`：角速度超过这个值才认为正在转弯。

当前默认：
```text
直线 vx 不缩放：navi_vx_scale = 1.0
转弯时 vx 折减到 75%：navi_turn_vx_scale = 0.75
```

#### 3. 现场标定方法

直线标定：
```text
1. 在平整地面拉尺，让车直行 2.000m。
2. 记录 /odom 里 x/y 的位移长度。
3. 如果 /odom 显示走了 2.20m，但实际只有 2.00m：
   navi_vx_scale = 2.00 / 2.20 = 0.909
4. 如果 /odom 显示走了 1.80m，但实际是 2.00m：
   navi_vx_scale = 2.00 / 1.80 = 1.111
```

转弯/走廊标定：
```text
1. 先把直线 navi_vx_scale 标准。
2. 再沿走廊做 S 弯或进出房间。
3. 如果转弯后地图总是被拉长，说明转弯时 vx 仍然偏大：
   降低 navi_turn_vx_scale，例如 0.75 -> 0.65。
4. 如果转弯后地图被压缩，说明转弯时 vx 偏小：
   提高 navi_turn_vx_scale，例如 0.75 -> 0.85。
```

临时启动调参示例：
```bash
ros2 launch lidar_py slam_nav_fusion_launch.py \
  navi_odom_yaw_source:=gyro
```

当前 launch 内部默认已经设置：
```text
navi_vx_scale = 1.0
navi_turn_vx_scale = 0.75
```

如果后续要把这几个参数暴露成 launch 命令行参数，可以继续把它们做成 `DeclareLaunchArgument`。

验证：
```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py
```

结果：Python 语法检查通过。

### 11.13 最新修改标签：DEPTH_OBSTACLE_DYNAMIC_SIZE_FUSION_2026-07-09

本次目标：
- 继续优化视觉避障，让它不是只按“有障碍/无障碍”粗暴处理，而是能根据障碍物距离、障碍物面积、左右两侧风险差动态调整转向幅度。
- 距离阈值继续读取 `road_v5_5_640_modular_v7_unified_io/calibration_640.py` 里的 `STOP_VALUE / WARN_VALUE / FAR_VALUE`，后续标定改距离后不用再改融合代码。
- 保持 `/depth_obstacle` 前 8 个字段兼容旧逻辑，追加 6 个字段给融合节点使用。

#### 1. /depth_obstacle 数据帧扩展

修改文件：
- `road_v5_5_640_modular_v7_unified_io/obstacle_vision.py`
- `road_v5_5_640_modular_v7_unified_io/depth_obstacle_node.py`
- `lidar/chapt1_ws/src/lidar_py/lidar_py/vision_obstacle_node.py`

新的 `/depth_obstacle` 格式：
```text
data[0]  depth_level          0=安全 1=预警 2=危险
data[1]  depth_dir            -1=建议走左边 0=无偏向 1=建议走右边
data[2]  nearest_mm           最近障碍距离(mm)
data[3]  center_danger        中间红/黄区是否有障碍 0/1
data[4]  center_far           中间绿区是否有障碍 0/1
data[5]  left_blocked         左侧通道是否有障碍 0/1
data[6]  right_blocked        右侧通道是否有障碍 0/1
data[7]  seq                  帧序号
data[8]  center_area_x1000    中间通道障碍面积占比 * 1000
data[9]  total_area_x1000     全部ROI障碍面积占比 * 1000
data[10] left_score_x1000     左侧通道障碍风险分数 * 1000
data[11] right_score_x1000    右侧通道障碍风险分数 * 1000
data[12] center_offset_x1000  右侧风险 - 左侧风险，正数代表右侧更堵
data[13] center_min_mm        中间通道最近障碍距离(mm)
```

说明：
- 前 8 个字段不变，所以旧的 echo、调试窗口、旧节点不会因为字段变长直接失效。
- SDK 版 `depth_obstacle_node.py` 和 ROS 内置版 `vision_obstacle_node.py` 都已经统一输出 14 个字段。
- `obstacle_vision.py` 的 `combine_stats()` 增加了 `roi_area / obstacle_ratio / obstacle_area_ratio`，用于估算障碍物占据面积。
- 左右风险分数里的距离增益读取 `calibration_640.py` 的 `FAR_VALUE`，不再写死 950mm。

#### 2. 视觉避障动态转向融合

修改文件：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py`

核心变化：
- 融合节点现在会解析 `center_area_x1000 / total_area_x1000 / left_score_x1000 / right_score_x1000 / center_min_mm`。
- `_width_risk()` 不再只看 `left_blocked/right_blocked`，而是综合：
```text
中间障碍面积风险
全部ROI障碍面积风险
左右通道障碍风险分数
原有左右blocked状态
```
- `_choose_dir()` 会优先比较左右风险分数：
```text
左侧更堵 -> 往右绕
右侧更堵 -> 往左绕
左右差不明显 -> 再使用原来的 preferred_dir / blocked / Nav2 角速度方向
```
- `_effective_nearest()` 会同时参考 `nearest_mm` 和 `center_min_mm`，避免中心区域已经很近但全局 nearest 不稳定时判断偏轻。
- `FUSION_STATUS` 日志新增：
```text
area=center/total
score=left/right
offset=right-left
center_min=...
```

现场调试建议：
```bash
ros2 topic echo /depth_obstacle
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
```

判断方法：
- `/depth_obstacle` 后 6 个字段明显增大，`/cmd_vel_safe.angular.z` 也随之增大：动态转向已经生效。
- 障碍远、小：小幅降速、小角度绕。
- 障碍近、大：明显降速、大角度绕。
- 两侧都堵且距离特别近：才会短暂停止或原地脱困。

#### 3. 半自动标定脚本说明更新

修改文件：
- `road_v5_5_640_modular_v7_unified_io/semi_auto_calibrate_640_with_guide.py`

结论：
- 本次算法没有改变点位数量，仍然是原来的 16 个标定点。
- 新增的障碍面积和左右风险分数来自原有 9 个 ROI 的深度点统计，不需要额外标点。

使用方法：
```bash
cd ~/CCCCAAAARRRR/road_v5_5_640_modular_v7_unified_io
python3 semi_auto_calibrate_640_with_guide.py
```

操作：
```text
f      冻结/恢复画面
鼠标左键 按提示依次点击当前标定点
z      撤销上一个点
r      清空重来
s      保存并输出 calibration_output_640.py/json
q/ESC  退出
```

点位顺序仍然是：
```text
NEAR:  左外 / 中左 / 中右 / 右外
45cm:  左外 / 中左 / 中右 / 右外
55cm:  左外 / 中左 / 中右 / 右外
95cm:  左外 / 中左 / 中右 / 右外
```

如果只想改避障距离，不用重新跑标定脚本，直接改：
```text
road_v5_5_640_modular_v7_unified_io/calibration_640.py

STOP_VALUE = 450
WARN_VALUE = 550
FAR_VALUE  = 950
```
单位是 mm。改完重启 `open_all.sh` 即可。

验证：
```bash
python -m py_compile \
  road_v5_5_640_modular_v7_unified_io/obstacle_vision.py \
  road_v5_5_640_modular_v7_unified_io/depth_obstacle_node.py \
  road_v5_5_640_modular_v7_unified_io/semi_auto_calibrate_640_with_guide.py \
  lidar/chapt1_ws/src/lidar_py/lidar_py/vision_obstacle_node.py \
  lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py
```

### 11.12 最新修改标签：NAV2_VISION_SMOOTH_FUSION_REAL_SIZE_2026-07-09

本次目标：
- 按真实车体尺寸 `长 480mm、宽 520mm、轮径 152mm` 继续优化 Nav2 和视觉避障。
- 解决 Nav2 自动导航前进速度过慢的问题。
- 解决视觉避障与 Nav2/网页遥控配合不够顺滑，近障时容易原地掉头的问题。
- 让视觉避障距离风险尽量跟随视觉配置文件，而不是在融合层写死一套距离。

#### 1. Nav2 自动导航速度与控制器优化

修改文件：
- `lidar/chapt1_ws/src/lidar_py/config/nav2_params.yaml`

核心变化：
```text
max_vel_x:       0.26 -> 0.42
max_speed_xy:    0.26 -> 0.42
max_vel_theta:   1.20 -> 1.35
acc_lim_x:       0.55 -> 0.75
decel_lim_x:    -0.80 -> -1.00
vx_samples:      18   -> 22
vtheta_samples:  36   -> 40
sim_time:        1.6  -> 1.7
```

解释：
- 自动导航速度从很慢的 `0.26m/s` 提到 `0.42m/s`，但仍明显低于网页手动遥控高档位，避免建图/定位/走廊环境下过激。
- DWB 采样数增加，局部规划会有更多速度候选，路径更不容易“动一下就犹豫”。
- `PathAlign/GoalAlign/RotateToGoal` 权重略降，减少一直原地调姿、贴线过度导致的停顿。
- 局部代价地图窗口从 `4m x 4m` 增到 `5m x 5m`，给走廊和绕障时更多前视空间。
- footprint 继续使用真实车体半长半宽：
```text
[[-0.24, -0.26], [-0.24, 0.26], [0.24, 0.26], [0.24, -0.26]]
```

#### 2. 视觉避障与 Nav2/网页遥控融合优化

修改文件：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py`

旧逻辑问题：
- `/depth_obstacle level>=2` 时，融合层基本会先停，然后进入原地转向。
- 网页按住前进测试视觉避障时，看起来容易变成“原地掉头”，不是“继续向前差速绕过去”。

新逻辑：
- 融合层根据最近障碍距离 `nearest_mm` 计算 `risk`。
- 同时根据 `left_blocked/right_blocked` 计算障碍宽度风险。
- `level=1`：继续前进，但按距离和宽度动态降速、动态加大转向偏置。
- `level=2`：只要仍有前进指令，就优先小速度大转向差速绕行；只有特别近、左右都堵、或者没有前进速度时，才停车/原地脱困。
- 障碍消失后，转向偏置按 `return_w_slew_rate` 平滑回正，避免突然甩回来。

新增/调整参数：
```text
avoid_v_min: 0.10
avoid_v_max: 0.42
avoid_w_min: 0.35
avoid_w_max: 1.55

danger_v_min: 0.04
danger_v_max: 0.18
danger_w_min: 1.05
danger_w_max: 2.35

danger_stop_hold_sec: 0.12
blocked_spin_delay_sec: 0.90
return_w_slew_rate: 2.4
avoid_w_slew_rate: 4.2
obstacle_width_w_gain: 0.35
```

#### 3. 视觉距离阈值读取配置

修改文件：
- `open_all.sh`
- `safety_fusion_node.py`

一键启动脚本新增：
```bash
export VISION_CODE_DIR="$ROAD_DIR"
```

融合层启动时会优先尝试从：
```text
road_v5_5_640_modular_v7_unified_io/calibration_640.py
```
读取：
```text
STOP_VALUE
WARN_VALUE
FAR_VALUE
```

当前配置里对应：
```text
STOP_VALUE = 450mm
WARN_VALUE = 550mm
FAR_VALUE  = 950mm
```

融合层会用这些值生成：
```text
critical / danger / warning
```
并在日志中打印：
```text
FUSION_STATUS ... nearest=... risk=... thresholds=(critical/danger/warning)
```

这样以后视觉标定改了 `calibration_640.py` 的距离阈值，重启一键脚本后融合层会跟随配置变化。

#### 4. 最近几次网页/导航/通信修改总览

已经完成的关键链路：
```text
网页目标点 -> /web/preview_goal -> /web/preview_path 绿色预览路线
网页确认导航 -> /web/nav_goal -> Nav2 NavigateToPose
Nav2 全局规划 -> /plan -> 网页蓝色实际路线
Nav2 / 网页遥控 -> /cmd_vel_nav
视觉避障 -> /depth_obstacle
/cmd_vel_nav + /depth_obstacle -> safety_fusion_node -> /cmd_vel_safe
chassis_node -> AA55 MOVE -> STM32
STM32 NAVI/echo -> chassis_node -> /odom / serial_debug / robot_pose
```

最近几次重点改动：
- 网页地图新增目标点选择、确认导航、清除目标。
- 网页新增 `/plan` 蓝色实际路径显示，`/web/preview_path` 绿色预览路径保留。
- 网页遥控从 5 向改为 8 向，新增左前/右前/左后/右后差速弧线按钮。
- 网页遥控四档按 STM32 PS2 参数换算：`15M / 30M / 75M / 450M cnt/s`，但仍受上位机 `max_wheel_cnt=100M` 保护。
- 底盘融合层放开手动遥控上限，避免网页按钮被 `0.26m/s` 夹死。
- `/robot_pose` 网页小车图标朝向改为跟随 `map->base_link`，尽量和 RViz 中地图坐标系一致。
- Nav2 网页目标桥 `web_goal_nav_node.py` 负责把网页点击点转成 Nav2 action。
- `web_path_preview_node.py` 负责未确认目标时实时预览路径。
- 视觉深度图、ROI、障碍点、串口发送、回响、陀螺仪等调试开关已经接入网页运行时控制。

现场调试建议：
```bash
ros2 topic echo /cmd_vel_nav
ros2 topic echo /cmd_vel_safe
ros2 topic echo /depth_obstacle
```

判断方法：
- `/cmd_vel_nav` 有速度、`/cmd_vel_safe` 变小但不为 0：视觉避障正在介入，属于正常差速绕障。
- `/cmd_vel_nav` 有速度、`/cmd_vel_safe` 长时间为 0：视觉认为太近或 baseline/深度状态阻塞，需要看 `FUSION_STATUS` 的 `level/nearest/risk/thresholds`。
- `/cmd_vel_nav` 本身为 0：问题在 Nav2 控制器、目标状态、costmap 或 TF/定位，不是视觉融合层。

验证：
```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py
```
结果：Python 语法检查通过。
### 11.14 最新修改标签：SLAM_LOG_AND_BASELINE_FIX_2026-07-09

本次目标：
- 新增 SLAM 日志系统：每 0.5s 保存地图 + TF 位姿 + STM32 陀螺仪数据
- 网页新增 SLAM 日志开关按钮
- 修复 baseline 锁定逻辑：不点击"采集深度校准"车不能动

#### 1. SLAM 日志系统

新增文件：
- `slam_logger.py` — ROS2 节点，订阅 `/map`、`/robot/serial_debug`、TF
- `open_all_log.sh` — 一键启动脚本，在 open_all.sh 基础上增加 slam_logger 启动

输出结构：
```text
SLAM_Log/YYYY-MM-DD_HH-MM-SS/
├── Maps/
│   ├── MM-DD  HH:MM:SS.s.pgm   ← 干净灰度地图（SLAM 用）
│   ├── MM-DD  HH:MM:SS.s.yaml  ← 地图元数据
│   ├── MM-DD  HH:MM:SS.s.png   ← 彩色版，红色三角形箭头 + 绿色光晕显示朝向
│   └── robot_pose.jsonl        ← 每帧 {x, y, yaw_deg, yaw_rad}
└── 陀螺仪/
    ├── 原始.json               ← STM32 NAVI 帧 hex dump
    └── 解析.json               ← yaw/vx/vz 解析值
```

命名格式与 SLAM_Log 模板一致：地图文件名 `MM-DD  HH:MM:SS.s`，陀螺仪条目 `MM/DD  HH:MM:SS.s`。

slam_logger 特性：
- 默认关闭，由网页按钮控制开启/停止
- 订阅 `/robot/web_control`，监听 `slam_log_enable` / `slam_log_disable` 命令
- 地图 PNG 上画红色三角形（尖头朝车头方向）+ 绿色光晕描边，白色外圈红心圆点标记中心
- 位置和朝向从 TF `map→base_link` 实时获取
- 依赖 Pillow (PIL)，未安装时退化为纯 PGM 输出

使用方法：
```bash
cd ~/CCCCAAAARRRR
./open_all_log.sh
```
启动后网页点击 "SLAM Log OFF" 按钮变绿即开始记录。

#### 2. 网页 SLAM 日志开关

修改文件：
- `web/index.html` — 在 STM32 AA55 区域的"回响OFF"按钮旁新增 `SLAM Log OFF` 按钮
- `web/src/main.js` — 新增 `state.slamLogEnabled` 状态和点击切换逻辑
- `web/src/ros/publishers.js` — 新增 `publishSlampLogControl(enabled)` 方法，发布 `slam_log_enable` / `slam_log_disable` 命令到 `/robot/web_control`
- `web/src/style.css` — 新增 `.button-on` 样式，激活时绿色背景

按钮行为：
- 默认灰色 "SLAM Log OFF"
- 点击变绿 "SLAM Log ON"，通过 rosbridge 控制 slam_logger 开始记录
- 再次点击变回灰色，停止记录

#### 3. Baseline 锁定逻辑修复

问题现象：
- `require_depth_baseline: True` 已设置，但车在未采集基线时仍能运动。

根因：
- `safety_fusion_node.py` 中 `baseline_seen = False` 初始值。
- open_all.sh 启动顺序：第3步启动 safety_fusion_node，**12秒后**才启动 depth_obstacle_node。
- 这12秒窗口内 `/depth/baseline_ready` 无人发布，`baseline_seen` 保持 False，锁条件不成立。
- 锁条件：`require_depth_baseline=True AND baseline_seen=True AND baseline_ready=False`。

修复：

修改 `lidar/chapt1_ws/src/lidar_py/lidar_py/safety_fusion_node.py`：
```python
- self.baseline_seen = False
+ self.baseline_seen = True  # lock active from startup
```

修改 `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`：
```text
- require_depth_baseline: False
+ require_depth_baseline: True
```

修复后行为：
| 时间 | baseline_seen | baseline_ready | 锁状态 |
|------|:--:|:--:|:--:|
| safety_fusion 启动 | True | False（默认） | **锁死** |
| depth_obstacle 启动 | True | False（无基线） | **锁死** |
| 点击"采集深度校准" | True | False→True | **解锁** |

验证：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
colcon build --packages-select lidar_py
```

```bash
cd ~/CCCCAAAARRRR/web
npm run build
```

结果：ROS colcon build + 网页 Vite build 均通过。

### 11.15 最新修改标签：SLAM_ODOM_YAW_SOURCE_SCAN_MATCH_FIX_2026-07-09

本次目标：
- 解决建图长期漂移、回到走廊后地图继续向外延伸的问题。
- 不再把 JY901B 积分出来的 yaw 当成长期稳定绝对方位角。
- 将下位机 NAVI 帧中的 yaw 保留为调试数据，但默认不再直接覆盖 `odom->base_link` 的朝向。

#### 1. 底盘 odom 的 yaw 来源调整

修改文件：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py`

旧逻辑：
```text
NAVI yaw -> 直接写入 odom_theta -> 发布 odom->base_link
```

问题：
- JY901B 在无磁力计长期参与时，本质是陀螺积分，yaw 会随时间漂移。
- SLAM 拿这个漂移 yaw 当 odom 先验，会导致回环时 scan matching 被错误初值拖偏。
- 典型现象就是回到走廊后，地图没有闭合，反而继续向外延伸一段走廊。

新逻辑：
```text
NAVI vx        -> 短中期前进里程
NAVI vz/gz     -> 短时间角速度积分
NAVI yaw       -> 仅保留为调试/日志/IMU数据，不默认覆盖 odom
LiDAR SLAM     -> 长期 scan matching + 回环优化，修正 map->odom
```

新增参数：
```text
navi_odom_yaw_source: gyro      # 默认，使用 vz/gz 短时间积分
navi_odom_yaw_source: absolute  # 旧逻辑，直接使用 NAVI yaw
navi_vz_deadband_deg_s: 0.15    # 小角速度死区，降低静止抖动
```

说明：
- 默认 `gyro` 模式下，`odom->base_link` 是短期预测，不再声明自己是长期绝对姿态。
- `/robot/serial_debug` 中的 `yaw_deg/yaw_deg_ros/yaw_deg_unwrapped_ros` 仍然保留，便于分析下位机漂移。
- `/imu` 中仍会发布 NAVI yaw/vz，但 SLAM 主链路不靠它做长期绝对方位。

#### 2. Launch 参数同步

修改文件：
- `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py`

同步传入：
```text
navi_odom_yaw_source:=gyro
navi_vz_deadband_deg_s:=0.15
```

如果现场需要临时回到旧逻辑，可以启动时加：
```bash
ros2 launch lidar_py slam_nav_fusion_launch.py navi_odom_yaw_source:=absolute
```

#### 3. slam_toolbox 参数调整

修改文件：
- `lidar/chapt1_ws/src/lidar_py/config/mapper_params_online_async.yaml`

关键变化：
```text
minimum_travel_distance: 0.05 -> 0.10
minimum_travel_heading:  0.05 -> 0.08
scan_buffer_size:        10   -> 30
link_scan_maximum_distance: 1.5 -> 2.0
loop_search_maximum_distance: 3.0 -> 8.0
loop_search_space_dimension: 8.0 -> 12.0
distance_variance_penalty: 0.5 -> 0.35
angle_variance_penalty:    1.0 -> 0.55
```

目的：
- 少插入由 odom 抖动造成的过密 scan 节点。
- 给回环检测更大的搜索范围，避免“回来后认不出自己来过”。
- 降低 odom 角度先验权重，让雷达 scan matching 更能修正 yaw 漂移。

#### 4. 现场判断方法

测试时重点看：
```bash
ros2 topic echo /odom
ros2 run tf2_ros tf2_echo map base_link
ros2 topic echo /robot/serial_debug
```

判断：
- `/robot/serial_debug` 的 yaw 可以慢慢漂，但 `/odom` 不应被它突然拖着跳。
- 短距离运动靠 `/odom` 连续预测。
- 长期回到已探索区域时，`map->odom` 应由 slam_toolbox 拉回闭合，而不是继续延伸新走廊。

验证：
```bash
python -m py_compile \
  lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py \
  lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py
```

结果：Python 语法检查通过。
### 11.17 最新修改标签：AMCL_LIDAR_MAP_CORRECTION_2026-07-09

本次目标：
- 解决“编码器/里程计认为前进 2.2m，但建好的地图中激光匹配只应该是 2.0m”这类问题。
- 在**建完图后的导航/定位模式**中，让激光雷达匹配已有地图来长期修正 `map->odom`。
- 不要求一套固定 `vx` 标定值适配所有地面，而是降低 AMCL 对轮式里程计的绝对信任。

核心说明：
```text
odom->base_link 由底盘编码器/NAVI 短期预测
map->odom       由 AMCL 根据 /scan 和已保存地图长期修正
map->base_link  = map->odom + odom->base_link
```

也就是说，导航模式下如果轮速积分多算了 20cm，只要激光和地图能稳定匹配，AMCL 会通过调整 `map->odom` 把车体在地图中的位置拉回正确位置。

修改文件：
- `lidar/chapt1_ws/src/lidar_py/config/nav2_params.yaml`

关键 AMCL 参数调整：
```text
alpha1: 0.20 -> 0.35
alpha2: 0.20 -> 0.35
alpha3: 0.20 -> 0.45
alpha4: 0.20 -> 0.35
alpha5: 0.20 -> 0.35

do_beamskip: false -> true
laser_likelihood_max_dist: 2.0 -> 2.5
max_beams: 90 -> 120
min_particles: 500 -> 800
max_particles: 2000 -> 3500
pf_err: 0.05 -> 0.04
recovery_alpha_fast: 0.0 -> 0.05
recovery_alpha_slow: 0.0 -> 0.005
sigma_hit: 0.20 -> 0.14
update_min_d: 0.10 -> 0.05
update_min_a: 0.10 -> 0.05
z_hit: 0.50 -> 0.72
z_rand: 0.50 -> 0.20
z_short: 0.05 -> 0.04
z_max: 0.05 -> 0.04
```

效果预期：
- AMCL 粒子分布会更能覆盖编码器打滑造成的误差。
- 激光命中已有地图的权重提高，长期定位更依赖雷达匹配。
- 小距离、小角度运动也会更频繁更新定位，避免走廊里误差积累太久。

注意：
- 这只对**已建图导航/定位**有效。
- 纯建图时没有固定地图可对齐，只能靠 `slam_toolbox` 的 scan matching 和 loop closure；因此建图阶段仍然要慢速、避免猛转，并尽量让走廊/房间有足够特征。
- 如果环境非常空旷、走廊两边高度相似，激光匹配本身也会退化，这时需要增加环境特征或采用更强的定位方案。
### 11.18 最新修改标签：RF2O_SWITCHABLE_ODOM_FRONTEND_2026-07-10

本次目标：
- 在建图阶段引入 2D 激光里程计 RF2O，让雷达连续帧参与估计底盘运动。
- 做成可切换模式，避免一次性替换已经能跑的轮速 odom。
- 保持 `slam_toolbox` 继续负责建图、scan matching、pose graph 和回环优化。

新增源码：
- `lidar/chapt1_ws/src/rf2o_laser_odometry`
  - 使用 MAPIRlab 的 ROS2 分支 RF2O。
  - 参数来源与源码一致：`laser_scan_topic`、`odom_topic`、`base_frame_id`、`odom_frame_id`、`publish_tf`、`init_pose_from_topic`、`freq`。
  - 修正了 RF2O 空初始姿态时 `orientation.w=0` 的问题，改为合法单位四元数 `orientation.w=1`。

新增节点：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/odom_fusion_node.py`
- `setup.py` 新增入口：
```text
odom_fusion_node = lidar_py.odom_fusion_node:main
```

新增模式：
```text
odom_mode:=wheel   # 默认，保持原来的 chassis_node 直接发布 /odom + odom->base_link
odom_mode:=laser   # RF2O /odom_rf2o 作为最终 /odom，先单独测试激光里程计效果
odom_mode:=fused   # chassis /wheel/odom + RF2O /odom_rf2o 融合后发布最终 /odom
```

TF/Topic 结构：
```text
wheel 模式：
  chassis_node -> /odom + odom->base_link

laser/fused 模式：
  chassis_node -> /wheel/odom，不发布 odom->base_link
  rf2o_laser_odometry -> /odom_rf2o，不发布 TF
  odom_fusion_node -> /odom + odom->base_link
```

注意：最终只能有一个节点发布 `odom->base_link`。本次已经通过 `publish_tf` 和 `odom_topic` 参数把它切开，避免 TF 冲突。

修改文件：
- `lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py`
  - 新增参数 `odom_topic`，默认 `/odom`。
  - 支持在辅助模式下发布 `/wheel/odom`，并通过 `publish_tf:=false` 关闭 TF。
- `lidar/chapt1_ws/src/lidar_py/lidar_py/odom_fusion_node.py`
  - 新增可切换前端里程计节点。
- `lidar/chapt1_ws/src/lidar_py/setup.py`
  - 注册 `odom_fusion_node`。
- `lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py`
- `lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py`
  - 新增 `odom_mode` 参数。
  - `wheel` 默认保持原链路。
  - `laser/fused` 自动启动 RF2O 和 `odom_fusion_node`。

推荐测试顺序：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
rosdep install --from-paths src --ignore-src -r -y
colcon build --symlink-install
source install/setup.bash
```

1. 原版轮速模式，确认没有被破坏：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=wheel
```

2. 只测 RF2O 激光里程计：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=laser
```

观察：
```bash
ros2 topic echo /odom_rf2o
ros2 topic echo /odom
ros2 run tf2_ros tf2_echo odom base_link
```

如果 `odom_mode:=laser` 下静止时基本不漂、慢速前进和转弯方向正确，再测融合模式。

3. 最终融合建图：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=fused
```

4. 建图 + Nav2 + 视觉避障融合：
```bash
ros2 launch lidar_py slam_nav_fusion_launch.py odom_mode:=fused enable_vision:=true
```

5. 已建图导航：
```bash
ros2 launch lidar_py nav2_launch.py map:=~/maps/lab_map.yaml odom_mode:=fused
```

现场判断：
- 如果 `laser` 模式比 `wheel` 明显少拉伸走廊，说明 RF2O 有效。
- 如果 `laser` 模式在空旷走廊中跳动，先使用 `fused`，因为轮速可以提供短时间连续性。
- 如果 `fused` 仍然偶尔跳，优先调 `odom_fusion_node` 参数：
```text
laser_xy_weight: 0.70   # 越大越信 RF2O 的平移
laser_yaw_weight: 0.55  # 越大越信 RF2O 的转角
max_laser_step_m: 0.40
max_laser_step_yaw_rad: 0.90
```

本次没有修改：
- `open_all.sh`
- `open_all_log.sh`

参考：
- RF2O: https://github.com/MAPIRlab/rf2o_laser_odometry
- RF2O paper: Planar Odometry from a Radial Laser Scanner, ICRA 2016
- slam_toolbox: https://github.com/SteveMacenski/slam_toolbox
### 11.19 完整说明标签：RF2O_三模式里程计融合结构与使用教程_2026-07-10

本章节是对 11.18 的完整整理，包含本次新增/修改的全部内容、目录结构、数据流、启动方式、测试顺序和注意事项。

#### 1. 本次修改目标

之前建图漂移的核心问题是：
```text
四轮差速小车在转弯、原地旋转、地面摩擦变化时会打滑。
编码器测到的是轮子转了多少，不一定等于车体真实走了多少。
如果 slam_toolbox 前端过度相信轮速 odom，走廊和房间之间容易出现地图拉长、重叠、错位。
```

本次目标：
```text
1. 引入 RF2O 2D 激光里程计，让雷达连续帧参与估计车体位移。
2. 保留原来的轮速 odom，避免直接破坏已经能跑的版本。
3. 新增融合节点，把 wheel odom 和 RF2O laser odom 做成可切换前端。
4. slam_toolbox 不替换，继续负责建图、scan matching、pose graph、回环优化。
5. Nav2/AMCL/视觉避障/串口运动控制路径继续保留。
```

#### 2. 完整目录结构

本次涉及目录：
```text
CCCCAAAARRRR/
├── 修改.md
├── open_all.sh                 # 本次未修改
├── open_all_log.sh             # 本次未修改
└── 激光雷达/
    └── chapt1_ws/
        └── src/
            ├── lidar_py/
            │   ├── package.xml
            │   ├── setup.py
            │   ├── config/
            │   │   ├── mapper_params_online_async.yaml
            │   │   ├── mapper_params_online_sync.yaml
            │   │   └── nav2_params.yaml
            │   ├── launch/
            │   │   ├── slam_launch.py
            │   │   ├── slam_nav_fusion_launch.py
            │   │   ├── nav2_launch.py
            │   │   ├── full_system_launch.py
            │   │   ├── hector_slam_launch.py
            │   │   ├── hector_simple_launch.py
            │   │   ├── laser_odom_slam_launch.py
            │   │   └── lidar_display.launch.py
            │   └── lidar_py/
            │       ├── chassis_node.py
            │       ├── odom_fusion_node.py        # 本次新增
            │       ├── lidar_node.py
            │       ├── safety_fusion_node.py
            │       ├── vision_obstacle_node.py
            │       ├── robot_pose_publisher.py
            │       ├── web_path_preview_node.py
            │       └── web_goal_nav_node.py
            └── rf2o_laser_odometry/               # 本次新增
                ├── package.xml
                ├── CMakeLists.txt
                ├── launch/
                ├── include/
                └── src/
                    ├── CLaserOdometry2D.cpp
                    └── CLaserOdometry2DNode.cpp
```

#### 3. 本次新增/修改文件清单

新增：
```text
lidar/chapt1_ws/src/lidar_py/lidar_py/odom_fusion_node.py
lidar/chapt1_ws/src/rf2o_laser_odometry/
```

修改：
```text
lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py
lidar/chapt1_ws/src/lidar_py/setup.py
lidar/chapt1_ws/src/lidar_py/launch/slam_launch.py
lidar/chapt1_ws/src/lidar_py/launch/slam_nav_fusion_launch.py
lidar/chapt1_ws/src/lidar_py/launch/nav2_launch.py
lidar/chapt1_ws/src/lidar_py/launch/full_system_launch.py
lidar/chapt1_ws/src/rf2o_laser_odometry/src/CLaserOdometry2DNode.cpp
修改.md
```

未修改：
```text
open_all.sh
open_all_log.sh
```

#### 4. 三种里程计模式

新增统一参数：
```text
odom_mode:=wheel
odom_mode:=laser
odom_mode:=fused
```

##### 4.1 wheel 模式

这是默认模式，也是原来的稳定版本。

数据流：
```text
STM32 NAVI / 编码器
        ↓
chassis_node
        ↓
/odom
odom -> base_link
        ↓
slam_toolbox / Nav2 / AMCL
```

特点：
```text
1. 不启动 RF2O。
2. 不启动 odom_fusion_node。
3. chassis_node 直接发布 /odom 和 odom->base_link。
4. 兼容原来的测试流程。
```

启动：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=wheel
```

##### 4.2 laser 模式

用于先单独测试 RF2O 激光里程计效果。

数据流：
```text
/scan
  ↓
rf2o_laser_odometry
  ↓
/odom_rf2o
  ↓
odom_fusion_node(mode=laser)
  ↓
/odom
odom -> base_link
  ↓
slam_toolbox / Nav2 / AMCL
```

同时：
```text
chassis_node 仍然运行，用于串口控制、接收 STM32 数据、发送运动指令。
但在 laser 模式下：
chassis_node -> /wheel/odom
chassis_node 不发布 odom->base_link
```

特点：
```text
1. 最终 /odom 主要来自 RF2O。
2. 可以检查“只靠激光连续帧”建图是否比轮速更稳。
3. 如果环境特征少、走廊太直、雷达点云质量差，RF2O 可能会跳动。
```

启动：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=laser
```

##### 4.3 fused 模式

这是最终推荐目标模式。

数据流：
```text
STM32 NAVI / 编码器
        ↓
chassis_node
        ↓
/wheel/odom

/scan
  ↓
rf2o_laser_odometry
  ↓
/odom_rf2o

/wheel/odom + /odom_rf2o
        ↓
odom_fusion_node(mode=fused)
        ↓
/odom
odom -> base_link
        ↓
slam_toolbox / Nav2 / AMCL
```

特点：
```text
1. 轮速 odom 保留短时间连续性。
2. RF2O 用激光连续帧修正轮速打滑导致的虚假位移。
3. slam_toolbox 继续做后端 scan matching、pose graph、回环优化。
4. 比单纯 vx_scale 更适合不同地面和不同环境。
```

启动：
```bash
ros2 launch lidar_py slam_launch.py odom_mode:=fused
```

#### 5. 为什么最终只能有一个 odom->base_link

ROS TF 中不能有两个节点同时发布同一条 TF：
```text
odom -> base_link
```

否则 RViz、slam_toolbox、Nav2 会收到互相冲突的位姿。

本次处理：
```text
wheel 模式：
  chassis_node 发布 /odom 和 odom->base_link

laser/fused 模式：
  chassis_node 只发布 /wheel/odom，不发布 TF
  rf2o_laser_odometry 只发布 /odom_rf2o，不发布 TF
  odom_fusion_node 统一发布 /odom 和 odom->base_link
```

#### 6. chassis_node 修改说明

文件：
```text
lidar/chapt1_ws/src/lidar_py/lidar_py/chassis_node.py
```

新增参数：
```text
odom_topic: 默认 /odom
publish_tf: 原本已有，继续使用
```

用途：
```text
wheel 模式：
  odom_topic=/odom
  publish_tf=true

laser/fused 模式：
  odom_topic=/wheel/odom
  publish_tf=false
```

这样 `chassis_node` 仍然负责：
```text
1. 打开 STM32 串口
2. 接收 AA55 NAVI 数据
3. 接收回响帧/调试帧
4. 接收 /cmd_vel_safe
5. 给下位机发送运动指令
```

但在 `laser/fused` 模式下，它不再抢最终 `/odom` 和 `odom->base_link`。

#### 7. odom_fusion_node 说明

文件：
```text
lidar/chapt1_ws/src/lidar_py/lidar_py/odom_fusion_node.py
```

输入：
```text
/wheel/odom    # chassis_node 输出的轮速/底盘 odom
/odom_rf2o     # RF2O 输出的激光里程计
```

输出：
```text
/odom
odom -> base_link
/robot/odom_fusion_debug
```

主要参数：
```text
mode: laser 或 fused
wheel_odom_topic: /wheel/odom
laser_odom_topic: /odom_rf2o
output_odom_topic: /odom
odom_frame: odom
base_frame: base_link
publish_tf: true
laser_xy_weight: 0.70
laser_yaw_weight: 0.55
laser_timeout_sec: 0.40
max_laser_step_m: 0.40
max_laser_step_yaw_rad: 0.90
```

融合逻辑简述：
```text
laser 模式：
  直接把 /odom_rf2o 转成最终 /odom。

fused 模式：
  从 /wheel/odom 取轮速增量。
  从 /odom_rf2o 取激光增量。
  按权重融合平移和角度。
  如果 RF2O 超时或跳变过大，则临时退回 wheel_predict。
```

调试 topic：
```bash
ros2 topic echo /robot/odom_fusion_debug
```

典型输出含义：
```text
mode=fused source=fused pose=(x,y,yaw) laser_xy_weight=0.70 laser_yaw_weight=0.55
```

`source` 说明：
```text
laser         当前使用 RF2O
fused         当前使用轮速 + RF2O 融合
wheel_predict RF2O 暂时无数据或被判定跳变，临时使用轮速预测
waiting       还没收到可用 odom
```

#### 8. RF2O 说明

目录：
```text
lidar/chapt1_ws/src/rf2o_laser_odometry
```

RF2O 作用：
```text
根据连续 2D LaserScan 帧估计机器人在平面上的位移和转角。
适合轮式里程计不准、地面打滑、四轮差速转弯有滑移的场景。
```

本次 launch 中参数：
```text
laser_scan_topic: /scan
odom_topic: /odom_rf2o
base_frame_id: base_link
odom_frame_id: odom_rf2o
publish_tf: false
init_pose_from_topic: ''
freq: 10.0
```

为什么 `publish_tf=false`：
```text
RF2O 只负责输出 /odom_rf2o。
最终 TF 由 odom_fusion_node 统一发布，避免 TF 冲突。
```

本次还修正 RF2O 初始姿态：
```text
CLaserOdometry2DNode.cpp
orientation.w: 0 -> 1
```

原因：
```text
空初始姿态时四元数 w=0 不是合法单位姿态，可能导致第一帧姿态异常。
```

#### 9. 启动文件修改说明

##### 9.1 slam_launch.py

用途：
```text
纯建图测试。
```

新增：
```text
odom_mode:=wheel/laser/fused
```

推荐用它做第一轮 RF2O 测试。

##### 9.2 slam_nav_fusion_launch.py

用途：
```text
建图 + Nav2 + 视觉避障 + 下位机运动控制。
```

新增：
```text
odom_mode:=wheel/laser/fused
```

推荐在 `slam_launch.py` 验证后再用。

##### 9.3 nav2_launch.py

用途：
```text
已建图导航，map_server + AMCL + Nav2。
```

新增：
```text
odom_mode:=wheel/laser/fused
```

注意：
```text
已建图导航时 AMCL 会根据已有地图修正 map->odom。
fused odom 只负责给 AMCL/Nav2 一个更好的短期预测。
```

##### 9.4 full_system_launch.py

用途：
```text
总入口，支持只建图、建图+视觉、建图+视觉+Nav2。
```

新增：
```text
odom_mode:=wheel/laser/fused
```

如果 `enable_nav2:=true`，会把 `odom_mode` 继续传给 `nav2_launch.py`。

#### 10. Ubuntu 编译步骤

进入工作区：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
```

安装依赖：
```bash
rosdep install --from-paths src --ignore-src -r -y
```

编译：
```bash
colcon build --symlink-install
```

加载环境：
```bash
source install/setup.bash
```

如果编译 RF2O 报缺少 eigen/cmake_modules：
```bash
sudo apt update
sudo apt install ros-foxy-cmake-modules ros-foxy-eigen3-cmake-module libeigen3-dev
```

然后重新：
```bash
colcon build --symlink-install
source install/setup.bash
```

#### 11. 推荐测试顺序

##### 第一步：确认原版未被破坏

```bash
ros2 launch lidar_py slam_launch.py odom_mode:=wheel
```

检查：
```bash
ros2 topic echo /odom
ros2 run tf2_ros tf2_echo odom base_link
```

预期：
```text
1. /odom 有数据。
2. odom->base_link 有 TF。
3. 不启动 RF2O。
4. 行为应与原来的纯轮速建图基本一致。
```

##### 第二步：只测 RF2O

```bash
ros2 launch lidar_py slam_launch.py odom_mode:=laser
```

检查 topic：
```bash
ros2 topic echo /odom_rf2o
ros2 topic echo /odom
ros2 topic echo /wheel/odom
ros2 topic echo /robot/odom_fusion_debug
ros2 run tf2_ros tf2_echo odom base_link
```

预期：
```text
1. /odom_rf2o 有数据。
2. /odom 由 odom_fusion_node 输出。
3. /wheel/odom 仍有数据，但不作为最终 TF。
4. odom->base_link 只有 odom_fusion_node 发布。
```

现场判断：
```text
慢速直行：
  RViz 中车体应大致直行，不应明显反向或横移。

慢速原地转：
  RViz 中转向应与现实一致。

静止：
  /odom_rf2o 不应持续明显漂移。

走廊：
  如果走廊特征太少，RF2O 可能会跳动，这是算法退化，不一定是代码错。
```

##### 第三步：测试融合模式

```bash
ros2 launch lidar_py slam_launch.py odom_mode:=fused
```

观察：
```bash
ros2 topic echo /robot/odom_fusion_debug
```

理想输出：
```text
source=fused
```

如果经常出现：
```text
source=wheel_predict
```

说明 RF2O 数据超时、跳变、或没有稳定输出，需要检查 `/scan`、TF、RF2O 是否正常。

##### 第四步：建图 + 视觉避障 + Nav2

```bash
ros2 launch lidar_py slam_nav_fusion_launch.py odom_mode:=fused enable_vision:=true
```

如果想先不冒险，可先：
```bash
ros2 launch lidar_py slam_nav_fusion_launch.py odom_mode:=wheel enable_vision:=true
```

##### 第五步：已建图导航

```bash
ros2 launch lidar_py nav2_launch.py map:=~/maps/lab_map.yaml odom_mode:=fused
```

如果要用总入口：
```bash
ros2 launch lidar_py full_system_launch.py enable_nav2:=true map:=~/maps/lab_map.yaml odom_mode:=fused
```

#### 12. 关键排查命令

查看 topic：
```bash
ros2 topic list
```

查看 scan：
```bash
ros2 topic echo /scan
```

查看 RF2O：
```bash
ros2 topic echo /odom_rf2o
```

查看轮速 odom：
```bash
ros2 topic echo /wheel/odom
```

查看最终 odom：
```bash
ros2 topic echo /odom
```

查看 TF：
```bash
ros2 run tf2_ros tf2_echo odom base_link
ros2 run tf2_ros tf2_echo base_link laser_frame
ros2 run tf2_ros tf2_echo map base_link
```

查看融合状态：
```bash
ros2 topic echo /robot/odom_fusion_debug
```

查看谁在发布 `/odom`：
```bash
ros2 topic info /odom -v
```

如果 `/odom` 有两个 publisher，要立刻停下来检查，因为说明 TF/odom 发布链路冲突。

#### 13. 参数调试建议

`odom_fusion_node` 默认：
```text
laser_xy_weight: 0.70
laser_yaw_weight: 0.55
laser_timeout_sec: 0.40
max_laser_step_m: 0.40
max_laser_step_yaw_rad: 0.90
```

如果 RF2O 很稳，但建图仍然跟着轮速拉长：
```text
提高 laser_xy_weight，例如 0.70 -> 0.80
提高 laser_yaw_weight，例如 0.55 -> 0.65
```

如果 RF2O 偶尔跳，导致车体在 RViz 抽动：
```text
降低 laser_xy_weight，例如 0.70 -> 0.55
降低 laser_yaw_weight，例如 0.55 -> 0.40
降低 max_laser_step_m，例如 0.40 -> 0.25
降低 max_laser_step_yaw_rad，例如 0.90 -> 0.60
```

如果 RF2O 经常超时：
```text
检查 /scan 频率。
检查 base_link -> laser_frame TF 是否存在。
检查 RF2O 是否启动。
可以把 laser_timeout_sec 适当加大，例如 0.40 -> 0.60。
```

#### 14. 建图阶段和导航阶段的区别

建图阶段：
```text
没有固定地图。
RF2O/fused odom 只是改善前端位姿预测。
slam_toolbox 仍然要靠 scan matching 和 loop closure 纠正全局地图。
```

导航阶段：
```text
已有固定地图。
fused odom 给 AMCL 一个短期预测。
AMCL 用 /scan 匹配已有地图，长期修正 map->odom。
```

所以：
```text
RF2O/fused odom 解决的是建图前端里程不准。
slam_toolbox 解决的是建图后端图优化和回环。
AMCL 解决的是已建图导航时长期定位纠偏。
```

#### 15. 现场测试重点

建议每个模式都做同样动作：
```text
1. 静止 10 秒，看 /odom 是否漂。
2. 慢速直行 1 米，看 RViz 是否接近 1 米。
3. 慢速原地转 90 度，看方向是否一致。
4. 走一个小矩形，看回到原点附近时地图是否闭合。
5. 走廊进入房间再返回，看是否还会把走廊拉长。
```

判断优先级：
```text
如果 wheel 比 laser 稳：先继续用 wheel 或 fused 低 RF2O 权重。
如果 laser 明显减少走廊拉伸：优先测试 fused。
如果 fused 最稳：后续建图/导航默认可以切 fused。
```

#### 16. 当前保守结论

目前代码结构已经支持：
```text
1. 原版 wheel 回退。
2. RF2O 单独测试。
3. wheel + RF2O 融合测试。
4. slam_toolbox 后端继续使用。
5. Nav2/AMCL/视觉避障/串口控制继续接在最终 /odom 上。
```

但 RF2O 的 C++ 包需要在 Ubuntu ROS2 Foxy 环境中实际 `colcon build` 验证。Windows 这边已做 Python 语法检查，不能替代 Ubuntu 下的 C++ 编译和真车测试。

#### 17. 参考链接

RF2O：
```text
https://github.com/MAPIRlab/rf2o_laser_odometry
```

RF2O 论文：
```text
Planar Odometry from a Radial Laser Scanner. A Range Flow-based Approach. ICRA 2016
```

slam_toolbox：
```text
https://github.com/SteveMacenski/slam_toolbox
```

### 11.20 最新修改标签：UBUNTU_COLCON_BUILD_FIX_2026-07-10

本次目标：
- 在 Ubuntu 20.04 + ROS2 Foxy 上 `colcon build` 通过
- 修复 Windows 语法检查无法发现的 C++ 编译问题

#### 编译环境

```text
OS: Ubuntu 20.04
ROS2: Foxy (本机安装)
C++标准: C++14
```

#### 修复 1: package.xml 依赖清理

文件：`lidar/chapt1_ws/src/rf2o_laser_odometry/package.xml`

问题：
- `cmake_modules` 包在 Foxy apt 源中不存在
- CMakeLists.txt 实际用 `eigen3_cmake_module`，不需要 `cmake_modules`
- `nav_msgs` 在 CMakeLists.txt 中 `find_package` 了但 package.xml 没声明

修改：
```diff
-  <build_depend>cmake_modules</build_depend>
+  <build_depend>nav_msgs</build_depend>
-  <run_depend>cmake_modules</run_depend>
+  <run_depend>nav_msgs</run_depend>
```

#### 修复 2: 头文件扩展名

文件：`lidar/chapt1_ws/src/rf2o_laser_odometry/include/rf2o_laser_odometry/CLaserOdometry2DNode.hpp`

问题：
- ROS2 Foxy 中 `tf2_geometry_msgs` 头文件是 `.h`，不是 `.hpp`
- Galactic 及以后才改成 `.hpp`

修改：
```diff
-#include <tf2_geometry_msgs/tf2_geometry_msgs.hpp>
+#include <tf2_geometry_msgs/tf2_geometry_msgs.h>
```

#### 编译命令

```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
source /opt/ros/foxy/setup.bash
colcon build --symlink-install
```

#### 编译结果

```text
Starting >>> lidar_py
Starting >>> rf2o_laser_odometry
Finished <<< lidar_py [1.26s]
Finished <<< rf2o_laser_odometry [9.12s]
Summary: 2 packages finished [9.37s]
```

两个包均编译通过：
- `lidar_py` — Python 语法全通过（chassis_node, odom_fusion_node, safety_fusion_node, 4个launch）
- `rf2o_laser_odometry` — C++ 编译成功，二进制 `rf2o_laser_odometry_node` 已生成
### 11.21 最新修改标签：CARTOGRAPHER_2D_ALTERNATIVE_SLAM_2026-07-10

本次目标：
- 由于 RF2O 纯激光里程计和 RF2O 融合在真车转圈时效果不理想，新增 Cartographer 2D 作为另一套建图后端进行对比测试。
- 不替换现有 slam_toolbox 主线。
- 不修改 `open_all.sh` 和 `open_all_log.sh`。
- 新增独立启动文件，方便现场单独测试 Cartographer 2D。

#### 1. 新增文件

```text
lidar/chapt1_ws/src/lidar_py/config/cartographer_2d.lua
lidar/chapt1_ws/src/lidar_py/launch/cartographer_2d_launch.py
```

#### 2. 修改文件

```text
lidar/chapt1_ws/src/lidar_py/setup.py
lidar/chapt1_ws/src/lidar_py/package.xml
修改.md
```

#### 3. 新增依赖

`package.xml` 新增：
```xml
<depend>cartographer_ros</depend>
```

Ubuntu Foxy 上如果没有安装 Cartographer：
```bash
sudo apt update
sudo apt install ros-foxy-cartographer ros-foxy-cartographer-ros
```

#### 4. Cartographer 2D 数据流

```text
lidar_node
  └── /scan

chassis_node
  ├── /odom
  └── odom -> base_link

lidar_node
  └── base_link -> laser_frame

cartographer_node
  ├── 输入: /scan + /odom + TF
  └── 输出: map -> odom + submaps

cartographer_occupancy_grid_node
  └── 输出: /map
```

注意：
```text
Cartographer 这里使用 chassis_node 的 /odom 作为外部里程计先验。
Cartographer 负责发布 map->odom。
chassis_node 继续发布 odom->base_link。
最终 map->base_link = map->odom + odom->base_link。
```

#### 5. cartographer_2d.lua 关键配置

文件：
```text
lidar/chapt1_ws/src/lidar_py/config/cartographer_2d.lua
```

关键项：
```lua
map_frame = "map"
tracking_frame = "base_link"
published_frame = "odom"
odom_frame = "odom"
provide_odom_frame = false
publish_frame_projected_to_2d = true
use_pose_extrapolator = true
use_odometry = true
num_laser_scans = 1
num_subdivisions_per_laser_scan = 4
```

含义：
```text
tracking_frame=base_link：
  Cartographer 跟踪车体坐标系。

published_frame=odom + provide_odom_frame=false：
  使用外部底盘 odom，不让 Cartographer 再发布 odom->base_link。
  Cartographer 主要发布 map->odom。

use_odometry=true：
  订阅 /odom，把底盘里程计作为短期运动先验。

num_subdivisions_per_laser_scan=4：
  将一帧 LaserScan 分段处理，尝试减轻车体运动造成的扫描畸变。
```

启用：
```lua
TRAJECTORY_BUILDER_2D.use_online_correlative_scan_matching = true
```

目的：
```text
增强实时 scan matching，对走廊、转弯、回环前的局部匹配更积极。
```

#### 6. 启动方式

进入工作区：
```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
```

安装依赖：
```bash
sudo apt update
sudo apt install ros-foxy-cartographer ros-foxy-cartographer-ros
rosdep install --from-paths src --ignore-src -r -y
```

编译：
```bash
colcon build --symlink-install --packages-select lidar_py
source install/setup.bash
```

启动 Cartographer 2D：
```bash
ros2 launch lidar_py cartographer_2d_launch.py \
  lidar_serial_port:=/dev/ttyUSB1 \
  lidar_baudrate:=115200 \
  chassis_serial_port:=/dev/ttyUSB0 \
  use_rviz:=true
```

如果你的串口和自动识别不同，手动替换：
```text
lidar_serial_port:=你的雷达串口
chassis_serial_port:=你的 STM32 串口
```

#### 7. 测试顺序

1. 静止 10 秒：
```bash
ros2 topic echo /scan
ros2 topic echo /odom
ros2 run tf2_ros tf2_echo odom base_link
ros2 run tf2_ros tf2_echo map odom
```

检查：
```text
/scan 有数据。
/odom 有数据。
odom->base_link 存在。
map->odom 存在。
RViz 中 /map 开始出现。
```

2. 慢速直行 1 米：
```text
观察地图是否被拉长。
```

3. 慢速转 90 度：
```text
观察地图角度是否明显反向或扭曲。
```

4. 小矩形路线：
```text
观察回到起点附近时地图是否能闭合。
```

5. 两房间 + 走廊：
```text
重点看第二个房间是否还覆盖第一个房间，走廊是否继续延伸。
```

#### 8. 保存地图

Cartographer 输出 `/map` 后，可以用 Nav2 map_server 保存：
```bash
ros2 run nav2_map_server map_saver_cli -f ~/maps/cartographer_test_map
```

生成：
```text
~/maps/cartographer_test_map.yaml
~/maps/cartographer_test_map.pgm
```

#### 9. 和 slam_toolbox 的区别

```text
slam_toolbox：
  继续作为当前主线。
  ROS2 集成成熟，已经和现有网页/Nav2/视觉避障接好。

Cartographer 2D：
  新增对比后端。
  scan-to-submap + pose graph 思路不同，可能对长走廊和回环更好。
  参数更多，现场需要调。
```

#### 10. 当前建议

先不要把 `open_all.sh` 切到 Cartographer。

推荐先单独跑：
```bash
ros2 launch lidar_py cartographer_2d_launch.py use_rviz:=true
```

如果 Cartographer 2D 在“两房间 + 走廊”测试中明显优于 slam_toolbox，再考虑：
```text
1. 增加 Cartographer 一键启动模式。
2. 接入网页地图显示。
3. 接入 Nav2 导航链路。
4. 调整保存地图和加载地图流程。
```

#### 11. 参考

Cartographer ROS 官方 Lua 配置说明：
```text
https://google-cartographer-ros.readthedocs.io/en/latest/configuration.html
```

官方说明要点：
```text
1. Lua 配置必须指定 map_frame、tracking_frame、published_frame、odom_frame、use_odometry、num_laser_scans 等顶层选项。
2. topic 名在 Cartographer ROS 中使用基础名，例如 scan 和 odom，实际 remap 由 launch 负责。
3. 如果使用外部 odom，可以令 published_frame=odom，让 Cartographer 发布 map 中 odom 的位姿。
```

### 11.22 最新修改标签：CARTOGRAPHER_TURN_DESKEW_INPUT_FIX_2026-07-10

#### 1. 现场问题

Cartographer 已接入 `open_all.sh` 和 `open_all_log.sh` 后，真车直行尚可，但一转弯就会出现：

```text
地图整体偏转；
已经画过的墙发生旋转错位；
Cartographer 在错误方向重新建一层地图。
```

RF2O、RF2O 融合和 Cartographer 都在转弯时明显变差，说明问题不只在 SLAM 后端参数，更可能在三套算法共同使用的 `/scan` 与 `/odom` 输入。

#### 2. 查到的主要原因

原 `lidar_node.py` 存在以下转弯敏感问题：

```text
1. 检测到 360 度分圈后才发布，导致下一圈开头的一个 LD14P 数据包先被放进上一圈。
2. LaserScan.header.stamp 使用整圈接收完成时刻，不是第一束激光采集时刻。
3. LaserScan.time_increment 固定为 0，SLAM 无法知道一圈中各点的先后时间。
4. LaserScan.scan_time 固定为 0.1 秒，但现场雷达约 4.7Hz，一圈通常约 0.21 秒。
5. Cartographer 的 num_subdivisions_per_laser_scan 被设为 1，旋转时不能分段去畸变。
6. Cartographer 的 odom->base_link 可能带 IMU roll/pitch，不适合纯 2D 平面 SLAM。
7. 18 度实时角度搜索窗口过大，在走廊或相似墙面中容易跳到错误角度。
```

#### 3. 代码修改

修改文件：

```text
lidar/chapt1_ws/src/lidar_py/lidar_py/lidar_node.py
lidar/chapt1_ws/src/lidar_py/launch/cartographer_2d_launch.py
lidar/chapt1_ws/src/lidar_py/config/cartographer_2d.lua
```

`lidar_node.py`：

```text
在加入当前 LD14P 包之前检测分圈，杜绝跨圈混点；
启动后的第一段不完整扫描直接丢弃；
根据相邻两次分圈的真实时间计算 scan_time；
保留原 /scan，继续供 RViz、网页和 slam_toolbox 使用；
新增 /scan_timed，按真实采集顺序排列点；
/scan_timed 填写第一束激光时间、每点 time_increment 和真实 scan_time。
```

`cartographer_2d_launch.py`：

```text
Cartographer 的 scan 输入改为 /scan_timed；
chassis_node 在 Cartographer 模式下设置 use_imu_rp=false；
odom->base_link 只保留平面 x、y、yaw，避免车身颠簸倾斜整片 2D 激光。
```

`cartographer_2d.lua`：

```text
num_subdivisions_per_laser_scan = 4
num_accumulated_range_data = 4
use_pose_extrapolator = true
角度搜索窗口从 18 度收紧到 8 度
平移搜索窗口从 0.18m 收紧到 0.12m
提高错误位姿偏移的代价，降低相似走廊中的跳变概率
回环 min_score 从 0.55 提高到 0.65；
全局定位分数从 0.60 提高到 0.70；
约束采样率从 0.30 降到 0.15；
Huber 鲁棒核从 100 收紧到 10，降低错误约束对整图的拖拽。
```

注意：本次没有修改 `open_all.sh` 和 `open_all_log.sh`。

#### 4. Ubuntu 编译与启动

```bash
cd ~/CCCCAAAARRRR/lidar/chapt1_ws
source /opt/ros/foxy/setup.bash
colcon build --packages-select lidar_py
source install/setup.bash
```

可以继续使用已经接入 Cartographer 的一键脚本，选择 `4) cartographer`：

```bash
cd ~/CCCCAAAARRRR
./open_all_log.sh
```

也可以单独启动：

```bash
ros2 launch lidar_py cartographer_2d_launch.py \
  lidar_serial_port:=/dev/ttyUSB1 \
  lidar_baudrate:=115200 \
  chassis_serial_port:=/dev/ttyUSB0 \
  use_rviz:=true
```

#### 5. 启动后必须检查

```bash
ros2 topic hz /scan
ros2 topic hz /scan_timed
ros2 topic echo /scan_timed --once
```

正常情况下：

```text
/scan 和 /scan_timed 都约为雷达真实转速，通常 4~6Hz；
/scan_timed.time_increment 必须大于 0；
/scan_timed.scan_time 通常约 0.16~0.25 秒，不能继续固定为 0.1；
默认 scan_angle_sign=-1 时，/scan_timed.angle_increment 应小于 0；
终端 LiDAR 日志会打印 speed=...Hz 和 scan_time=...s。
```

#### 6. 推荐真车测试顺序

```text
1. 静止 20 秒，确认墙线不自行旋转或双层扩散。
2. 以约 15~25 度/秒缓慢原地旋转 90 度，观察旧墙是否保持固定。
3. 缓慢原地旋转一整圈，检查 odom 的 x/y 是否产生明显假位移。
4. 走一个 2m × 2m 小矩形，回到起点检查闭合。
5. 最后再跑两个房间和走廊，不要一开始就用高速原地掉头测试。
```

如果第 2 步仍然整体偏转，下一步重点不再继续乱调 Cartographer，而是记录并对比：

```text
/scan_timed 的真实 scan_time；
/odom 在原地 360 度后的 x/y 假位移；
/odom 的 yaw 与现场实际旋转角；
base_link->laser_frame 的实测 x、y、yaw 外参。
```

#### 7. 本次现场日志证据

读取 `SLAM_Log/2026-07-10_18-44-37` 后得到：

```text
高角速度转弯样本：46 个；
转弯角速度：约 14.2~15.6 deg/s；
转弯期间 abs(vx) 平均值：约 0.0002 m/s；
转弯期间 abs(vx) 最大值：0.008 m/s。
```

说明这次原地转弯时，下位机没有上报足以造成大范围地图平移的虚假 `vx`。

但地图位姿出现了明确跳变：

```text
18:45:32.8  x=3.379  y=-0.102  yaw=166.19 deg
18:45:33.3  x=3.266  y=+0.536  yaw= 73.06 deg
```

0.5 秒内地图解算跳了约 93 度和 0.65 米，而同一时段陀螺仪角速度连续，没有对应突变。因此本次优先修复扫描时序/去畸变并提高错误回环门槛，而不是继续修改 `vx` 比例。

#### 8. 回退方式

本次保留了原 `/scan`，slam_toolbox 主线没有改为读取 `/scan_timed`。如果 Cartographer 仍不合适，在一键脚本选择 `wheel` 即可回到原 slam_toolbox + 底盘 odom 模式，不需要删除本次代码。

### 11.23 最新修改标签：SLAM_TOOLBOX_MAPPING_WEB_PROTOCOL_V2_2026-07-11

#### 1. 本次目标与边界

本次以纯 `slam_toolbox` 主线为基础，同步完成实车几何和编码器单位统一、同步 SLAM 参数、激光散斑滤波、STM32 `0x08` 建图模式、普通/建图速度曲线、Nav2 第二档速度约束、网页重构以及 Nav2/网页遥控/视觉避障的统一仲裁。

明确未修改已经实测通过的：

```text
open_all.sh
open_all_log.sh
```

#### 2. 实车尺寸与差速运动学

读取 `车尺寸.txt` 后统一为：

```text
轮胎直径/半径              0.151 / 0.0755 m
轮胎外侧/内侧总宽          0.620 / 0.510 m
左右轮中心距/半轮距        0.565 / 0.2825 m
前后轮中心距/半轴距        0.429 / 0.2145 m
亚克力车体长宽             0.665 m
Nav2 footprint             x/y = ±0.333 m
```

四轮差速的 `v/w -> 左右轮速` 只使用左右轮中心距 `0.565 m`。旧代码把半轴距与半轮距相加得到约 `1.00 m` 等效轮距，会直接造成旋转里程比例错误，本次已纠正。旧 35 字节编码器兼容分支的 yaw 符号也统一为 ROS 约定：右轮位移大于左轮时为逆时针正角速度；默认 `0x07 NAVI` 主线不受该兼容修正影响。

#### 3. 编码器 PPR 的 8 倍单位错误

最新 STM32 工程的权威定义是：

```text
编码器单圈：8,388,608 count/rev
减速比：25:1
```

部分上位机文件此前使用 `1,048,576`，恰好小 8 倍。本次将 `chassis_node.py`、`safety_fusion_node.py`、`encoder_parser.py`、独立串口工具和网页档位统一为 `8,388,608`，STM32 轮径统一为 `0.151 m`。

换算基准：

```text
count_per_meter ≈ 442,082,261 count/m
0.20 m/s -> 88,416,452 count/s
0.10/0.15 m/s -> 44,208,226 / 66,312,339 count/s
0.08/0.16 m/s -> 35,366,581 / 70,733,162 count/s
9/12 deg/s 原地旋转 -> 19,617,399 / 26,156,532 count/s
```

现场仍要确认下位机上下行 count 是否已经包含 `25:1`。若实测固定差 25 倍或其他整数倍，应修正单位定义，不能用网页倍率掩盖。

#### 4. 建图与 Nav2 速度

读取 `速度.txt` 后，STM32 建图曲线为：

```text
直行/后退：0.20 m/s
档 1：圆弧内/外轮 0.10/0.15 m/s，原地旋转 9 deg/s
档 2：圆弧内/外轮 0.08/0.16 m/s，原地旋转 12 deg/s
```

建图模式摇杆圆弧保持两侧向前；只在明确原地旋转时使用左右反向轮速。Nav2 和安全融合节点限制为直行 `0.20 m/s`、曲线外轮 `0.16 m/s`、原地旋转 `0.209 rad/s`，即使用建图第二档物理包络。

#### 5. AA55 0x08 建图模式

新增 20 字节命令：

```text
开启：AA 55 08 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 08
关闭：AA 55 08 02 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 09
```

定义为 `spd[0]=1/2` 开启/关闭，`spd[1..3]` 必须为零。模式真正变化时 STM32 先置四轮零速；若处于 UART MOVE 则保留串口控制权并允许下一帧立即恢复，归还 PS2 后从档位 1 开始。它不改变 MOVE/PS2 控制权，CANopen 未就绪时也允许记录模式。接收校验拒绝非法 mode 或非零保留字段。

涉及文件：

```text
STM32/Core/Inc/main.h
STM32/Core/usart/bsp_debug_usart.h
STM32/Core/Src/freertos.c
STM32/上下位机通信标准.txt
road_v5_5_640_modular_v7_unified_io/config_switches.py
road_v5_5_640_modular_v7_unified_io/serial_comm.py
```

#### 6. 唯一状态源与安全初始化

`chassis_node` 现在是唯一权威状态源，通过 `/robot/control_state` 同步 MOVE/PS2、回响、建图模式、baseline、档位、串口运动、障碍红点、避障框、RGB 调试信息和 SLAM 日志状态。

浏览器连接或刷新只订阅状态，不发送默认帧。`chassis_node` 每次真正启动时只执行一次：

```text
0x06 ECHO_OFF -> 0x08 MAPPING_ON -> 0x01 四轮零速 MOVE
```

这样满足默认 MOVE 接管、回响 OFF、建图 ON，同时避免第二台设备重复初始化或抢控制权。

同一个串口读缓冲解析：

```text
AA55 0x01~0x06/0x08：控制帧和回响
AA55 0x07：NAVI yaw/vx/vz
AA56：旧 IMU 兼容帧
```

按当前下位机协议，ECHO_ON 会暂停 NAVI，ECHO_OFF 确认后恢复 20 Hz NAVI，两者不是并行上报。

#### 7. 纯 slam_toolbox 与激光滤波

主链路使用：

```text
sync_slam_toolbox_node
mapper_params_online_sync.yaml
```

同步/异步配置统一收紧：地图更新 `0.5 s`、量程 `8 m`、最小时间 `0.05 s`、TF 超时 `0.2 s`、最小移动 `0.04 m`、最小转角 `0.04 rad`、扫描缓冲 30、回环搜索 `2.5 m`、最小链 12，并修复同步配置误写的 `odom_frame: map` 为 `odom`。

新增 `config/laser_filter.yaml`，数据链路为：

```text
LD14P -> /scan_raw -> laser_filters -> /scan -> slam_toolbox/Nav2/RViz
```

滤波组合：

```text
LaserScanRangeFilter：保留 0.10~8.00 m
LaserScanSpeckleFilter：type=1, max_range_difference=0.18, window=2
```

选择 ROS 官方 `laser_filters` 的散斑插件，只清除孤立坏点，不对墙角做均值平滑。Foxy 安装：

```bash
sudo apt install ros-foxy-laser-filters
```

参考：

```text
https://docs.ros.org/en/ros2_packages/rolling/api/laser_filters/generated/classlaser__filters_1_1LaserScanSpeckleFilter.html
https://docs.ros.org/en/rolling/p/laser_filters/__CHANGELOG.html
https://index.ros.org/r/laser_filters/
```

#### 8. 网页、Nav2 与视觉避障仲裁

速度输入分开：

```text
/cmd_vel_nav：Nav2
/cmd_vel_web：网页八方向遥控
```

网页命令最近 `0.35 s` 内有效时优先，松开后自动回 Nav2；视觉避障始终作用于当前有效命令，最终只由 `/cmd_vel_safe` 进入 `chassis_node` 和 STM32。动态避障继续读取 config 距离，根据障碍距离、面积和左右分布调转向；非危险绕障保留最小前进轮速，危险距离才停车或原地脱困。

#### 9. 网页重构内容

```text
1. MOVE/PS2 合并为一个切换按钮；
2. ECHO_ON/OFF 合并为一个切换按钮；
3. 新增建图模式按钮，建图两档、普通四档；
4. 删除独立零速 MOVE，八方向中心停止发送四轮零速 MOVE；
5. 四个对角按钮执行差速斜向；按住发送、松开停止；
6. RGB 和地图增大，均支持全屏、底部半透明控制条和 Esc 退出；
7. 深浅主题使用圆形扩散动画；
8. 保留按页面主机自动填写 Video/ROSBridge 地址；
9. 地图单击立即预览路径，确认才导航，清除同时删目标和路径；
10. TX/Echo/NAVI/Events 改为标签页，校验红绿状态保留；
11. 全部状态从 /robot/control_state 同步；
12. baseline 未完成时继续锁定网页运动、视觉运动和 PS2 归还。
```

图标使用 `lucide`。桌面和 390 px 窄屏均使用稳定网格，避免按钮重叠和横向溢出。

#### 10. 关键文件结构

```text
CCCCAAAARRRR/
├─ 修改.md
├─ CAR_使用教程_注意事项.md
├─ STM32/
│  ├─ Core/Inc/main.h
│  ├─ Core/Src/freertos.c
│  └─ 上下位机通信标准.txt
├─ lidar/chapt1_ws/src/lidar_py/
│  ├─ config/laser_filter.yaml
│  ├─ config/mapper_params_online_*.yaml
│  ├─ config/nav2_params.yaml
│  ├─ launch/slam_nav_fusion_launch.py
│  └─ lidar_py/{lidar_node,chassis_node,safety_fusion_node}.py
├─ road_v5_5_640_modular_v7_unified_io/
└─ web/
   ├─ index.html
   └─ src/{main.js,style.css,config/gearConfig.js,ros/*.js}
```

#### 11. 编译、启动与验证

```bash
sudo apt update
sudo apt install ros-foxy-laser-filters

cd ~/CCCCAAAARRRR/lidar/chapt1_ws
source /opt/ros/foxy/setup.bash
colcon build --packages-select lidar_py
source install/setup.bash

cd ~/CCCCAAAARRRR/web
npm install
npm run build

cd ~/CCCCAAAARRRR
./open_all.sh
# 或 ./open_all_log.sh
```

车轮架空后依次确认初始化三帧、`/scan_raw -> /scan`、NAVI 20 Hz、四轮符号、两档 count/s、第二台设备不重复初始化、串口运动关闭后路径仍刷新。落地后依次测试 1 m 直行、90 度慢转、小矩形闭环、网页前进动态绕障、预览路径和确认导航，最后再跑两房间走廊。

本次 Windows 侧完成网页生产构建、桌面/窄屏布局、两种全屏、Esc、主题、按钮碰撞、浏览器 console、Python 语法及 AA55 checksum 检查。Windows 没有 ROS2 Foxy、ARM/Keil 编译链和真车硬件，`colcon build`、STM32 编译与实车串口/CANopen 验证仍需在 Ubuntu 真车完成。

### 11.24 最新修改标签：TURNING_MAP_ROTATION_SAFE_ROLLBACK_2026-07-11

现场反馈纯 slam_toolbox 和 Cartographer 在同一房间原地转圈时都会旋转旧房间并重画。两种后端同时失败说明首要问题在共同的 `/scan`、`/odom` 或 `base_link->laser_frame`，不是继续提高回环门槛可以解决。

本次恢复稳定默认链路：

```text
scan_angle_sign 恢复为大改前已经实测使用的 -1.0，本次不再改动角度方向；
async_slam_toolbox_node；
mapper_params_online_async.yaml 原稳定参数；
enable_laser_filter=false，默认 LD14P 直接发布 /scan。
```

原来的同步 SLAM、严格参数和激光滤波没有删除，改为 launch 可选项：

```bash
enable_laser_filter:=true
slam_executable:=sync_slam_toolbox_node
mapper_params_file:=<sync yaml>
scan_angle_sign:=1.0    # 仅用于现场正负号对照，默认仍为原来的 -1.0
```

涉及：`lidar_node.py`、`slam_nav_fusion_launch.py`、`slam_launch.py`、`full_system_launch.py`、`nav2_launch.py`、`cartographer_2d_launch.py`。`open_all.sh` 和 `open_all_log.sh` 仍未修改。

本次回归的首要处理是撤销 11.23 强制启用的同步 SLAM、严格匹配参数和默认激光滤波。角度方向保持大改前配置，不再把两个问题混在一起。转圈排查仍可在 RViz 将 Fixed Frame 设为 `odom`，关闭 Map，只显示 LaserScan、TF、Odometry，用来区分 SLAM 参数问题和输入数据问题。

### 11.25 最新修改标签：ASYNC_SLAM_WITH_LASER_FILTER_2026-07-11

按现场要求重新启用激光滤波，但不恢复有问题的同步 SLAM 和严格参数。纯 SLAM 固定链路为：

```text
LD14P /scan_raw -> LaserScanRangeFilter + LaserScanSpeckleFilter -> /scan
-> async_slam_toolbox_node + mapper_params_online_async.yaml
```

因此这次 A/B 组合只相对大改前增加激光噪点过滤，SLAM 节点类型和参数保持原稳定版。

### 11.26 最新修改标签：CARTOGRAPHER_TURN_STABLE_FRONTEND_2026-07-11

Cartographer 原配置在 LD14P 原地转圈时使用 `num_accumulated_range_data=4`。一圈约 0.2 秒时，会在一次匹配中累计约 0.8 秒的多朝向数据，是转圈重影和房间偏转的高风险组合。

新增 `config/cartographer_2d_turn_stable.lua` 并设为 Cartographer 默认，旧 `cartographer_2d.lua` 原样保留。新配置：

```text
num_subdivisions_per_laser_scan = 4
num_accumulated_range_data = 1
use_online_correlative_scan_matching = false
Ceres translation/rotation weight = 12/80
motion filter = 0.35s / 0.05m / 0.5deg
submap num_range_data = 60
pose graph optimize every 90 nodes
constraint distance = 6m
constraint min/global score = 0.68/0.72
```

四段 subdivision 继续利用 `/scan_timed` 的首束时间和 `time_increment` 去除转动畸变；每段立即进入前端，不再把四个完整旋转周期压成一次匹配。关闭在线相关匹配后，前端以 20Hz `/odom` 短时 yaw 预测为初值，由 Ceres 在附近细化，避免方形房间相似墙面导致角度搜索跳解。

默认启动：

```bash
ros2 launch lidar_py cartographer_2d_launch.py use_rviz:=true
```

回退旧配置：

```bash
ros2 launch lidar_py cartographer_2d_launch.py \
  cartographer_config:=cartographer_2d.lua use_rviz:=true
```

本次只修改 Cartographer 专用配置、安装清单及其 launch 默认配置，不修改纯 slam_toolbox、`open_all.sh` 或 `open_all_log.sh`。

### 11.27 最新修改标签：TRUE_ORIGINAL_ASYNC_SLAM_BASELINE_2026-07-11

用户提供了两个明确目录后，改为以 `CCCCAAAARRRR` 原版文件夹为唯一基线，不再使用 Git 暂存区推断“大改前版本”。已将优化版的：

```text
lidar/chapt1_ws/src/lidar_py/config/mapper_params_online_async.yaml
```

逐项恢复为原版参数，包括 0.10/0.08 关键帧阈值、30 帧 buffer、8m 回环距离、0.9m 相关搜索空间、12m 回环搜索空间，以及 0.35/0.55 距离和角度惩罚。

最终 SHA-256 对比：

```text
原版    DA59F37A2A595FB2909842DBA6C6F9E1645DEBF6227AE0E00E8F9939D9DE95FF
优化版  DA59F37A2A595FB2909842DBA6C6F9E1645DEBF6227AE0E00E8F9939D9DE95FF
IDENTICAL=True
```

因此优化版纯 slam_toolbox 现在使用真正原版 async 参数，额外只保留 `/scan_raw -> laser_filters -> /scan` 滤波链。Cartographer 仍独立使用 `cartographer_2d_turn_stable.lua`，不会影响 slam_toolbox YAML。

### 11.28 最新修改标签：CARTOGRAPHER_DIRECT_PLANAR_IMU_FIX_2026-07-11

基于用户的新 Cartographer 参数继续核查后修复两处实际问题：

```text
1. Lua 后部重复设置 real_time_correlative_scan_matcher，导致前面的 15deg
   搜索窗口实际被 5deg 覆盖；重复块已删除。
2. 原 /imu 的 NAVI 分支只有真实 gyro z，linear_acceleration 全零，不能直接
   满足 Cartographer IMU tracker 对重力观测的要求。
```

`chassis_node.py` 新增仅供 Cartographer 使用的 `/imu_cartographer`：

```text
angular_velocity.z       使用 0x07 NAVI 的真实 Vz，并应用 navi_vz_sign
linear_acceleration.z    9.80665 m/s^2，表示水平二维底盘的稳定重力观测
frame_id                 base_link
发布频率                 跟随 0x07 NAVI，约 20Hz
```

Cartographer launch 现在把 `imu` remap 到 `/imu_cartographer`，同时继续使用 `/odom` 和 `/scan_timed`。15deg 相关搜索窗口已真正生效，旋转偏移代价设为 2.0，允许搜索较大的 yaw 误差但降低相似墙面跳解风险。

用户设置的 motion filter `0.5s/0.08m/1.0deg` 会保留更少而不是更多节点，代码注释已修正；本次保留这组数值用于减少近似重复约束。

### 11.29 最新修改标签：CARTOGRAPHER_TURN_AND_MCU_TIME_SYNC_2026-07-11

本次只修改 `CCCCAAAARRRR优化版`，未修改 `open_all.sh`、`open_all_log.sh`，也未改动已经恢复的纯 `slam_toolbox` 参数。

#### Cartographer 转弯配置

- 雷达一圈拆成4段，因此将 `num_accumulated_range_data` 从2恢复为4，确保每次局部匹配使用完整360度扫描。
- 相关搜索角度从15度收敛到8度，线性范围从0.15m收敛到0.12m，降低相似墙面的错误匹配概率。
- 转弯节点阈值改为0.5度，`submaps.num_range_data` 恢复为60，雷达有效范围恢复为0.08m到12m。
- 继续保留 `use_imu_data=true` 和真实Wz输入。

#### 0x07 NAVI MCU时间戳

20字节帧长度、帧头和校验规则不变：

```text
[0..1]   AA 55
[2]      07
[3..6]   yaw * 100，int32 LE
[7..10]  Vx * 1000，int32 LE
[11..14] Wz * 100，int32 LE
[15..18] HAL_GetTick()，uint32 LE，单位ms
[19]     sum(byte[0..18]) & 0xFF
```

STM32继续以50Hz（20ms）发送。50Hz已经高于雷达扫描频率，足以提供角速度和里程计预测；没有改成100Hz，以免增加115200串口占用、ROS解析负载和线程调度抖动。

上位机按无符号32位解析时间戳，支持约49.7天回绕。`/imu_cartographer`、`/odom` 和 `odom -> base_link` TF使用同一测量时间，并避免重复发布同一采样。旧固件字段为0时自动回退电脑接收时间。串口调试增加 `tick=...ms`、`mcu_tick_ms` 和 `time_source`。

Ubuntu重新编译：

```bash
cd ~/CCCCAAAARRRR优化版/lidar/chapt1_ws
colcon build --packages-select lidar_py --symlink-install
source install/setup.bash
```

STM32必须重新编译并烧录，否则上位机将显示 `host_fallback`。

### 11.30 最新修改标签：CARTOGRAPHER_LD14P_ACQUISITION_TIME_FIX_2026-07-11

目标目录为 `新建文件夹 (2)/CCCCAAAARRRR优化版`。本次没有修改 `open_all.sh` 和 `open_all_log.sh`。

#### 启动报错复核

Foxy版Cartographer报错：

```text
Key 'use_pose_extrapolator' was used the wrong number of times.
```

当前两个Lua配置中已经删除该不兼容字段，保留此修复。`1.txt`中提到的“motion filter导致每秒5个节点、后端来不及优化”不是Cartographer退出的根因；真正的启动退出原因是Lua字段与本机Foxy版本不兼容。

#### 转弯飘移根因修复

原 `/scan_timed` 存在两个数据层问题：

1. 使用 `scan_time/(N-1)` 作为逐点间隔，导致上一圈最后一点时间恰好等于下一圈第一点时间。Cartographer要求传感器时间严格递增，因此出现 `previous subdivision time is not before current time` 并丢弃数据。
2. 原始点先进入按角度索引的字典，再用固定0.7度网格重新构造采集顺序。重复角度会被合并，空角度会被人工插入，逐点采集顺序和时间对应关系被破坏。

现已修改为：

- 为Cartographer单独保存LD14P原始物理采集顺序，保留每一束光线，包含无效回波占位。
- `time_increment=scan_time/N`，一圈时间范围为 `[本圈开始, 下一圈开始)`，相邻扫描保持严格递增。
- 启用LD14P数据包自带的16位毫秒时间戳，支持回绕展开，并以固定偏移映射到ROS时钟；运行中不跟随USB调度延迟调整时间轴。
- `/scan_timed` 使用实际每圈点数，不再从普通 `/scan` 的固定角度字典反推采集顺序。
- 普通 `/scan` 保持原逻辑，避免影响已验证的slam_toolbox、RViz、网页和Nav2链路。
- Cartographer继续使用完整360度单圈匹配：`num_subdivisions_per_laser_scan=1`、`num_accumulated_range_data=1`。
- `rotation_delta_cost_weight` 从0.5提高到2.0，降低对称房间内转弯时跳到相似墙面的概率。
- Cartographer最大量程改为8m，与驱动实际过滤范围一致。

#### 编译和检查

```bash
cd ~/CCCCAAAARRRR优化版/lidar/chapt1_ws
rm -rf build/lidar_py install/lidar_py
colcon build --packages-select lidar_py --symlink-install
source install/setup.bash
```

测试Cartographer时重点确认终端不再出现：

```text
Ignored subdivision
previous subdivision time is not before current time
```

雷达周期日志应显示 `clock=device`、稳定的 `scan_time` 和约500个左右的 `timed_points`。如果仍发生飞图，必须同时保存Cartographer终端日志以及静止、原地转90度、原地转360度三个阶段的rosbag，继续判断Wz符号/尺度和激光外参，不能再只凭地图现象调整Lua参数。

### 11.31 最新修改标签：REMOVE_OLED_RESTORE_NAVI_50HZ_2026-07-12

实机记录证明0x07 NAVI第100帧到第200帧的STM32时间戳相差28351ms，真实上报频率只有3.53Hz。源码虽然设置20ms发送门限，但NAVI上报与OLED软件I2C整屏刷新位于同一个低优先级 `EncoderReportTask`，OLED同步写入会阻塞整个任务循环。

用户确认车辆不再使用OLED，因此已从运行路径移除：

- `main.c`中的 `oled.h`、`soft_i2c.h`引用；
- `display_id`显示页状态；
- 开机软件I2C扫描、OLED探测、初始化、清屏和欢迎文字；
- `freertos.c`中5Hz OLED整屏刷新；
- PC13显示页切换和全部OLED字符串格式化。

OLED驱动源码仍保留在工程目录中，但运行时没有任何调用。NAVI任务现在每5ms调度一次，并继续按20ms门限发送20字节0x07帧，目标真实频率为50Hz。本次没有改成100Hz，也没有改变AA55协议、字段、校验或回响逻辑。

烧录新固件后必须用MCU时间戳验证真实频率，不能只看上位机重复发布的 `/odom`：

```text
连续0x07帧 tick_ms差值：约20ms
100帧MCU时间差：约2000ms
真实NAVI频率：约50Hz
```

若仍接近284ms，说明运行的不是新固件，或任务还有其他阻塞；此时不要继续调整Cartographer参数。

### 11.32 最新修改标签：MEASURED_LIDAR_X_EXTRINSIC_2026-07-12

用户实测顶部亚克力板中心（对应底盘几何中心）到雷达旋转中心的车头方向距离为0.20m。为避免不同launch使用不同外参，将统一默认值设置在 `lidar_node.py`：

```text
laser_x=+0.20m
laser_y=0.0m
laser_z=0.0m
laser_yaw_deg=0.0deg
```

该设置生成静态TF `base_link -> laser_frame`，所有未显式覆盖位置参数的Cartographer、slam_toolbox、Nav2及雷达显示启动方式都会自动使用同一外参。若以后确认雷达位于底盘中心后方，应将 `laser_x` 改为 `-0.20`；本次按位于车头方向处理。没有修改任何启动脚本。

### 11.33 最新修改标签：LD14P_MEASURED_ANGLE_FIXED_SCAN_V2_2026-07-13

本次只修改 `测试1/CCCCAAAARRRR稳定版` 测试副本，没有修改 `open_all.sh`、`open_all_log.sh` 或外部的稳定版目录。

根据 2026-07-13 rosbag 离线解析，车辆静止且 `odom -> base_link` 不动时，原 `/scan_timed` 仍会在相邻帧之间出现约 3deg 假旋转。当每圈点数从 396 变为 384 时，假旋转约 +2.44deg；384 变为 396 时约 -2.33deg，两者相关系数约 -0.965。根因是旧 `/scan_timed` 将串口采集顺序直接按 `360deg / 本圈点数` 重新赋角，没有使用每个点的实测角度进行网格定位。

新增内容：

- `lidar_py/fixed_scan_grid.py`：逐点识别 359deg -> 0deg 跨圈，按实测原始角度装入固定 360 格，异常点数或异常扫描周期整圈丢弃。
- `lidar_node.py`：新增 `/scan_timed_v2`，旧 `/scan`、`/scan_timed` 完整保留；同时按上报转速插值一个数据包内 12 个点的相对采集时间。
- `launch/cartographer_scan_v2_launch.py`：独立 A/B 启动入口，仅将 Cartographer 的 `scan` 输入改为 `/scan_timed_v2`，其余底盘、IMU、Lua、RViz 参数与现有 `cartographer_2d_launch.py` 一致。
- `test/test_fixed_scan_grid.py`：覆盖 384/396 点数切换、实测角度网格定位和畸形整圈拒收。

离线检查结果：3 项核心测试全部通过，`fixed_scan_grid.py`、`lidar_node.py`、新 launch 和 `setup.py` 均已通过 Python 语法编译。

Ubuntu 编译与测试：

```bash
cd "<测试副本根目录>/lidar/chapt1_ws"
rm -rf build/lidar_py install/lidar_py
colcon build --packages-select lidar_py --symlink-install
source install/setup.bash
ros2 launch lidar_py cartographer_scan_v2_launch.py
```

新启动入口不代替旧入口。回退时仍执行：

```bash
ros2 launch lidar_py cartographer_2d_launch.py
```

本阶段不再改 Cartographer 搜索角度和权重。先验证静止状态 `/scan_timed_v2` 是否消除假旋转，通过后再进入慢速转圈去畸变和 `num_subdivisions_per_laser_scan` 调整。
### 11.34 最新修改标签：CARTOGRAPHER_LONG_RANGE_ODOM_GAP_GUARD_V2_2026-07-13

本次基于 `测试2` 的 27.7 分钟长距离、同层两遍重复建图日志修正。没有修改 `open_all.sh`、`open_all_log.sh`，也没有覆盖原 `cartographer_scan_v2_launch.py` 的默认行为。

#### 数据结论

- STM32 `0x07` NAVI 共 82371 帧，校验错误 0；绝大多数 MCU tick 间隔为 20 ms。
- `/imu_cartographer` 约 49.86 Hz，`/scan_timed_v2` 约 5.99 Hz。
- `/odom` 平均仅约 45.6 Hz，并出现约 5.277、8.537、8.136、8.704、8.923 秒等大断档。
- odom 断档与 `map -> odom` 最大约 9.311 m / 24.748 度的修正簇时间一致，说明剩余楼梯间歪图和第二遍双墙首先是上位机 odom 发布断档，随后在重复走廊产生错误约束，并非 STM32 校验或 yaw 传输本身中断。

#### 代码修改

- `chassis_node.py` 新增 `odom_publish_mode=timer|navi`。默认 `timer` 保持旧行为；`navi` 模式每解析一个 `0x07` 帧，就使用对应 MCU 映射时间发布一次 `/odom` 和 TF。补齐首帧及时间重同步分支的 IMU/odom 发布。
- `fixed_scan_grid.py` 和 `lidar_node.py` 新增 `fixed_scan_min_valid_points`。默认 0 不影响旧模式；长距离 V2 使用 180，拒绝严重稀疏整圈。
- `cartographer_scan_v2_launch.py` 新增可选的 NAVI odom、串口窗口、稀疏门限和 `laser_filters` 参数，全部默认关闭或旧值。
- 新增 `cartographer_long_range_v2_launch.py`：启用逐 NAVI odom、关闭阻塞式串口 GUI、启用 `/scan_timed_v2` 散点滤波和 180 点门限。
- 新增 `cartographer_2d_long_corridor_guarded.lua`：缩小长走廊搜索范围、提高匹配门槛、限制远距离约束并记录 constraint match。
- 新增 `cartographer_2d_long_corridor_no_loop.lua`：仅用于判断是否为错误回环，禁止作为最终地图配置。
- `setup.py` 已安装上述新 launch 和 Lua 文件。
- `test_fixed_scan_grid.py` 新增稀疏扫描拒绝和正常扫描放行测试。

#### 验证

- 相关 Python 文件全部通过 `py_compile`。
- 固定扫描 5 项测试全部通过：384/396 点输出几何一致、实测角度索引、畸形圈拒绝、稀疏圈拒绝、正常圈放行。
- Windows 当前没有 ROS2 Foxy 和 Cartographer 运行环境，必须按根目录 `长距离建图V2_使用说明.md` 在 Ubuntu 编译并完成实车 A/B。

#### 推荐启动

```bash
ros2 launch lidar_py cartographer_long_range_v2_launch.py \
  chassis_serial_port:=/dev/ttyUSB0 \
  lidar_serial_port:=/dev/ttyUSB1 \
  use_rviz:=true
```

完整日志要求、验收阈值、旧版回退和禁回环诊断步骤见 `长距离建图V2_使用说明.md`。

### 11.35 最新修改标签：LD14P_30000MS_WRAP_AND_CORRIDOR_V3_2026-07-13

#### 测试3数据结论

- 地图在约 `20:39:30-20:40:15` 离开初始房间进入走廊时已经开始歪，早于进入后续超大房间。
- 走廊直行时 NAVI yaw 基本稳定，但地图 yaw 仍发生约 2 度变化，证明局部 scan matching 在重复长墙环境中拉偏朝向。
- 终端日志约每 29.88 秒出现一次 `scan_time=0.04x s` 异常整圈。LD14P 官方协议规定时间戳在 30000 ms 清零，而原驱动按 uint16 的 65536 周期计算，回绕模型错误。
- Cartographer 曾接受分数约 68%-79%、最终位移修正约 3-7 m 的约束；旧 `max_constraint_distance=5.0` 不能约束匹配完成后的修正结果。
- `V4_LONG` rosbag 在实际长距离建图开始前已经停止写入，本次结论由完整地图快照、`robot_pose.jsonl`、NAVI 日志和 Cartographer 终端交叉得到。下一轮必须全程正常录包。

#### 代码修改

- 新增 `lidar_py/lidar_timing.py`，按 30000 ms 周期连续展开 LD14P 时间戳；正常回绕不再重置 ROS 时间映射或当前扫描。
- `lidar_node.py` 改用上述计时器，并记录前三次正常回绕日志；真正倒退或超过 2000 ms 的异常跳变仍执行安全重同步，越界时间戳包只丢弃并报警。
- 新增 `test/test_lidar_timing.py`，覆盖 29990 -> 5 ms 回绕、异常倒退和异常大跳。
- 新增 `cartographer_2d_corridor_time_fixed.lua`：局部窗口收紧到 0.06 m / 2 度，提高平移和旋转先验权重；跨 submap 约束门槛提高到 0.80，全局门槛 0.85。
- 新增 `cartographer_long_range_v3_launch.py`，旧 V2 launch 和 Lua 均保留。
- `setup.py` 已安装新 V3 launch 和 Lua。
- 没有修改 `open_all.sh` 或 `open_all_log.sh`。

#### 验证

- Python 语法检查通过。
- 固定扫描原 5 项测试和时间计数器新增 3 项测试全部通过，共 8 项。
- Windows 无 ROS2 Foxy 实车环境，下一轮按根目录 `排查V5.md` 先完成 70 秒跨回绕静止测试和初始走廊短测，不应直接再次扫描整层。

### 11.36 最新修改标签：NAVI_YAW_WZ_AND_LIDAR_CLOCK_COHERENCE_V4_2026-07-14

#### 测试4/V5数据结论

- LD14P `30000 ms` 时间回绕修复实测通过：约 120 秒跨 3 次回绕，无 `scan_time=0.04x s` 异常圈，固定扫描持续有效。
- V5 rosbag 完整覆盖 292 秒，可直接对齐 `/odom`、`/imu_cartographer` 和 `/robot_pose`。
- 对 2010 个有效转弯样本统计发现：odom 绝对 yaw 的导数与发布的 wz 共 2010 次反号、0 次同号，反号率 100%。两者绝对值中位数分别为 0.1329 和 0.1319 rad/s，证明是符号配置冲突而非随机噪声。
- 当前 STM32 原始 yaw 和原始 vz 方向本来一致；旧启动链路的 `navi_vz_sign=-1.0` 将角速度多翻转了一次。
- `/scan_timed_v2` 的入包时间减消息时间戳从约 `+17 ms` 漂移到约 `-1042 ms`，292 秒累计约 `1.059 s`（约 `3620 ppm`）；末段雷达消息已经被标到主机未来，而 odom/IMU 延迟始终约为数毫秒。
- 原 V3 固定设备时钟 offset 的策略没有补偿 LD14P 与主机之间约 0.36% 的频差，长时间运行会产生雷达与底盘数据错时。
- `constraint_builder` 的拒绝记录属于 pose graph 约束，不代表逐帧局部 scan matching 未运行。V5 结果中“17 次拒绝所以完全依赖 odom”的推断不成立。
- V5 没有提供真实尺量距离，因此暂时不能根据 `odom=4.39 m` 判断 vx 尺度错误。

#### 代码修改

- 新增 `cartographer_long_range_v4_launch.py`，继承 V3 的时间修复、固定扫描和 Lua 参数，仅将 `navi_vz_sign` 明确设为 `+1.0`，并保持 `navi_yaw_sign=+1.0`。
- `chassis_node.py` 新增绝对 yaw 导数与 wz 的在线符号一致性检查；累计至少 25 个有效转弯样本且反号率达到 80% 时，输出 `NAVI yaw/vz sign mismatch` 错误。
- `lidar_timing.py` 新增 `MonotonicMinimumDelayMapper`，仅跟踪更早的串口 offset 观测并将单包调整限制为 `0.1 ms`，避免 USB 延迟尖峰和设备时钟频差把时间轴拉向未来。
- `lidar_node.py` 改用上述漂移跟踪映射，并在周期日志输出 `device_lag` 供实车验收。
- `test_lidar_timing.py` 新增五分钟快设备时钟仿真与 USB 延迟尖峰测试。
- `setup.py` 安装新增 V4 launch。
- 保留原 V3；未修改 `open_all.sh` 和 `open_all_log.sh`。

#### 下一轮验证

- 按根目录 `排查V6.md` 先静止 70 秒验证 `device_lag` 不累积，再完成顺/逆时针各一次的原地符号验证，最后走与 V5 完全相同的初始房间出口和短走廊。
- V6 不调整 Cartographer Lua、轮径、vx 比例、外参或 submap 参数，确保是单变量验证。
- `排查V6.md` 已补充完整判定树：频率、时钟、转向符号、静止漂移、直线尺度、原地转角、转弯平移、局部走廊漂移、突然跳图、回环失败和扫描噪点均给出明确阈值与单变量调整顺序。
- V4 新增可从启动命令覆盖的 `lidar_clock_max_adjustment_ns`、`navi_vx_scale`、`navi_turn_vx_scale` 和 `navi_turn_wz_threshold_rad_s`，默认仍分别为 `100000`、`1.0`、`0.75`、`0.25`，不改变当前基准行为。V5 转弯角速度大多约 `0.13 rad/s`，低于旧阈值 `0.25 rad/s`，因此 V5 中转弯 vx 折减基本未生效。

### 11.37 最新修改标签：V6_5_REPLAY_AND_TURN_DESKEW_AB_2026-07-14

#### V6 全部测试复核结论

- V6_5 rosbag 完整覆盖约 449.2 秒，`/odom` 和 `/imu_cartographer` 均约 50Hz，固定扫描约 6.03Hz；雷达设备时间映射、NAVI 符号和 30000ms 回绕修复均保持正常。
- 用户描述的走廊末端原地转向实际发生在约 `13:39:29-13:39:53`。此期间 odom 平移仅约 1mm，排除“原地转向被误积分成前进距离”。
- 每圈约 165.9ms；圈内绝对 yaw 变化与 wz 积分变化平均只差约 0.010 度，排除 IMU/雷达时间错位和 yaw/wz 积分不一致。
- 原地约 180 度转向期间，Cartographer 的 map 相对 odom 航向修正范围约 2.94 度，局部位置在 odom 不动时被拉动约 0.14m；该时段没有新增跨 submap 约束，因此是局部前端在长走廊和机械抖动下的偏移，不是错误大回环。
- 约 `13:40:35` 接受的唯一相关约束仅修正约 0.05m/1.2度、分数80.5%，不是转向偏移的起点。
- V6_1 最终地图几乎没有残影，但长走廊被逐渐拉成轻微 U 型；V6_5 走廊明显更直，但返程存在平行残影。这证明静态权重存在明确折中：权重过低时激光可连续合墙却会在退化走廊中弯曲，权重偏高时 odom/IMU 可保持直线却不易吸收小角度分歧。

#### 隔离测试代码

- 新增 `cartographer_2d_v7_subdiv2.lua`：保持 V6 的 `rw135/rd12`，只将6Hz整圈拆成两个带独立时刻的半圈，并用 `num_accumulated_range_data=2` 保持每次局部匹配仍使用完整一圈。
- 新增 `cartographer_2d_v7_subdiv2_rw130.lua`：相比上一组只将 Ceres 旋转先验权重从135小幅降到130，用于寻找“走廊不弯”和“残影能合拢”的中间值。
- 新增 `cartographer_v7_replay_launch.py`：只用于 rosbag 离线重放，不启动真实底盘和雷达串口。
- 新增 `odom_tf_replay.py`：从录制的 `/odom` 单独重建 `odom->base_link`，避免重放旧 `/tf` 时把旧 Cartographer 的 `map->odom` 混入新结果。
- `setup.py` 只增加上述新文件的安装项；没有修改 `open_all.sh`、`open_all_log.sh`、V4 启动入口或 V6 基准 Lua。

#### 验证方法

根目录新增 `排查V7.md`。必须用同一份 V6_5 bag 按 A/B/C 顺序离线重放：

1. A：V6 基准 `rw135/rd12/subdiv1`，先确认可复现；
2. B：保持 `rw135/rd12`，只改为 `subdiv2/accumulated2`；
3. C：保持 B 的分段，只将 `rw135 -> rw130`。

原先计划的 `rw150` 主测试已根据 V6_1 与 V6_5 地图对照取消：继续增大权重可能进一步强化“直但有残影”，无法解决当前折中。回放只允许输入 `/scan_timed_v2_filtered`、`/odom`、`/imu_cartographer`、`/tf_static`，禁止播放旧 `/tf`、`/map` 和旧 SLAM 输出。只有 A 能复现后才能比较 B/C；若三组同样偏移，下一轮才考虑转向扫描质量门控，不得直接把门控混入稳定版。

### 11.38 最新修改标签：PUBLISH_LATENCY_ONLY_AND_FINAL_SAVE_2026-07-16

#### 修改范围

- 冻结当前已经取得较好效果的 Cartographer、SLAM Toolbox、Nav2、雷达滤波、车体尺寸、外参、QoS 和串口协议参数，没有进行任何调参。
- `chassis_node.py` 新增低开销发布耗时统计，分别测量 `/robot/serial_debug`、`/imu`、`/imu_cartographer`、`odom->base_link` TF 和 `/odom` 的发布调用。
- 统计使用 `time.monotonic_ns()`；默认每秒输出一行 `PUBLISH_LATENCY` 汇总，单次超过 20ms 时输出 `PUBLISH_STALL`，包含通道、耗时、NAVI 帧号、MCU tick 和 ROS 时间。
- 新增节点参数 `publish_latency_stats=true`、`publish_stall_warn_ms=20.0`、`publish_latency_report_sec=1.0`；关闭统计只需将第一个参数设为 `false`。
- `slam_logger.py` 继续保持 `start_enabled=false`，默认不周期写地图或陀螺仪；手动开启时的默认保存间隔由 0.5 秒改为 2 秒。
- 测试结束只保留 `map_saver_cli` 生成的最终 PGM/YAML，以及 Cartographer `/write_state` 生成的 `.pbstream`。
- `排查V13.md` 已重写为“只统计发布耗时、不改参数”的完整实车步骤和判定方法。
- 未修改 `open_all.sh`、`open_all_log.sh`。

#### 验证

- `chassis_node.py` 与 `slam_logger.py` 均通过 `python -m py_compile`。
- 配置目录全部文件 SHA256 保持不变；当前基准 `cartographer_2d_v9_tightened.lua` 仍为 `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。
- Windows 环境未运行 ROS2 Foxy 实车测试，Ubuntu 编译、测试和最终保存步骤见 `排查V13.md`。

### 11.39 最新修改标签：WEB_MAPPING_PROFILE_NO_STM32_0X08_2026-07-16

#### 行为调整

- V13 建图、导航、滤波、里程计和启动参数全部冻结，本次没有修改任何 SLAM 配置。
- 网页“建图 ON/OFF”改为纯上位机速度模式开关：ON 使用两档建图速度，OFF 使用四档常规速度。
- 网页按钮改发 `gear_change`，携带 `profile=mapping/normal` 和一档状态；`chassis_node` 只更新 `mapping_mode`、`web_profile`、`web_gear` 并通过 `/robot/control_state` 同步所有浏览器。
- 启动默认队列删除 `STARTUP_MAPPING`，现在只发送 `ECHO_OFF 0x06` 和零速 `MOVE 0x01`。
- `_handle_serial_command()` 对旧网页缓存发来的 `mapping_on/mapping_off` 做本地兼容处理，输出 `WEB_PROFILE_ONLY ... serial 0x08 suppressed`，绝不写串口。
- `0x08` 仍保留在上行/回响解析名称中，便于识别历史数据，但不再有任何主动发送路径。
- 网页按钮副标题由“底盘模式”改成“网页速度模式”，避免误解为下位机工作模式。
- `CAR_使用教程_注意事项.md` 已同步更新当前行为。
- 未修改 `open_all.sh`、`open_all_log.sh` 和任何 Cartographer/SLAM/Nav2 配置。

#### 验证

- `chassis_node.py` 通过 `python -m py_compile`。
- 网页通过 `npm run build`，生产文件已更新到 `web/dist`。
- 静态检查确认 `STARTUP_MAPPING`、`WEB_MAPPING_ON/OFF` 和 `CTRL_CMD_MAPPING` 主动发送映射均已移除。

### 11.40 最新修改标签：WEB_SLAM_LOG_INTERVAL_2026-07-16

#### 行为调整

- 网页 `SLAM Log` 按钮旁新增记录间隔输入框，默认 `3` 秒、默认关闭。
- 输入范围为 `0.5–60` 秒，步进 `0.5` 秒；关闭日志时也可预先修改间隔。
- 网页通过 `/robot/web_control` 发送 `slam_log_enable`、`slam_log_disable` 或 `slam_log_config`，统一携带 `interval_sec`。
- `chassis_node.py` 保存 `slam_log_enabled` 和 `slam_log_interval_sec`，并通过 `/robot/control_state` 每 0.5 秒同步到所有网页设备。
- `slam_logger.py` 支持运行时重建保存定时器，并订阅 `/robot/control_state` 恢复当前开关和间隔，避免错过单次网页消息。
- 日志默认参数由 `2.0` 秒改为 `3.0` 秒，`start_enabled` 继续保持 `false`。
- 本次只修改日志控制与网页界面；未修改 `open_all.sh`、`open_all_log.sh` 和任何 Cartographer、SLAM Toolbox、Nav2、雷达滤波参数。

#### 验证

- `slam_logger.py` 与 `chassis_node.py` 通过 `python -m py_compile`。
- 网页通过 `npm run build`，新生产文件已写入 `web/dist`。
- 浏览器实测桌面端按钮与输入框无重叠；390px 窄屏无横向溢出。
- `cartographer_2d_v9_tightened.lua` SHA256 仍为 `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。
- `open_all.sh` 与 `open_all_log.sh` 哈希保持不变。

### 11.41 最新修改标签：JAZZY_AUTO_MAPPING_TO_FOXY_FRONTIER_NAV2_2026-07-16

#### 来源工程审查

- 完整审查 `auto_mapping_v1` 的启动脚本、`frontier_exploration_ros2`、Nav2/SLAM 参数、动态扫描过滤、路口监督、自动保存和自定义短目标 BT。
- 原工程针对 Ubuntu 22.04 + ROS2 Jazzy/Humble，使用新版 Nav2 接口、BehaviorTree.CPP、自定义 BT 插件和 Smac State Lattice；这些内容不能直接复制到 Ubuntu 20.04 + ROS2 Foxy。
- 移植保留其 WFD 前沿搜索、costmap 验证、NavigateToPose 调度、失败前沿屏蔽、完成确认、返回起点和自动保存等核心行为，使用 Python 3.8 + Foxy 原生接口重新实现。
- 原工程的仿真世界真值过滤、Jazzy 专属 C++ typesupport、自定义短目标 BT 和 Smac 配置没有混入测试9，避免破坏现有 Foxy Nav2。

#### 新增实现

- `lidar_py/frontier_core.py`：独立 WFD 核心，只搜索与机器人连通的已知自由区域；支持前沿聚类、长前沿拆分、车体净空、目标后退距离、信息增益/路程/朝向评分及近重复目标去除。
- `lidar_py/frontier_explorer_foxy.py`：订阅 `/map`、`/global_costmap/costmap` 和 `/robot_pose`，只向 `/navigate_to_pose` 发送目标；支持服务开关、失败屏蔽、无进展/总时长超时、连续完成确认和可选返回起点。
- `lidar_py/auto_map_saver.py`：收到 `/exploration_complete` 后保存 PGM/YAML 与包含未结束 submap 的 PBStream；不调用 `finish_trajectory`。另提供 `/auto_mapping/save_now` 手动保存服务。
- `config/auto_mapping_foxy.yaml`：按当前约 0.666m Nav2 方形 footprint 设置候选点净空、前沿最小尺寸、失败半径和完成确认参数。
- `launch/cartographer_auto_mapping_foxy_launch.py`：包含原 `cartographer_scan_v2_launch.py`，明确使用 V13 的 `cartographer_2d_v9_tightened.lua`、`/scan_timed_v2`、180 点门限、固定扫描滤波、NAVI 逐帧 odom 和 absolute yaw；在外围增加 Foxy Nav2、视觉安全融合、自动探索和保存节点。
- 根目录新增 `start_auto_mapping_foxy.sh`，完成串口识别、编译、ROSBridge、V13 Cartographer、Nav2、SDK 深度相机、网页和自动探索的一键启动。
- 根目录新增 `自动建图_Foxy_使用教程.md`，包含目录、安装、启动、停止、状态、保存、参数、首次实车验收和回退方法。

#### 控制权与安全

- 自动探索节点不发布 Twist、不计算轮速、不打开串口；唯一控制链仍为 `Nav2 /cmd_vel_nav -> safety_fusion_node /cmd_vel_safe -> chassis_node -> STM32`。
- 默认 `explorer_autostart=false`，必须在完成深度 baseline、确认 MOVE 接管和急停可用后，通过 `/auto_mapping/set_enabled` 服务启用。
- 自动建图没有新增或恢复 `0x08` 发送；网页建图模式仍只负责上位机速度档位。
- 自动探索 launch 不启动 `web_goal_nav_node`，避免自动前沿目标与网页确认的手动 Nav2 目标互相抢占；网页地图和路径预览仍保留。

#### 验证与冻结项

- `frontier_core.py` 的 4 项算法测试全部直接执行通过：旋转地图原点坐标换算、开口前沿检测、全已知地图完成判定、机器人不在已知区的安全拒绝。
- 新增 Python/launch/setup 文件均通过 `python -m py_compile`；Windows 环境没有 ROS2 Foxy，ROS graph、Nav2 lifecycle 和实车运动必须按教程在 Ubuntu 验证。
- 没有修改 `open_all.sh`、`open_all_log.sh`、`cartographer_scan_v2_launch.py`、`cartographer_2d_v9_tightened.lua`、`nav2_params.yaml` 或 STM32 工程。

### 11.42 最新修改标签：FULL_NAV2_POLICY_AND_WEB_GOAL_FOXY_PORT_2026-07-16

> 本节记录第一阶段 Hybrid-A* 等价移植；当前默认方案已由 11.43 的 State Lattice 源码回移植取代，本节文件仍作为回退方案保留。

#### 为什么旧 Nav2 哈希仍保持不变

- `config/nav2_params.yaml` 保留原哈希，只是为了让测试9已验证的旧启动链可随时回退，不代表新自动建图继续使用旧导航参数。
- 新 `cartographer_auto_mapping_foxy_launch.py` 的默认参数已切换为 `config/nav2_auto_mapping_foxy.yaml`；启动时终端和 `ros2 param dump` 应显示该新配置。
- `open_all.sh`、`open_all_log.sh`、V13 Cartographer Lua 和 STM32 工程仍未修改。

#### Nav2 调参和导航逻辑完整移植

- 新增 `config/nav2_auto_mapping_foxy.yaml`，迁移对方项目的 20Hz DWB、`required_movement_radius=0.10`、`movement_time_allowance=30s`、`xy/yaw=0.25`、前进优先批评器、车体 footprint 碰撞批评器、Path/Goal 对齐权重和恢复速度。
- 车体 footprint 不照搬原项目 0.72m 方形，保持当前实车已校准的 `+-0.333m`；Nav2 最大前进速度限制为 `0.23m/s`，与视觉安全层上限一致。
- Foxy 没有 Jazzy `SmacPlannerLattice`，因此使用 Foxy 原生 `smac_planner/SmacPlanner` Hybrid-A*；使用 `REEDS_SHEPP`、`reverse_penalty=8.0`、`minimum_turning_radius=0.50m` 和内置 cost-aware smoother，实现“前进优先，必要时才倒退”的等价策略。
- Foxy 没有 Jazzy 独立 Rotation Shim/Constrained Smoother/behavior server 组合；新增 `behavior_trees/navigate_to_pose_forward_preferred_foxy.xml`，用 Foxy 标准 BT 实现 0.2Hz 低频重规划、单层 costmap 自恢复、清图、低速后退、90度旋转和等待重试。
- `recoveries_server` 的 `/cmd_vel` 已重映射到 `/cmd_vel_nav`，恢复旋转/后退也必须通过 `safety_fusion_node`，不会绕开视觉安全链。
- `setup.py` 安装新 Nav2 配置和 BT XML，`package.xml` 新增 `smac_planner` 依赖；一键脚本启动前会检查 `ros-foxy-smac-planner`。

#### 网页选点导航与自动探索仲裁

- 新自动建图 launch 现在同时启动 `web_goal_nav_node` 和 `web_path_preview_node`。网页未确认的点仅计算 `/web/preview_path`，不会产生运动。
- `web_path_preview_node` 会在 ROS 主机端重新打时间戳，并丢弃快速连续选点产生的过期规划结果，避免浏览器时钟不同步导致预览线不显示或回跳。
- 网页确认 `/web/nav_goal` 后，`web_goal_nav_node` 先调用 `/auto_mapping/set_enabled=false`，然后取消自己的旧网页目标并发送新 `NavigateToPose`。
- `frontier_explorer_foxy` 也直接监听 `/web/nav_goal` 作为第二道保险；收到人工确认目标时立即暂停并取消当前 frontier，该取消不会记为导航失败或屏蔽正常前沿。
- Frontier 失败调度与原项目对齐：同一区域连续失败 2 次后才屏蔽 300s；若剩余所有前沿都已屏蔽，需连续 8 次地图更新确认后才结束；默认完成后原地停止，不自动返回起点。
- 网页目标发送加入序号仲裁：快速连续点击、取消或旧 action 延迟应答时，过期目标会自动取消，不会把旧 handle 误认成当前目标。
- 网页导航和自动探索共用新 Foxy Nav2 配置，两者输出都只能通过 `/cmd_vel_nav -> safety_fusion_node -> /cmd_vel_safe -> chassis_node -> STM32`。

#### 验证

- `frontier_explorer_foxy.py`、`web_goal_nav_node.py`、`web_path_preview_node.py`、新 launch 和 `setup.py` 通过 Python 语法检查；新 BT XML 通过 XML 解析。
- 网页源码已确认：点击预览发布 `/web/preview_goal`，确定导航发布 `/web/nav_goal`，不需要改变已定版 UI。
- Windows 环境无 ROS2 Foxy，Smac 插件加载、BT lifecycle、网页目标抢占和实车轨迹仍需按 `自动建图_Foxy_使用教程.md` 在 Ubuntu 上验收。

### 11.43 最新修改标签：FULL_STATE_LATTICE_NAV2_FOXY_BACKPORT_2026-07-16

#### 完整导航能力回移植

- 新增 `nav2_smac_planner_foxy`，将 Humble 版 State Lattice 规划器按 Foxy `nav2_core::GlobalPlanner` 接口回移植；只编译 `SmacPlannerLattice`，不会覆盖 Foxy 自带的 `smac_planner`。
- 新增 `nav2_rotation_shim_controller_foxy`，将 Galactic Rotation Shim 按 Foxy 双参数 `computeVelocityCommands(pose, velocity)` 接口回移植。
- 新增 `short_goal_bt_foxy`，移植起步预旋转、旋转空间安全检查、控制器切换、倒车脱困监测和倒车后的重新对齐。
- BehaviorTree.CPP 3.5 只有一个 Parallel 阈值，无法直接表达原项目的“任一成功或失败即结束”；新增 `ParallelSuccessOneFailureOne` 控制节点保持原并行语义。
- 新增 `lattice_forward_turnaround_5cm.json`，包含 0.05m 分辨率、16 个朝向和 128 条差速运动原语。

#### 默认 State Lattice 导航配置

- 新增 `nav2_auto_mapping_state_lattice_foxy.yaml`，默认使用 `nav2_smac_planner/SmacPlannerLattice`。
- `FollowPath` 使用 `nav2_rotation_shim_controller::RotationShimController` 包装前向优先 DWB；`FollowPathNoShim` 作为原地旋转不安全时的普通 DWB 回退。
- 保留对方项目的 20Hz 控制、0.2Hz 全局重规划、0.10m/30s 进度检查、倒车高惩罚和受控恢复逻辑。
- 车体 footprint 和视觉安全融合继续使用当前实车尺寸；所有运动仍经过 `/cmd_vel_nav -> safety_fusion_node -> /cmd_vel_safe -> chassis_node -> STM32`。
- `cartographer_auto_mapping_foxy_launch.py` 已修正 lattice 路径注入位置：`GridBased.lattice_filepath` 只传给 `planner_server`，不再误传给 `controller_server`。
- `start_auto_mapping_foxy.sh` 默认 `NAV_PROFILE=state_lattice`；保留 `NAV_PROFILE=hybrid` 作为编译或现场问题的快速回退方案。
- 修正回退构建隔离：`state_lattice` 模式编译三个回移植包和 `lidar_py`，`hybrid` 模式只执行 `--packages-select lidar_py` 并使用 Foxy 系统 `smac_planner` 与标准 BT。即使 State Lattice、Rotation Shim 或自定义 BT 编译失败，Hybrid 仍可独立启动，不再是名义回退。
- 新导航验证和启动改用独立 `build_auto_mapping_foxy`、`install_auto_mapping_foxy`、`log_auto_mapping_foxy`，避开工程复制后旧 `build/install` 软链接的 `File exists` 问题，也不污染定版启动链的构建产物。
- 修复 State Lattice 公共链接清单：补回官方 Lattice 依赖的 `costmap_downsampler.cpp`，并在创建 Foxy `Costmap2DPublisher` 前将生命周期节点弱指针 `lock()` 为共享指针，避免插件加载时未定义符号及 Foxy 构造参数不匹配。

#### 网页目标与 frontier 仲裁

- 网页确认目标会先暂停自动 frontier，并等待自动目标取消完成后再发送网页 `NavigateToPose`，避免两个 action 客户端同时抢目标。
- 快速连续点击只保留最新序号；旧目标的延迟接受结果会自动取消。
- 修正 Nav2 action server 未就绪时先清空待发送目标的问题：现在目标会保留，服务器可用后再发送。
- 未确认的网页点继续只计算和显示规划路线，不产生运动。

#### 验证与文档

- 新增 `validate_auto_mapping_foxy.sh`，在 Ubuntu 上无硬件执行 Bash、依赖、colcon 编译、YAML/JSON/XML、动态库依赖和 launch 参数检查。
- 重写 `自动建图_Foxy_使用教程.md`，加入完整文件结构、依赖、默认/回退启动、网页选点、插件确认命令和首次实车验收步骤。
- Windows 已完成 Python AST/py_compile、BT/package XML、State Lattice JSON 和导航 YAML 静态检查。
- 当前 Windows 没有 ROS2 Foxy 和 Ubuntu 编译环境，三个 C++ 插件必须先在 Ubuntu 执行 `./validate_auto_mapping_foxy.sh`，通过后再架空车轮验收。
- 定版 `open_all.sh`、`open_all_log.sh`、`cartographer_2d_v9_tightened.lua` 和原 `nav2_params.yaml` 哈希保持不变。

### 11.44 最新修改标签：ROTATION_SHIM_HYSTERESIS_AND_LATTICE_ORIENTATION_FOXY_2026-07-16

#### 补齐原项目实际启用的 Rotation Shim 行为

- Foxy 回移植新增 `angular_disengage_threshold` 和 `in_rotation_` 状态：路径朝向误差超过 `2.80 rad` 才进入原地对齐，进入后持续旋转至误差小于 `0.10 rad` 才交还 DWB，避免旧 Galactic 逻辑在误差刚降到 `2.80 rad` 时过早退出。
- 新增 `closed_loop=true`，角加速度限制使用底盘反馈的当前角速度；同时保留上一次指令作为可选开环基准。
- 新增 `use_path_orientations=true`，使用 State Lattice 路径点姿态判断目标行驶朝向。这样规划器产生倒车原语时，Rotation Shim 不会仅因采样点位于车后方就命令小车掉头。
- 旋转碰撞预测改为比较绝对模拟转角，并按当前退出阈值限制预测区间，左转和右转采用完全对称的检查。
- 原项目的 `rotate_to_goal_heading=false`、`rotate_to_heading_once=false` 均未启用，因此 Foxy 不增加无行为收益的 GoalChecker 新接口；初始对齐和最终目标处理继续由现有自定义 BT 与 DWB 完成。

#### 验证与冻结项

- `validate_auto_mapping_foxy.sh` 新增四项配置断言：进入阈值 `2.80`、退出阈值 `0.10`、闭环反馈开启、路径姿态模式开启。
- `FOXY_BACKPORT.md`、`自动建图_Foxy_使用教程.md` 和 `CAR_使用教程_注意事项.md` 已同步说明。
- 未修改定版 `open_all.sh`、`open_all_log.sh`、`cartographer_2d_v9_tightened.lua` 或原 `nav2_params.yaml`。

### 11.45 最新修改标签：UNIFIED_LAUNCHER_PROCESS_GROUP_AND_FINAL_SAVE_2026-07-16

#### 两个一键启动入口

- `open_all.sh` 现在是统一运行实现，启动当前定版的
  `cartographer_auto_mapping_foxy_launch.py`，也就是
  `/scan_timed_v2`、`cartographer_2d_v9_tightened.lua`、Foxy Nav2、
  网页选点导航、视觉安全融合和可选 frontier 自动建图链路。
- `open_all_log.sh` 不再维护第二份容易漂移的启动代码，只设置
  `SAVE_FINAL_ARTIFACTS=true` 后复用 `open_all.sh`。
- 两个入口都显式传入 `show_serial_window:=false`，不再打开显示 STM32
  NAVI/串口帧的 OpenCV 窗口；NAVI 帧仍正常接收、解析和发布。
- 两个入口都启动根目录 `slam_logger.py`，参数为
  `start_enabled=false`、`save_interval_sec=3.0`。网页可以实时开关记录并
  修改 0.5-60 秒间隔，地图快照和陀螺仪/NAVI 数据写入本次会话的
  `Logger/` 目录。

#### 保存策略

- `open_all.sh`：退出时不调用 `map_saver_cli`，不调用 `/write_state`，
  不生成专门的最终 PGM/YAML 或 PBStream。网页若主动开启周期日志，
  已经产生的周期快照仍会保留。
- `open_all_log.sh`：退出时先停止 frontier、取消网页导航目标、发送零速
  MOVE，并关闭后续串口运动输出；随后在 Cartographer 仍运行时
  保存 `Final/final_map.pgm`、`Final/final_map.yaml` 和
  `Final/result.pbstream`，保存完成后才关闭 ROS 节点。
- 两个脚本都不再自动录制 rosbag，避免长时间运行产生大体积文件。
- `cartographer_auto_mapping_foxy_launch.py` 新增
  `enable_auto_map_saver` 参数，默认仍为 `true`，不会改变原
  `start_auto_mapping_foxy.sh` 的行为。两个 `open_all*` 入口显式传入
  `false`，防止 frontier 完成时额外生成一套地图；最终保存只由所选脚本
  的退出策略决定。

#### 启动和退出可靠性

- ROSBridge、完整 ROS launch、深度相机、Logger 和 Vite 分别在独立
  session/process group 中启动，脚本记录每个 PGID。
- `Ctrl+C` 后按 `SIGINT -> SIGTERM -> SIGKILL` 分级关闭整个进程组，
  不再只杀最外层 `ros2 launch` 或短暂存在的网页启动包装进程。
- Vite 由脚本直接启动并纳入进程组，不再通过会立即退出的
  `start_web.py` 间接启动。
- 启动前先清理旧 ROS/网页进程和 9090、5173、8080 端口；退出后再做
  一次已知节点和端口兜底清理，并停止 ROS daemon。
- 启动阶段增加串口、依赖、ROS 核心节点和三个网络端口的就绪检查；
  任一关键进程组异常退出会触发整套清理，避免留下半套系统。

#### 使用

```bash
cd ~/测试9
chmod +x open_all.sh open_all_log.sh

# 日常运行：网页可控周期日志，退出不保存最终地图/PBStream
./open_all.sh

# 需要保留最终建图结果：网页可控周期日志，退出保存最终文件
./open_all_log.sh
```

正常结束只按一次 `Ctrl+C`，等待终端出现 `Shutdown complete`。不要在保存
最终地图时直接关闭终端或断电。

#### 静态验证

- `open_all.sh` 与 `open_all_log.sh` 已通过 Git Bash `bash -n`。
- `cartographer_auto_mapping_foxy_launch.py` 已通过 `python -m py_compile`。
- 定版 `cartographer_2d_v9_tightened.lua` 未修改，SHA256 仍为
  `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。

### 11.49 最新修改标签：PROJECT_CARTOGRAPHER_ONLY_CLEANUP_2026-07-16

#### 当前运行链收敛

- 项目只保留 `Cartographer V13 + Foxy Nav2 + State Lattice/Hybrid-A* + 深度视觉避障 +
  网页 + STM32`。
- 根目录只保留两个一键入口：
  - `open_all.sh`
  - `open_all_log.sh`
- `setup.py` 只安装当前两个 launch、六个配置、两个 BT、一个 RViz 配置和当前运行节点。
- `cartographer_scan_v2_launch.py` 的独立默认配置改为定版
  `cartographer_2d_v9_tightened.lua`，RViz 统一使用 `nav2_display.rviz`。

#### 删除的旧算法与入口

- 删除工作空间和根目录下载副本中的 RF2O。
- 删除 slam_toolbox 的 async/sync 参数、启动文件和生命周期激活节点。
- 删除 wheel/RF2O/fused 三套旧里程模式及 `odom_fusion_node.py`。
- 删除 Hector SLAM、旧 laser odometry、旧独立 Nav2/AMCL、旧融合 launch。
- 删除 Cartographer V2-V8 实验 Lua、长走廊实验 launch 和 rosbag 回放节点。
- 删除早期 Docker/Jazzy、旧 Foxy 多终端和重复的一键启动脚本。
- 删除普通 `build/install/log` 历史产物、Python 缓存和 Orbbec 运行日志。

#### 根目录整理

- 删除已被 `修改.md` 覆盖的 V2-V13 排查文档、V10 副本、AI 调试转录和早期使用说明。
- 重写 `CAR_使用教程_注意事项.md`，只描述当前目录、两个入口和真实运行方式。
- 新增 `.gitignore`，忽略 Python、colcon、SLAM Log、网页依赖/构建和 Keil 构建产物。
- 保留 `SLAM_Log`、`maps`、STM32 源码、网页依赖和深度相机两种读取实现。

### 11.48 最新修改标签：LOG_PROFILE_ROLE_CLARIFICATION_2026-07-16

- `open_all.sh` 与 `open_all_log.sh` 都会启动 `slam_logger.py`，周期地图和陀螺仪记录均由网页
  `SLAM Log` 开关控制，默认关闭，默认间隔 3 秒。
- `open_all.sh` 退出时不保存最终文件。
- `open_all_log.sh` 是完整保存入口。网页是否打开周期 `SLAM Log` 不影响退出保存；正常按一次
  `Ctrl+C` 后，脚本仍会保存：
  - `Final/final_map.pgm`
  - `Final/final_map.yaml`
  - `Final/result.pbstream`
- `open_all_log.sh` 继续复用 `open_all.sh` 的启动、清理和保存实现，通过
  `SAVE_FINAL_ARTIFACTS=true` 明确启用最终保存，避免两份长脚本产生逻辑差异。
- Log 版启动时会直接打印“周期日志由网页控制、最终三类文件保存已启用”；验证脚本也会检查
  普通版默认关闭最终保存、Log 版明确开启最终保存，防止后续误改。

### 11.47 最新修改标签：FOXY_BACKPORT_BUILD_AND_FINAL_SAVE_FIX_2026-07-16

#### 根据 `E:\3.txt` 确认的问题

Ubuntu 实测记录确认了以下独立问题，本轮没有修改 Cartographer V13、雷达滤波、里程计或 Nav2
运动参数：

1. State Lattice 依赖 `libompl-dev`、`nlohmann-json3-dev`、`angles` 和
   `behaviortree_cpp_v3`，缺少时 `colcon` 会直接失败。
2. 自定义 BT 使用了 Foxy 不支持的两参数 `create_callback_group()` 和手动
   `SingleThreadedExecutor::add_callback_group()`。
3. Rotation Shim 使用了 Foxy 不存在的 `transformPoseInTargetFrame()`，并用不兼容的方式
   声明 `primary_controller` 参数。
4. State Lattice 的 `AStarAlgorithm` 需要从 `GridCollisionChecker` 取得 costmap，但回移植
   类缺少 `getCostmap()`。
5. `lidar_py/setup.py` 强制安装不存在的 `maps/my_map.yaml` 和 `my_map.pgm`，导致最后一个
   Python 包构建失败。
6. 设备已经具备读写权限时，启动脚本仍尝试执行 root 所有者的 `chmod`，从而无意义地要求
   `sudo` 密码。
7. Foxy `map_saver_cli` 的 `save_map_timeout` 是整数参数，原来的 `15.0` 会报
   `expected [integer] got [double]`。
8. 强制 `SIGKILL` 不可能触发 Bash 的退出 trap，因此不会保存最终地图；保存期间缺少明确
   状态，也不容易区分“正在保存”和“已经卡死”。

#### Foxy C++ 兼容修复

- `short_goal_bt_foxy` 改用 Foxy 的单参数回调组接口。回调组自动随 BT Navigator 节点加入
  其 executor，不再创建第二个不兼容的内部 executor，也不会删掉订阅本身。
- `nav2_rotation_shim_controller_foxy`：
  - `primary_controller` 使用带默认字符串的 `rclcpp::ParameterValue`；
  - 位姿转换改用 Foxy `tf2_ros::Buffer::transform()`，带 0.1 秒超时和明确异常信息；
  - 补充 `tf2_geometry_msgs` 构建和运行依赖。
- `nav2_smac_planner_foxy` 为 `GridCollisionChecker` 增加只读语义的 costmap 指针访问器，
  供现有 A* 搜索初始化使用。
- `lidar_py/setup.py` 删除无实际用途的占位地图安装项。当前自动建图和网页地图均来自实时
  `/map`，不需要伪造 `my_map.yaml/pgm`。

#### 一键启动与权限

- State Lattice 构建前自动检查以下系统依赖，缺失时打印完整安装命令后停止，不再让 CMake
  输出难以判断的末级错误：

```bash
sudo apt install -y \
  libompl-dev nlohmann-json3-dev python3-yaml \
  ros-foxy-angles ros-foxy-behaviortree-cpp-v3
```

- 串口/相机已经可读写时直接通过，不再调用 `sudo`；只有实际无权限时才请求一次授权。
- 构建完成后检查以下四个包可见，并核对三个关键插件动态库真实存在：
  `nav2_smac_planner`、`nav2_rotation_shim_controller`、`short_goal_bt`、`lidar_py`。

#### `open_all_log.sh` 最终保存

- 第一次 `Ctrl+C`：
  1. 停止 frontier 和网页导航目标；
  2. 先发送零速 MOVE，再关闭后续串口运动输出；
  3. 先保存 `result.pbstream`；
  4. 再用 Foxy 毫秒单位的 `save_map_timeout:=20000` 保存 `final_map.pgm/yaml`；
  5. 校验三个文件存在且非空后，再关闭所有进程组。
- 保存过程会持续打印 `[save]` 状态。保存期间再次按 `Ctrl+C` 才会放弃保存并强制退出。
- `SIGHUP` 和 `SIGTERM` 也走同一有序退出入口；`SIGKILL` 仍无法保存，这是操作系统行为。
- 映射 launch 进程组状态异常时不再直接静默返回，而是继续检查 `/write_state` 服务，并输出
  明确原因。
- `open_all.sh` 仍按设计不保存最终 PBStream/PGM/YAML，仅 `open_all_log.sh` 保存。

#### Ubuntu 验证

```bash
cd ~/测试9
chmod +x open_all.sh open_all_log.sh validate_auto_mapping_foxy.sh

# 先完成语法、依赖、编译、插件库和配置检查
./validate_auto_mapping_foxy.sh

# 启动保存版
./open_all_log.sh
```

正常结束时只按一次 `Ctrl+C`，等待出现：

```text
[save] PBStream: .../Final/result.pbstream
[save] Final map: .../Final/final_map.pgm + .../Final/final_map.yaml
[save] Final-save sequence completed successfully.
[stop] Shutdown complete.
```

文件位置：

```text
SLAM_Log/open_all_log_日期时间/Final/result.pbstream
SLAM_Log/open_all_log_日期时间/Final/final_map.pgm
SLAM_Log/open_all_log_日期时间/Final/final_map.yaml
```

#### 静态验证

- `open_all.sh`、`open_all_log.sh`、`validate_auto_mapping_foxy.sh` 均通过 `bash -n`。
- `lidar_py/setup.py` 通过 Python 语法检查。
- 已增加验证脚本断言，防止上述 Foxy API、占位地图和浮点超时参数再次回退。
- Windows 当前没有 ROS2 Foxy，C++ 最终编译和真实 `/write_state`、`map_saver_cli` 保存仍需
  在 Ubuntu 执行上述验证。
- 定版 `cartographer_2d_v9_tightened.lua` 未修改，SHA256 仍为
  `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。
- 当前 Windows 环境没有 ROS2 Foxy 和实车串口，进程组退出、地图服务和
  PBStream 写入仍需在 Ubuntu 实机完成一次启动/退出验收。

### 11.50 最新修改标签：WEB_AUTO_MAPPING_TOGGLE_2026-07-16

#### 网页自动建图开关

- 在地图面板 `开始导航` 左侧新增 `自动建图 OFF/ON` 按钮。
- 页面初始状态和机器人端 `frontier_explorer_foxy` 启动状态均默认为关闭。
- 点击开启时通过 `/robot/web_control` 发送 `auto_mapping_start`；再次点击发送
  `auto_mapping_stop`。
- 按钮在等待机器人回执时显示 `自动建图切换中`，3.5 秒未收到状态会解除等待并提示检查
  `frontier_explorer_foxy`。

#### 状态同步与目标仲裁

- 网页新增订阅 `/auto_mapping/status`，以机器人端状态为准显示开关，不使用浏览器本地状态
  猜测自动建图是否开启。
- 多个局域网设备打开网页时会收到同一份自动建图状态，一个设备切换后其他设备同步更新。
- 手动点击 `开始导航` 时继续沿用现有优先级：先暂停 frontier 自动建图并取消其 Nav2 目标，
  再发送网页确认的目标，避免两个 `NavigateToPose` 目标源冲突。
- 自动建图只负责选择 Nav2 探索目标，不直接发布轮速；运动仍走
  `Nav2 -> safety_fusion_node -> chassis_node -> STM32`。

#### 前端文件与验证

- 修改 `web/index.html`、`src/main.js`、`src/style.css` 和 ROS
  topics/publishers/subscribers 模块。
- 三个地图操作按钮在窄屏下允许自适应换行，不覆盖地图或其他控件。
- `npm run build` 已通过并更新 `web/dist`；相关 JavaScript 文件通过 `node --check`。
- Cartographer、Nav2、视觉避障、串口协议和定版 Lua 参数均未修改。

### 11.51 最新修改标签：FULL_CHAIN_AUDIT_AND_ROOT_README_2026-07-16

#### 全链路重新审查

- 按 `一键启动 -> 两路串口 -> 激光时间戳/滤波 -> NAVI odom/IMU -> Cartographer/TF
  -> Nav2/Frontier/网页目标 -> 深度避障 -> 速度融合 -> AA55 下行 -> 网页/日志/退出保存`
  重新核对当前实际运行链。
- 定版 `cartographer_2d_v9_tightened.lua` 和 State Lattice、Rotation Shim、DWB、行为树
  参数未修改；Lua SHA256 仍为
  `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。

#### 明确问题修复

- 网页顶部软件急停原来发布 `/robot/emergency_stop`，但 ROS 端没有实际接收方。
  现在由 `chassis_node` 保存软件急停状态并周期发布
  `/robot/emergency_stop_state`，`safety_fusion_node` 以此持续输出零速度。
- 软件急停同时写入 `/robot/control_state`，多个局域网网页会同步显示和解除状态。
- 软件急停同时取消正在执行的网页 Nav2 目标并关闭 Frontier 自动探索；解除急停后不会
  自动恢复旧目标，必须由操作者重新确认导航或重新开启自动建图。
- 网页目标和 Frontier 现在还会直接订阅 `/robot/emergency_stop_state`，即使急停由 ROS
  命令行或其他客户端触发、没有同时发送网页 JSON，也能取消自主运动任务。
- 根文档的 `15,000,000 cnt/s` 二进制示例经过 `struct.pack('<i')` 复算，负数小端字节
  修正为 `40 1E 1B FF`；完整帧校验仍为 `0xFA`。
- 独立的 `ESTOP 0x03` 仍是 STM32 锁存急停，不能被网页“解除急停”按钮伪装解除。
- “清除标记”原来会让 `web_goal_nav_node` 取消正在执行的 Nav2 目标；现在只清除目标标记
  和预览路径。
- 修正 `chassis_node.py` 中 IMU 上行帧 `21B -> 23B` 的错误说明。
- 修正 STM32 协议头文件中编码器上行帧 `34B -> 35B` 的错误说明。
- `auto_map_saver.py` 的 Foxy `save_map_timeout` 使用整数 `20000`（20 秒）。
- `lidar_py/package.xml` 补充 `cartographer_ros_msgs` 运行依赖，并更新包描述。
- `运行环境.txt` 更新为当前 `cartographer_auto_mapping_foxy_launch.py`、正向符号和网络端口。
- `config_switches.py` 的相机后端说明改为当前实车 SDK 主链，ROS2 读取保留为兼容模式。

#### 根目录总文档

- 新增根目录 `ReadMe.md`，完整记录：
  - 项目能力和运行边界；
  - 硬件、ROS、视觉、Nav2、网页和 STM32 的系统拓扑；
  - TF、建图、导航、自动 Frontier、视觉避障和轮速输出链；
  - 根目录和每个主要源码模块的作用；
  - STM32 AA55 下行、NAVI 上行、兼容编码器/IMU 帧和 LD14P 47B 协议；
  - ROS topic/action/service、网页 JSON、HTTP/WebSocket 接口；
  - 两个一键脚本的区别、操作流程、日志结构和快速排查。
- 在系统拓扑图前新增完整图例表，逐项解释 19 个硬件/ROS/网页方框的输入、输出和职责，
  并补充串口、topic、Action、TF、AA55、NAVI、MJPEG 与 ROSBridge 箭头含义。
- 补充 `validate_auto_mapping_foxy.sh` 的六阶段检查内容、不会操作硬件的安全边界、推荐执行
  时机和无需重复执行的场景，明确它是部署前体检而不是每次启动前的强制步骤。
- 文档明确说明 `maps/` 当前不会被在线建图启动链自动加载，深度障碍属于速度层局部避障，
  `ECHO_ON` 会暂停 50 Hz NAVI。

#### 验证

- 所有 Python 文件通过 `compileall`。
- `fixed_scan_grid`、`lidar_timing`、`frontier_core` 共 14 个纯 Python 测试通过。
- 网页 `npm run build` 通过，`web/dist` 已更新。
- package XML、行为树 XML、State Lattice JSON 和根文档关键接口校验通过。
- `validate_auto_mapping_foxy.sh` 增加软件急停链、清标记语义、自动保存整数超时和
  `cartographer_ros_msgs` 依赖断言。
- 当前 Windows 环境没有 ROS2 Foxy 和 Bash，完整 `bash -n`、colcon 编译、ROS graph 和
  实车串口验证仍需在 Ubuntu 执行 `./validate_auto_mapping_foxy.sh`。

### 11.46 最新修改标签：STARTUP_PREFLIGHT_SERIAL_AUTODETECT_CARTOGRAPHER_ONLY_2026-07-16

#### 启动前残留检查

- `open_all.sh` 和复用它的 `open_all_log.sh` 会在启动任何 ROS 节点前，检查并列出旧的
  Cartographer、Nav2、视觉节点、ROSBridge、Vite、RViz、logger、ROS daemon 和历史
  slam_toolbox/RF2O 节点 PID。
- 同时检查 `/dev/ttyACM*`、`/dev/ttyUSB*` 的占用进程，以及 `9090`、`5173`、`8080`
  三个网络端口的占用进程。
- 发现残留后按 `SIGTERM -> SIGKILL` 清理，并对串口设备和三个网络端口执行占用清理。
  清理后会再次复查；仍有资源被占用时拒绝启动，防止两套 ROS/网页/串口程序同时运行。
- `slam_toolbox` 和 `rf2o` 名称只保留在旧进程清理名单中，不代表会启动或编译它们。

#### 串口自动识别与授权

- 串口识别优先读取 sysfs USB VID/PID，并显示 `/dev/serial/by-id` 别名、设备型号和厂商。
- 当前识别规则：
  - USB PID `55d4`：2D LiDAR；
  - USB PID `7523`：STM32/CH340 底盘串口。
- VID/PID 无法识别时，按实测连接回退：
  - 优先把 `/dev/ttyACM0` 作为雷达；
  - 双 USB 串口场景优先 `/dev/ttyUSB1` 为雷达、`/dev/ttyUSB0` 为 STM32。
- 仍可显式覆盖自动识别：

```bash
LIDAR_PORT=/dev/ttyUSB1 CHASSIS_PORT=/dev/ttyUSB0 ./open_all.sh
```

- 如果雷达和 STM32 被识别成同一个设备，脚本会立即报错停止。
- 清理完成后、ROS 启动前，脚本会自动为雷达、STM32 和存在的相机设备授予读写权限，
  并打印最终权限。普通 `chmod` 无权时自动调用 `sudo chmod 666`。

#### 唯一 SLAM 启动链

- 两个一键脚本现在只显示并启动 `Cartographer V13 only`。
- 原来的 `wheel + slam_toolbox`、纯 RF2O、`wheel + RF2O` 融合三个启动模式已经从一键
  构建和启动路径中移除。
- 历史源码仍保留作故障追溯和备份，避免误删影响回退；普通启动不会构建或运行这些历史模式。
- 定版 `cartographer_2d_v9_tightened.lua` 未修改，SHA256 仍为
  `00DFD1C721F0FE8C61AC6F2B417001920694E4FC77E895FB4A1F194330C910D9`。

### 11.52 最新修改标签：FOXY_NAV_ACTION_AND_FINAL_SAVE_RECOVERY_2026-07-17

#### 本次 Ubuntu 日志的根因

- 网页选点和自动建图命令已经正常发布；真正的首次错误是
  `planner_server: Original error: Could not open lattice file!`。
- lifecycle manager 因此中止 Nav2 bringup，`/navigate_to_pose` 从未建立；网页目标节点持续打印
  `Waiting for Nav2 navigate_to_pose action server`，所以两个按钮表面上都没有反应。
- 原启动检查只看 `/planner_server` 等节点名是否存在。lifecycle 节点即使配置失败仍会出现在
  `ros2 node list`，导致终端错误显示 `[ready]`。
- Ctrl+C 后出现的 `planner_server SIGSEGV` 是配置失败对象在销毁阶段触发的次生崩溃，不是
  网页按钮无响应的第一原因。
- Final 保存同时存在两处 Foxy 兼容性错误：`WriteState` 没有
  `include_unfinished_submaps` 字段；`map_saver_cli` 的整数超时单位是毫秒，原值 `15` 实际
  只有 15 ms，所以约 0.1 秒就超时。

#### Nav2 启动修复

- `cartographer_auto_mapping_foxy_launch.py` 在创建 planner 前解析并校验 State Lattice JSON。
- 校验后的 JSON 复制到 `/tmp/car_nav2_<uid>/lattice_forward_turnaround_5cm.json`，再把这个
  已解析、纯 ASCII 的绝对路径直接传给 `GridBased.lattice_filepath`。
- 删除 Nav2 YAML 中会抢先提供空值的 `lattice_filepath: ""`。
- C++ State Lattice 打开失败时会打印完整实际路径，后续排查不再只有笼统报错。
- `open_all.sh` 现在要求 controller、planner、recoveries、BT navigator 全部达到
  `active [3]`，并确认 `/navigate_to_pose` 出现后才把整套系统标记为启动成功。
- Nav2 未 active 时启动会明确失败并清理进程，不会继续启动网页后给出“可以导航”的假象。

#### Final 保存修复

- `/write_state` 请求改为 Foxy 支持的 `{filename: '.../result.pbstream'}`。
- `map_saver_cli` 改为 `save_map_timeout:=20000`，即 20 秒；外层仍有 35 秒总超时保护。
- 可选 `auto_map_saver.py` 同步修正这两个问题，避免以后启用自动保存时再次失败。
- `open_all.sh` 仍不保存 Final；只有 `open_all_log.sh` 在一次 Ctrl+C 后保存
  `final_map.pgm`、`final_map.yaml`、`result.pbstream`。

#### 未改动与验证边界

- 未修改定版 Cartographer Lua、Nav2 实车权重、车体尺寸、速度、视觉避障和网页 UI。
- Windows 已通过 Python 语法、State Lattice JSON、YAML 关键不变量和 `git diff --check`。
- Windows 没有 ROS2 Foxy/Bash，最终 colcon 编译、lifecycle active、Action 可用和三个 Final
  文件仍需在 Ubuntu 执行 `./validate_auto_mapping_foxy.sh` 与 `./open_all_log.sh` 实测。

### 11.53 最新修改标签：WEB_AUTONOMY_HANDSHAKE_AND_SLAM_BASELINE_GUARD_2026-07-17

#### 本次日志确认的根因

- Nav2 已经接受网页目标并持续输出非零 `/cmd_vel_nav`，但网页当时处于 `PS2 控制`，且
  `motion_serial_enabled=false`。`chassis_node` 明确打印
  `TX_FRAME blocked by web runtime option`，所以“有路径但车不动”发生在 STM32 发送前，
  不是全局规划器没有计算路径。
- `web_path_preview_node` 使用了 Foxy 不存在的 `goal_msg.goal`，点击地图后抛出
  `AttributeError` 并退出。Foxy `ComputePathToPose.Goal` 的目标字段实际为 `pose`。
- Final 三个文件已经保存成功；随后 `controller_server`、`planner_server`、
  `recoveries_server` 在整组同时收到 SIGINT 时触发 Foxy/CycloneDDS 析构崩溃和 Ubuntu
  Apport 弹窗。
- 10:50 的放射状地图截图发生在 `3.txt` 记录结束之后，不能用该日志伪造归因。定版
  Cartographer Lua 与测试8文件 SHA256 完全一致，扫描网格和激光滤波文件也一致。

#### 修复

- 路径预览按消息定义自动选择 `pose`/`goal` 字段，Foxy 使用 `pose`；未知定义只禁用预览，
  不再杀死节点。
- “开始导航”和“自动建图 ON”增加统一自主控制握手：检查 ROSBridge、Baseline 和软件急停，
  自动关闭回响并发送零速 `MOVE 0x01` 接管，等待上位机确认帧已写入且
  `/robot/control_state` 确认 MOVE 与运动串口均开启后，才发送导航/Frontier 命令。
  仅地图单击仍只预览，不接管 PS2。
- `chassis_node` 只有在 MOVE 帧实际写入串口成功后才更新控制状态；串口关闭或写失败时保持
  自主输出禁用，避免网页显示假接管。这里的确认是“上位机已成功写串口”，回响关闭时并不
  冒充 STM32 应答；网页 3 秒收不到状态会中止，导航目标增加 800 ms 防重复点击窗口。
- 启动器锁定定版 Lua、扫描 launch、激光滤波三个 SHA256；任一文件变化立即停止启动。
- 启动完成前检查 `/chassis_node`、`/lidar_node`、Cartographer 和四个 Nav2 核心节点名称唯一，
  并检查 `/odom`、`/imu_cartographer`、`/scan_timed_v2_filtered` 都只有一个发布者，防止旧进程
  或同域第二套 ROS 数据污染 TF 与地图。
- Ctrl+C 在 Final 保存完成后先调用 Nav2 lifecycle manager 的 `SHUTDOWN=4`，清理插件和 DDS
  资源后才向进程组发 SIGINT，降低 Foxy 退出阶段 SIGSEGV/Apport 弹窗概率。

#### 未修改与验证

- 未修改 `cartographer_2d_v9_tightened.lua`、激光滤波参数、固定角度网格、NAVI 里程计、
  Nav2 实车权重、车体尺寸、速度和视觉避障参数。
- Windows 已通过网页生产构建、三个 Python 节点语法编译、Git Bash 的两个启动脚本
  `bash -n` 和 `git diff --check`。ROS graph、lifecycle 有序退出及实车运动仍需 Ubuntu 验证。
### 11.54 已撤销：NAVI_YAW_DROPOUT_CONTINUITY_GUARD_2026-07-17

- 该版本增加的绝对 yaw 动态重定基准保护已在 11.56 完整撤销，禁止继续部署。
- 撤销原因：保护在实车原地旋转期间反复触发并累计改变 `/odom` 航向参考，破坏了测试8/V12
  已验证的绝对 yaw 解包链。历史问题判断保留在 Git 记录中，不再代表当前实现。

### 11.55 最新修改标签：LAUNCHER_RVIZ_NAVI_GUI_OPTIONS_AND_README_2026-07-17

- `open_all.sh` 保持 `USE_RVIZ=true` 默认值，并在启动摘要中新增 `RViz: true/false`，显示最终
  传给 ROS launch 的实际值。当前终端若曾 `export USE_RVIZ=false`，继承值仍会覆盖源码默认值。
- 新增 `SHOW_NAVI_GUI=false` 环境变量，替代原先写死的 `show_serial_window:=false`；需要调试
  下位机 NAVI 窗口时可使用 `SHOW_NAVI_GUI=true ./open_all_log.sh`。
- 两个显示开关在传入 launch 前统一归一化为 `true` 或 `false`，启动摘要与实际行为一致。
- `ReadMe.md` 补充一键脚本的界面、运行、日志、串口、目录和两个启动配置的完整表格，以及
  `unset USE_RVIZ SHOW_NAVI_GUI`、`DISPLAY`、`pgrep -af rviz2` 排查步骤。
- 最新 `E:\3.txt` 中没有任何 `rviz2` 进程启动记录，因此当次不是 RViz 启动后崩溃，而是
  launch 条件未启用；旧摘要未显示 RViz 实际值，无法进一步从该日志区分继承环境变量来源。
- `open_all.sh`、`open_all_log.sh` 已通过 Git Bash `bash -n`，`git diff --check` 通过。

### 11.56 最新修改标签：REVERT_YAW_CONTINUITY_GUARD_TO_V12_BASELINE_2026-07-17

- 最新 `E:\3.txt` 显示直行阶段 `yaw_jump_fixes=0`，原地旋转开始后保护连续触发 7 次。
  每次触发都会修改传感器对 `/odom` 的对齐偏置，累计后 `/odom` 与原始 yaw 相差约 `94°`，
  与网页地图原地旋转后放射状发散的时间完全一致。
- 完整撤销提交 `4a65c60` 的运行改动：`chassis_node.py` 恢复原 `_unwrap_navi_yaw()` 绝对角
  解包；删除 `yaw_continuity.py`、对应测试和验证脚本断言；ReadMe 删除该保护的操作说明。
- 已逐项对照测试8/V12稳定版：撤销后的 yaw、NAVI 时间戳、odom 和 IMU 处理与稳定链一致。
- 未修改 STM32；未修改 Cartographer V13 Lua、激光滤波、固定扫描网格、雷达外参、yaw/vz
  符号、Nav2、视觉避障和网页。11.55 的 RViz/NAVI GUI 启动选项继续保留。

### 11.57 最新修改标签：FOXY_RVIZ_LAUNCH_SCOPE_COLLISION_FIX_2026-07-17

- 最新 `E:\3.txt` 的启动摘要明确显示 `RViz: true`，但 launch 只创建了 15 个节点，没有
  `rviz2`，也没有任何 Qt/`DISPLAY` 崩溃信息，因此问题发生在进程创建条件之前。
- 根因是外层自动建图 launch 和被包含的稳定建图 launch 共用 `use_rviz` 键。内层为了避免
  重复窗口传入 `use_rviz=false`，在 Foxy launch 上下文中覆盖了外层同名条件。
- 外层参数改名为独立的 `launch_rviz`；内层继续保持 `use_rviz=false`，只由外层创建一个
  使用 `nav2_display.rviz` 的 RViz，不会重复启动。
- `open_all.sh` 改传 `launch_rviz:=$USE_RVIZ`，并在请求 RViz 时等待 `/rviz2` 节点；未出现就
  明确失败，不再输出整套系统已成功启动的假状态。
- 验证脚本增加参数隔离、传参和启动健康检查三项静态断言，ReadMe 同步说明。

### 11.58 最新修改标签：INTERRUPTIBLE_HARD_DEADLINE_SHUTDOWN_2026-07-17

- 最新 `E:\3.txt` 在第一次 Ctrl+C 后只打印 `[stop] Stopping...`，没有进入任何 `[save]` 日志，
  证明卡点位于 `prepare_safe_shutdown()` 的 ROS2 topic 发布阶段，而不是 PBStream/地图保存。
- 原 `timeout 4s` 只发送 TERM；ROS2/CycloneDDS 清理阶段若不响应 TERM，`timeout` 也会一直
  等待。Bash 正等待该前台命令时还会延迟后续 INT trap，造成连续 Ctrl+C 无效。
- 新增 `hard_timeout()`，所有退出阶段 ROS2 发布、服务、地图保存、lifecycle 和 daemon 命令
  都使用 `timeout --kill-after=1s`，超过期限后必定发送 KILL。
- 停车、Final 保存和 Nav2 lifecycle 三个阶段改为后台可中断任务。主脚本通过 `wait` 等待，
  第二次 Ctrl+C 可立即进入 `force_shutdown()`，递归杀死当前阶段及其子进程，再清理全部已登记
  进程组、串口占用和网页端口。
- 退出日志增加阶段进度和“再次 Ctrl+C 强制退出”提示；验证脚本增加硬超时与可中断阶段断言。

### 11.59 最新修改标签：REALTIME_NAVI_BACKPRESSURE_AND_FULL_RUNTIME_LOG_FIX_2026-07-17

#### 实车日志结论

- `open_all_log_2026-07-17_14-41-12` 的最终 PGM/YAML/PBStream 均已正常保存，故本次不是
  退出保存故障。
- 雷达在地图停止更新期间仍稳定产生约 `6.03 Hz` 固定网格扫描，设备延迟仅
  `4–9 ms`，不是雷达串口断流。
- 底盘节点同一时段出现 `serial_debug` 最高约 `1927 ms`、`/imu_cartographer`
  约 `1679 ms`、`/odom` 约 `1309 ms` 的发布阻塞；原本 1 秒的统计窗口被拉长到
  `6–17 s`。
- 串口因此积压，Cartographer 收到了约 78 秒前的 odom/IMU 时间戳，出现超过
  4000 次 `extrapolation into the past`；恢复后以 `302–394% real time` 高速追帧。
  这与“地图长时间不动，回房间时突然刷新并把走廊匹配歪”完全对齐。

#### 实时链修复

- STM32 真实 NAVI 解析、`/odom`、TF 和串口运动帧频率保持不变。
- 仅将网页/日志用的高频 NAVI 与连续运动 TX 调试 JSON 限制为 `10 Hz`；
  STOP、ESTOP、PS2、MOVE 接管、回响等一次性控制事件仍每帧立即显示。
- `/imu_cartographer` 改用 Cartographer 官方传感器订阅匹配的
  `BEST_EFFORT + KEEP_LAST(5)` QoS，避免传感器流被可靠 DDS 队列反压。
- 当前一键链不使用的重复 `/imu` 兼容输出默认关闭；
  `/imu_cartographer` 依旧保持 50 Hz，不改变 Cartographer 的 IMU 输入。

#### 完整运行日志

- `open_all.sh` 和 `open_all_log.sh` 现在都将底盘、雷达、Cartographer、Nav2 和 RViz 的
  完整输出直接写入本次会话的 `runtime_stack.log`。
- ROS 栈不再向终端滚动区高频写屏，避免终端渲染或缓冲对实时进程施加反压；
  日志也不再因终端行数上限丢掉卡顿前半段。
- 启动健康检查失败时，脚本自动打印 `runtime_stack.log` 最后 80 行。

#### 严格保留的定版基线

- 未修改 `cartographer_2d_v9_tightened.lua`、`cartographer_scan_v2_launch.py`、
  `laser_filter.yaml`、雷达外参、扫描网格、Nav2 权重、车体尺寸、速度、视觉避障和 STM32 代码。

### 11.60 最新修改标签：NAV2_DEPTH_COSTMAP_AND_STATEFUL_FUSION_2026-07-17

#### 根因与协作结构

- 原链路只在 Nav2 输出之后修改 `/cmd_vel_nav`，低矮障碍没有进入 Nav2 costmap。Nav2 因此
  持续要求沿原路线前进，视觉层又持续停车/转向，两个控制器互相纠正，表现为抽搐、走一下
  停一下或原地掉头。
- 原融合逻辑还会把很小的 Nav2 线速度强行抬高到固定 `avoid_v_min/danger_v_min`，在 Nav2
  主动减速时造成速度跳变；低线速度下的“最小前进轮速”角速度截断又会把 DWB 转向压到接近
  零，进一步触发无进展和恢复动作。
- 现在由 Nav2 继续负责方向、路径和恢复，视觉同时提供 costmap 障碍与末端安全修正，底盘仍
  只订阅唯一 `/cmd_vel_safe`，没有增加第二条串口运动链。

#### 新的状态化融合控制

- 新增纯 Python `fusion_control.py`，将避障状态、风险滤波、方向锁定、速度缩放和虚拟扫描生成
  与 ROS 节点分离，便于回归测试。
- 障碍升级立即生效，降级/消失保持 `0.40 s` 后再退出；同一避障事件锁定方向，只有左右分数
  差超过 `80` 且连续 `6` 帧支持反向时才切换。
- 视觉减速只做乘法缩放，输出永远不高于 Nav2/网页原始速度，不再使用固定最低避障速度。
- 仅在源命令确实向前时施加视觉转向。Nav2 停车、倒车、原地对正和恢复旋转时完全保留 Nav2
  角速度，视觉不再自行等待后原地掉头。
- Nav2 已向安全侧转弯时，视觉只补足所需最小曲率；若 Nav2 仍朝障碍侧转弯，则平滑过渡到
  锁定的安全方向，避免两个角速度简单相加后反复换向。
- 极近障碍或左右通道同时堵塞时输出稳定零速，等待 Nav2 的 costmap 重规划；移除会在低速时
  把 DWB 角速度钳成零的 `min_forward_wheel_mps` 后处理。

#### 深度障碍进入 Nav2

- `safety_fusion_node` 新增 30 Hz `/depth_obstacle_scan`，在 `base_link` 前方约 120 度范围内
  将中/左/右 ROI 障碍转换为粗粒度 `LaserScan`。
- 相机深度是相对相机光心的距离，虚拟扫描增加 `virtual_scan_origin_x_m=0.18` 前向补偿，
  防止近障碍被错误标到 66.5 cm 车体 footprint 内；后续可直接替换为实测安装距离。
- Nav2 局部和全局 obstacle layer 新增 `depth_virtual` source，物理 `/scan` 在同一更新中排在
  深度清除射线之后，可重新标记真实墙体；深度数据超时后虚拟扫描自动清空。
- `/depth_obstacle_scan` 只供 Nav2 使用，不连接 Cartographer，不修改已定版 Lua、建图扫描、
  激光滤波、NAVI odom/IMU、STM32 协议或网页目标逻辑。

#### 验证与实车观察点

- 新增 6 个纯控制测试，覆盖低速不抬升、停止不自转、恢复旋转不被反向覆盖、方向锁定、状态
  退出滞回和虚拟扫描扇区；Windows 手动执行全部通过，相关 Python 文件通过 `py_compile`。
- `validate_auto_mapping_foxy.sh` 新增融合核心与两个 costmap 深度 source 的部署断言。
- Ubuntu 实车重点观察：`FUSION_STATUS` 应平稳经历 `clear -> caution/avoid -> clear`；原地对正时
  `safe.angular.z` 与 Nav2 同向；出现低障碍时 `/depth_obstacle_scan` 有有限距离点，消失后恢复
  `inf`。当前 Windows 没有 ROS2 Foxy 和实车，仍需 Ubuntu `colcon` 与实车低速验收。

### 11.61 最新修改标签：CAMERA_OFFSET_AND_NAV_MAPPING_GEAR2_2026-07-17

- 根据实测值，将相机光心到车体中心的前向距离从临时 `0.18 m` 更新为 `0.30 m`；虚拟深度
  扫描最大距离同步从 `1.20 m` 扩到 `1.30 m`，完整容纳 `0.95 m` 视觉远区加安装偏移。
- Nav2 两套 DWB 控制器的直行上限统一为 `0.20 m/s`，Rotation Shim、DWB 和恢复旋转上限
  统一为建图二档 `0.209 rad/s`（12 度/秒）。
- 移动圆弧继续由融合层限制曲线外轮不超过 `0.16 m/s`；网页建图二档原有
  `0.12 m/s + 0.142 rad/s` 对应内轮 `0.08 m/s`、外轮 `0.16 m/s`，无需重复修改网页。
- `validate_auto_mapping_foxy.sh` 增加 Nav2 二档直行、DWB 转向和 Rotation Shim 转速断言。
- 未修改 Cartographer、激光滤波、NAVI odom/IMU、STM32 和视觉距离阈值。

### 11.62 最新修改标签：UBUNTU24_JAZZY_NATIVE_NAV2_FRONTIER_2026-07-19

#### 系统迁移

- 目标系统从 Ubuntu 20.04 + ROS 2 Foxy 更新为 Ubuntu 24.04 + ROS 2 Jazzy。
- `open_all.sh`、`open_all_log.sh` 和 `运行环境.txt` 统一 source `/opt/ros/jazzy`，固定
  `NAV_PROFILE=jazzy_native`，继续使用 CycloneDDS 和 `ROS_DOMAIN_ID=88`。
- 旧 `nav2_smac_planner_foxy`、`nav2_rotation_shim_controller_foxy`、
  `short_goal_bt_foxy`、Foxy launch/config/BT、Python Frontier 回移植和 Foxy 验证脚本均删除，
  防止 Jazzy 系统插件被同名回移植库覆盖。

#### 直接接入 auto_mapping_v1

- 将 `auto_mapping_v1/frontier_exploration_ros2` 原生 C++ 包完整纳入 ROS 工作空间，直接使用其
  Frontier 搜索、全局/局部 costmap 校验、候选排序、失败区域抑制、Nav2 目标管理和完成判断。
- 将原生 `short_goal_bt` 和 BT.CPP v4 行为树纳入工作空间，保留短目标预旋转、旋转空间检查、
  无 Shim DWB、倒车脱困、路径有效性低频重规划和受控恢复逻辑。
- 新增 `frontier_web_bridge.py`，把已有 `/robot/web_control`、`/auto_mapping/status` 和
  `/auto_mapping/set_enabled` 接口转换为原生 `/control_exploration` 服务；人工网页目标仍先停
  Frontier，再提交 `NavigateToPose`，避免两个客户端抢占同一个 Action。

#### Jazzy 原生 Nav2

- 新增 `cartographer_auto_mapping_jazzy_launch.py`，启动 Jazzy 原生 controller、smoother、
  planner、behavior、BT navigator、waypoint follower 和 lifecycle manager。
- 新增 `nav2_auto_mapping_jazzy.yaml`：使用系统 `SmacPlannerLattice`、
  `RotationShimController`、DWB、`ConstrainedSmoother` 和 `nav2_behaviors`，不再维护 Foxy
  兼容分支。
- 直接接入 `auto_mapping_v1` 的 State Lattice JSON 和行为树；所有硬编码路径改为 launch
  动态注入。Lattice 启动前解析校验并复制到 `/tmp/car_nav2_jazzy_<uid>/`。
- 保留本车实测约束：0.666 m 方形保守 footprint、二档 `0.20 m/s` 与 `0.209 rad/s`、相机到
  车体中心前向距离 0.30 m、视觉虚拟扫描和统一 `/cmd_vel_nav -> /cmd_vel_safe` 安全链。

#### 构建、启动和文档

- Jazzy rosidl/CMake 可能不接受中文物理构建路径；一键脚本通过
  `~/.cache/huichuan_agv_jazzy_ws/src` 的 ASCII 软链接编译，但源码仍保存在项目目录。
- 新增 `validate_auto_mapping_jazzy.sh`：支持 `--check-only` 和 `--build`，检查 Jazzy 包、
  YAML、State Lattice JSON、BT.CPP v4、Python 语法及冻结建图文件哈希，不操作硬件。
- 启动摘要、旧进程清理、lifecycle/Action/Frontier 服务健康检查、RViz、NAVI GUI、网页日志、
  普通版不保存 Final、Log 版保存 PGM/YAML/PBStream 的职责继续保留。
- `ReadMe.md`、`CAR_使用教程_注意事项.md`、网页页脚、错误提示和 GitHub 上传规定更新为 Jazzy。

#### 冻结建图基线

本次没有修改定版 Cartographer 链，迁移前后 SHA-256 保持一致：

- `cartographer_2d_v9_tightened.lua`：
  `00dfd1c721f0fe8c61ac6f2b417001920694e4fc77e895fb4a1f194330c910d9`
- `cartographer_scan_v2_launch.py`：
  `8f341ed30e435f144d0178fac326836fb99244b2c3c5057966b58f527e029e10`
- `laser_filter.yaml`：
  `8583a2ca7e99a29b13f2fc339df468e621562d61f0adfa1e7e1828254705b306`

Windows 侧只能完成静态、网页和配置验证；Jazzy C++ 编译、lifecycle、Action、原生 Frontier
服务和实车运动必须先在 Ubuntu 执行 `./validate_auto_mapping_jazzy.sh --build`，再按教程低速验收。

### 11.63 最新修改标签：JAZZY_SYSTEM_PYTHON_BUILD_GUARD_2026-07-19

#### `3.txt` 结论

- 首次执行 `open_all_log.sh` 在预检阶段发现缺少 `ros-jazzy-rmw-cyclonedds-cpp` 后主动退出；
  当次没有启动任何 ROS 节点，因此不生成 Final 文件是正确行为，不是退出保存故障。
- 后续手动编译曾被 `uv` 的 `~/.local/bin/python3.11` 抢占，而 Ubuntu 24.04/Jazzy 的
  `empy`、`lark` 和 ROS Python 模块属于系统 Python 3.12，造成 `rosidl` 生成失败。
- 在中文物理路径中手动执行 `colcon build` 还会重新触发 Jazzy `rosidl` 路径编码问题；三个
  自定义包通过 ASCII 缓存工作空间已经成功编译，源码本身不是该错误的根因。

#### 修正

- `open_all.sh` 和 `validate_auto_mapping_jazzy.sh` 默认固定使用 `/usr/bin/python3`，colcon 的
  CMake 参数显式传入 `Python3_EXECUTABLE`，构建期间设置 `PYTHONNOUSERSITE=1`。
- 预检新增 `python3-empy`、`python3-lark`、`python3-yaml` apt 包和 `em/lark/yaml` 实际导入
  检查，缺失时直接给出完整安装命令。
- 保留用户自己的 `uv`/Conda 环境，不再依赖删除 `~/.local/bin/python3` 软链接。
- `ReadMe.md`、`CAR_使用教程_注意事项.md` 和 `运行环境.txt` 已补充系统 Python 策略、中文
  路径限制和正确构建入口。

#### 未改动

- 未修改 Cartographer V13 Lua、扫描 launch、激光滤波、雷达外参、NAVI odom/IMU、Nav2
  参数、视觉避障、网页、串口协议或 STM32 工程。

### 11.64 最新修改标签：JAZZY_STARTUP_RUNTIME_ERROR_VISIBILITY_2026-07-19

- 最新 `3.txt` 已确认 Jazzy 三个自定义包编译成功，串口、ROSBridge、`chassis_node` 和
  `cartographer_node` 均已启动；当前唯一阻断点是 `/controller_server` 在 45 秒内未出现。
- Nav2 子进程的真实异常写在会话 `runtime_stack.log`，但原 `die()` 仅在 `STACK_STARTED=true`
  后打印日志。controller 启动失败发生在该标志置位之前，导致最有价值的插件/参数错误被隐藏。
- `open_all.sh` 现在只要运行日志非空，就会先筛选 `ERROR/FATAL/exception/pluginlib/class_loader`
  等失败行，再打印末尾 100 行；下次同类失败会直接显示在启动终端并继续保留完整日志文件。
- `lidar_py/setup.cfg` 的 `script-dir`、`install-scripts` 更新为 setuptools 支持的
  `script_dir`、`install_scripts`，消除 Jazzy 编译时的弃用警告，不改变安装位置或节点入口。
- 本节未修改任何 Cartographer、Nav2、视觉、底盘、速度或 STM32 行为参数。controller 的具体
  根因必须以 `runtime_stack.log` 第一条异常为准，禁止在缺少该证据时盲改导航权重。

### 11.65 最新修改标签：JAZZY_FULL_CHAIN_LIFECYCLE_AUDIT_2026-07-19

#### 审查结论与修正

- Jazzy 官方 `controller_server` 参数接口使用字符串数组 `progress_checker_plugins`；原配置沿用
  对方工程中的单数 `progress_checker_plugin`，该字段会被 Jazzy 忽略。现已改成
  `progress_checker_plugins: ["progress_checker"]`，SimpleProgressChecker 的实际参数保持不变。
- launch 中所有 Nav2 节点和 lifecycle manager 的 `use_sim_time` 都通过
  `ParameterValue(..., value_type=bool)` 强制按布尔值传入，避免 launch 字符串在 Jazzy 参数声明
  阶段形成类型歧义。
- `lidar_py/package.xml` 和一键启动依赖检查补充 `dwb_critics`、`dwb_plugins`；项目不仅加载
  `dwb_core::DWBLocalPlanner`，还直接使用其 trajectory generator 和九个 critics，因此三者必须
  同时存在。包清单同时补齐启动脚本直接使用的 `rosbridge_server` 与
  `rmw_cyclonedds_cpp`，让 `rosdep install` 能完整安装运行依赖。
- `validate_auto_mapping_jazzy.sh --build` 不再止于 colcon 成功。编译后会在隔离的 ROS domain
  中启动 controller、smoother、planner、behavior、BT navigator、waypoint follower，并逐个执行
  lifecycle `configure`。该阶段会真实加载 Rotation Shim、DWB、State Lattice、Constrained
  Smoother、自定义 BT 库及其参数，但不会 activate、不会发布速度或操作硬件。
- 冒烟测试失败会立即打印对应节点的插件/参数异常和日志末尾；只有六个节点全部 configure 成功
  才输出最终 preflight passed，消除“YAML 能解析、C++ 能编译、正式启动却崩溃”的验证盲区。
- 新增根目录 `.github/workflows/jazzy-preflight.yml`，在 Ubuntu 24.04 + ROS 2 Jazzy runner 上
  自动安装 rosdep 依赖、执行完整 preflight，并用 Node 24 完成网页生产构建。该 CI 不接硬件，
  用于在推送阶段拦截系统迁移和插件加载回归。

#### 已完成的本机验证

- `bash -n`：`open_all.sh`、`open_all_log.sh`、`validate_auto_mapping_jazzy.sh` 全部通过。
- Python `compileall`：LiDAR/底盘/融合/网页桥、launch、视觉避障和 logger 全部通过。
- 雷达固定栅格、时间戳及视觉融合控制测试共 16 项全部通过。
- 网页 `npm run build` 通过，Vite 完成 1829 个模块的生产构建。
- Nav2 YAML、Frontier YAML、128 条 State Lattice 原语和两份 BT.CPP v4 XML 均可解析；所有行为树
  内置/自定义节点已对照 Jazzy 源码注册名。
- 三份定版建图文件 SHA-256 与冻结值完全一致；未修改 Cartographer Lua、扫描 launch、
  `laser_filter.yaml`、NAVI odom/IMU、视觉阈值、车体尺寸、速度、AA55 协议或 STM32。

#### Ubuntu 验证边界

Windows 没有 ROS 2 Jazzy 运行时，无法代替目标机执行 pluginlib/lifecycle 动态加载。Ubuntu 拉取
本提交后必须运行 `./validate_auto_mapping_jazzy.sh --build`；只有看到六个 lifecycle configure
逐项成功和最终 `Jazzy migration preflight passed.`，才继续运行 `./open_all_log.sh`。

#### 首次 Jazzy CI 回查修正

- 首次 GitHub Jazzy runner 已确认三个工作空间包全部编译成功，controller server 与 local costmap
  能正常创建；失败点是验证器在节点刚出现在 ROS graph、lifecycle 服务尚未就绪时过早发送
  `configure`，不是 Cartographer 或导航插件崩溃。
- 冒烟测试现改为等待 `ros2 lifecycle get` 成功后再切换状态，并让 ROS CLI 与全部服务统一使用
  隔离 domain 下的 CycloneDDS；测试前停止可能属于其他 domain 的 ros2 daemon，消除发现竞态。
- 修正后的第二轮 GitHub Actions 已在 Ubuntu 24.04 + ROS 2 Jazzy 上完整通过：三包 colcon
  编译、六个 Nav2 lifecycle configure、网页 `npm ci` 与 Vite 生产构建均成功，总耗时约 6 分
  53 秒。对应代码提交为 `6b7fb2e`，Actions run 为 `29673808502`。

### 11.66 最新修改标签：JAZZY_CARTOGRAPHER_OCCUPANCY_EXECUTABLE_FIX_2026-07-19

#### `3.txt` 根因

- Ubuntu 端已经完成 Git 拉取、三个自定义包编译、串口识别、ROSBridge、底盘节点和
  `cartographer_node` 启动；本次并不是 Git、编译、串口或 SLAM 参数错误。
- 正式 launch 随后报错：`executable 'occupancy_grid_node' not found`。ROS 2 Jazzy 的
  `cartographer_ros` 安装入口名是 `cartographer_occupancy_grid_node`，旧入口名导致整个 launch
  在 Nav2 启动前退出，因此网页导航和最终地图保存都不会进入可用状态。

#### 修正与防回归

- `cartographer_scan_v2_launch.py` 只把 OccupancyGrid 节点的 `executable` 从
  `occupancy_grid_node` 改为 `cartographer_occupancy_grid_node`；节点名、参数、分辨率、发布周期、
  TF、话题和其余启动链全部不变。
- `validate_auto_mapping_jazzy.sh` 现在会读取 `ros2 pkg executables cartographer_ros`，明确要求
  `cartographer_node` 和 `cartographer_occupancy_grid_node` 两个入口都存在；静态检查也会拒绝旧入口名。
- 扫描 launch 的新 SHA-256 为
  `5650100fbdaf7fd40bdb6cc8dfaa1d642b7fa44aa914028c48817af5be5a9106`，已同步到一键启动器、
  验证器和使用教程。
- 未修改 `cartographer_2d_v9_tightened.lua`、`laser_filter.yaml`、NAVI odom/IMU、Nav2 参数、
  视觉避障、车体尺寸、速度、串口协议或 STM32 工程。

### 11.67 最新修改标签：STAGED_CARTOGRAPHER_NAV2_STARTUP_2026-07-19

#### 现象与根因

- 最新 `3.txt` 中 controller 和 smoother 已经进入 `active`，终端随后等待 planner；同一时刻 RViz
  报 `Fixed Frame [map] does not exist` 和 `No map received`。
- 原启动链让 lifecycle manager 在 Cartographer 第一帧 `/map` 出现前立即自动激活 Nav2。manager
  按 controller、smoother、planner 的顺序处理，而 planner 的 global costmap 依赖 `/map`，因此会
  在第三个节点处等待地图，表现成终端卡住。RViz 报错是同一上游缺图状态的结果，不是 RViz 配置损坏。

#### 修正

- 一键启动显式传入 `nav_autostart:=false`，先启动传感器、底盘、Cartographer、Nav2 进程和 RViz，
  但暂不激活 Nav2 lifecycle。
- 启动器现在先确认 `lidar_node`、`cartographer_occupancy_grid_node` 和 lifecycle manager 唯一存在，
  再逐项等待 `/odom`、`/imu_cartographer`、`/scan_timed_v2_filtered` 和首帧 `/map` 的真实消息。
- 四路输入就绪后，启动器通过官方 `ManageLifecycleNodes` 的 `STARTUP=0` 请求激活 Nav2；调用期间
  每 5 秒打印进度，成功后再逐个检查六个 lifecycle、两个 Action 和 Frontier 服务。
- 任一路话题超时都会打印具体 topic、publisher/QoS 和完整 `runtime_stack.log` 摘要，避免再次只看到
  “卡住”。验证脚本加入了分阶段启动保护断言，防止后续改动把自动激活提前。

#### 未修改

- 未修改 Cartographer Lua、激光滤波参数、雷达/底盘数据算法、Nav2 规划与控制参数、视觉避障、
  速度、车体尺寸、网页逻辑、AA55 协议或 STM32 工程。

### 11.68 最新修改标签：JAZZY_CI_NODE24_TIMEOUT_2026-07-19

- GitHub Actions 第 4 次检查中，ROS 2 Jazzy 编译和全部 Nav2 lifecycle configure 测试已经通过，
  后续 `actions/setup-node` 下载已废弃的 Node 20 时长期停在 0 字节，和 ROS/实车代码无关。
- 网页生产构建环境改为 GitHub runner 当前支持的 Node 24，并给 setup-node 步骤增加 5 分钟独立
  超时，避免网络下载异常时工作流无限占用。
- 未修改 Cartographer、Nav2、视觉避障、网页业务代码、启动顺序、串口协议或 STM32 工程。

### 11.69 最新修改标签：JAZZY_SYSTEM_PYTHONPATH_RUNTIME_FIX_2026-07-19

#### CAR_PROJECT_A1（迁移前目录名为测试10）与仓库版本的真实差异

- 排除 `SLAM_Log`、`__pycache__` 和 `.pytest_cache` 后，当前项目相对提交 `80b9846` 只有
  根目录 `open_all.sh` 一份源码发生变化，共新增 3 行、删除 2 行。
- ROS 总 launch 的环境新增
  `/usr/lib/python3/dist-packages:$HOME/.local/lib/python3.12/site-packages:$ROAD_DIR`，且系统
  dist-packages 排在用户 site-packages 前。
- 深度视觉进程不再使用 PATH 中含义不确定的 `python3`，改为启动器已经校验过的
  `$SYSTEM_PYTHON`（默认 `/usr/bin/python3`），并使用同一套 PYTHONPATH 顺序。

#### 上一版问题的准确结论

- `18:41:06` 的实际日志已经出现六个 Nav2 lifecycle 全部激活，且会话目录成功生成
  `final_map.pgm`、`final_map.yaml` 和 `result.pbstream`。因此 `11.67` 的“首帧 `/map` 后再激活
  Nav2”修复已经生效，不再是 lifecycle/planner 启动卡死。
- 当次仍在 `safety_fusion_node -> calibration_640.py -> cv2` 链路出现 ABI 错误：用户目录先加载
  NumPy 2.5.1，而 Ubuntu 的 OpenCV 模块按 NumPy 1.x 编译，报
  `numpy.core.multiarray failed to import`。融合节点虽然回退到默认距离阈值继续运行，但视觉标定值
  未加载，独立深度视觉进程也可能因同一环境冲突失败，所以整套系统不能算完整可用。
- 迁移前目录在 `18:48` 修改运行时 Python 路径后，`18:49:50` 会话不再出现该 ImportError；Nav2
  六个节点正常激活，并再次成功保存 PGM、YAML、PBStream。这证明有效修复点是 Python 包解析
  顺序和解释器选择，不是 Cartographer、雷达、Nav2 或 STM32 参数。

#### 防回归

- `validate_auto_mapping_jazzy.sh` 新增静态断言，要求一键启动器保留系统 dist-packages 优先顺序，
  并要求深度视觉使用 `$SYSTEM_PYTHON`；以后误删这两项会在正式启动前直接报错。
- `ReadMe.md` 补充 Ubuntu 24.04/Jazzy 下 NumPy/OpenCV ABI 与 PYTHONPATH 顺序说明。
- 未修改 Cartographer V13 三个冻结文件、Nav2 参数、激光滤波、底盘里程计/IMU、视觉避障算法、
  网页、速度、车体尺寸、AA55 协议或 STM32 工程。

### 11.70 最新修改标签：ASCII_PROJECT_PATH_MIGRATION_2026-07-19

#### 目录迁移

为避免 Ubuntu、colcon、rosidl、CMake、Node.js 和 Shell 在中文物理路径下出现编码或生成器兼容
问题，项目运行链路统一改为 ASCII 目录名：

| 原目录 | 新目录 |
|---|---|
| `测试10` | `CAR_PROJECT_A1` |
| `激光雷达` | `lidar` |
| `网页` | `web` |
| `开关网页` | `web_ctrl` |

#### 同步修改

- `open_all.sh`、`validate_auto_mapping_jazzy.sh` 的默认工作空间、网页目录和停止脚本路径全部改为
  ASCII 路径；自定义环境变量覆盖方式保持不变。
- GitHub Actions 的 rosdep 源路径、npm 缓存依赖路径和网页工作目录同步更新。
- `.gitignore`、`ReadMe.md`、`CAR_使用教程_注意事项.md`、`GitHub上传规定.md` 以及历史记录中的
  可执行路径示例同步更新。
- `web_ctrl/start_web.py` 本来就优先寻找 `web`，目录迁移后可直接工作，不需要新增兼容分支。

#### 边界

- 本次只有目录和路径引用迁移，没有修改 Cartographer V13、激光滤波、odom/IMU、Nav2、Frontier、
  视觉避障、速度、车体尺寸、AA55 协议、网页业务逻辑或 STM32 工程。
- Git 仓库远端名称不随本地根目录改名；GitHub 私有仓库仍使用原远端地址，代码内容以
  `CAR_PROJECT_A1` 的新目录结构呈现。
