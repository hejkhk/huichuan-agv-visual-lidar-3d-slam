# CAR 使用教程与注意事项

当前正式环境为 Ubuntu 22.04 + ROS 2 Humble，完整架构、通讯协议、启动参数和故障排查统一以根目录 `ReadMe.md` 为准。

首次部署：

```bash
chmod +x STEP0_INSTALL_VISUAL_SLAM_DEPS.sh validate_auto_mapping_humble.sh
./STEP0_INSTALL_VISUAL_SLAM_DEPS.sh
./validate_auto_mapping_humble.sh --build
```

双分辨率建图：

```bash
./START_DUAL_2D_3D_MAPPING.sh
```

双分辨率建图和导航：

```bash
./START_DUAL_2D_3D_NAVIGATION.sh
```

普通一键启动与退出保存版：

```bash
./open_all.sh
./open_all_log.sh
```

注意：

- 不要 source `/opt/ros/jazzy/setup.bash`，也不要复用 Jazzy 编译出的 Orbbec 工作空间。
- 项目源文件可以位于中文路径，一键脚本会在 `~/.cache/huichuan_agv_humble_ws` 下构建。
- Cartographer V13 的 Lua、雷达滤波、里程计和 IMU 参数属于稳定基线，本次系统迁移没有修改。
- 第一次迁移后必须执行一次 `./validate_auto_mapping_humble.sh --build`，通过后再连接实车。
