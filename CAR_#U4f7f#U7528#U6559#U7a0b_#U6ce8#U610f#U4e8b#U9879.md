# CAR 使用教程与注意事项

## 1. 定版运行链

当前工程面向 Ubuntu 24.04、ROS 2 Jazzy 和树莓派 5：

```text
LD14P 2D 雷达
  -> 固定角扫描与 laser_filters
  -> 冻结的 Cartographer V13 建图定位
  -> Jazzy 原生 Nav2
     -> SmacPlannerLattice 全局规划
     -> RotationShim + DWB 局部控制
     -> ConstrainedSmoother 服务
     -> BT.CPP v4 受控恢复
  -> Gemini2 SDK 近地面视觉避障
  -> safety_fusion_node 唯一速度仲裁
  -> chassis_node 生成 AA55 四轮速度帧
  -> STM32 / 汇川伺服
```

自动建图直接使用 `auto_mapping_v1` 的 Jazzy 原生源码：

- `frontier_exploration_ros2`：C++ Frontier 搜索、失败抑制、目标调度和完成判断。
- `short_goal_bt`：短目标预旋转、旋转安全检查和倒车脱困 BT 插件。
- `frontier_web_bridge`：保留现有网页开关和多设备状态同步接口。

旧 Foxy 回移植、Hybrid 回退、slam_toolbox 和 RF2O 不再参与构建或运行。

## 2. 关键目录

```text
测试9/
├── open_all.sh
├── open_all_log.sh
├── validate_auto_mapping_jazzy.sh
├── slam_logger.py
├── ReadMe.md
├── CAR_使用教程_注意事项.md
├── 修改.md
├── 运行环境.txt
├── maps/
├── SLAM_Log/
├── lidar/chapt1_ws/src/
│   ├── lidar_py/
│   │   ├── launch/
│   │   │   ├── cartographer_scan_v2_launch.py
│   │   │   └── cartographer_auto_mapping_jazzy_launch.py
│   │   ├── config/
│   │   │   ├── cartographer_2d_v9_tightened.lua
│   │   │   ├── laser_filter.yaml
│   │   │   ├── nav2_auto_mapping_jazzy.yaml
│   │   │   ├── frontier_auto_mapping_jazzy.yaml
│   │   │   └── lattice_forward_turnaround_5cm.json
│   │   ├── behavior_trees/
│   │   │   ├── navigate_to_pose_jazzy.xml
│   │   │   └── navigate_through_poses_jazzy.xml
│   │   └── lidar_py/
│   │       ├── chassis_node.py
│   │       ├── safety_fusion_node.py
│   │       ├── web_goal_nav_node.py
│   │       ├── web_path_preview_node.py
│   │       └── frontier_web_bridge.py
│   ├── frontier_exploration_ros2/
│   └── short_goal_bt/
├── road_v5_5_640_modular_v7_unified_io/
└── web/
```

Jazzy 的自定义 C++ 服务包通过纯 ASCII 缓存工作空间编译：

```text
~/.cache/huichuan_agv_jazzy_ws/
├── src -> <项目>/lidar/chapt1_ws/src
├── build/
├── install/
└── log/
```

源码仍在项目目录中。使用这个链接是为了避免中文物理路径触发 rosidl/CMake 问题。

## 3. 首次安装

确认系统为 Ubuntu 24.04，并已经按照 ROS 官方方式安装 ROS 2 Jazzy Desktop。

```bash
sudo apt update
sudo apt install -y \
  python3-colcon-common-extensions python3-serial python3-numpy \
  python3-empy python3-lark python3-yaml \
  nodejs npm procps psmisc \
  ros-jazzy-navigation2 ros-jazzy-nav2-bringup \
  ros-jazzy-cartographer-ros ros-jazzy-laser-filters \
  ros-jazzy-rosbridge-server ros-jazzy-rmw-cyclonedds-cpp
```

安装网页依赖：

```bash
cd <项目根目录>/web
npm install
cd ..
```

部署后首次完整验证：

```bash
cd <项目根目录>
chmod +x open_all.sh open_all_log.sh validate_auto_mapping_jazzy.sh
./validate_auto_mapping_jazzy.sh --build
```

验证脚本不会打开串口、相机，也不会发送运动指令。`--check-only` 只做静态检查，
`--build` 还会实际编译 Jazzy C++ 包。

脚本固定使用 `/usr/bin/python3` 构建 ROS 包，因此无需删除 `uv`、Conda 或
`~/.local/bin/python3`。不要在带中文的项目目录中手动执行普通 `colcon build`；请使用验证脚本
或一键启动脚本，让工程在 `~/.cache/huichuan_agv_jazzy_ws` 的纯 ASCII 路径中编译。

## 4. 一键启动

日常运行，不保存退出时的 Final 地图：

```bash
cd <项目根目录>
./open_all.sh
```

测试或正式建图，需要在 Ctrl+C 后保存 Final：

```bash
cd <项目根目录>
./open_all_log.sh
```

两个脚本都会执行：

1. 清理旧 ROS、Vite、相机和串口占用进程。
2. 自动区分 LD14P 与 STM32 两根串口并申请权限。
3. 校验冻结的 Cartographer 三个文件。
4. 编译并加载 Jazzy Frontier、自定义 BT 和 `lidar_py`。
5. 启动 Cartographer，并确认里程计、IMU、过滤扫描和第一帧 `/map` 实际到达。
6. 首帧地图就绪后再激活 Nav2，随后启动视觉融合、网页、日志和 RViz，并检查 lifecycle、Action、Frontier 服务。

`open_all_log.sh` 正常按一次 Ctrl+C 后保存：

```text
SLAM_Log/open_all_log_时间/Final/
├── final_map.pgm
├── final_map.yaml
└── result.pbstream
```

保存过程中不要连续按 Ctrl+C。第二次 Ctrl+C 是强制终止，可能留下不完整文件。

## 5. 常用启动选项

```bash
LIDAR_PORT=/dev/ttyACM0 \
CHASSIS_PORT=/dev/ttyUSB0 \
USE_RVIZ=true \
SHOW_NAVI_GUI=false \
ENABLE_VISION=true \
AUTO_START=false \
./open_all_log.sh
```

| 变量 | 默认值 | 说明 |
|---|---:|---|
| `ROS_DOMAIN_ID` | `88` | ROS 2 Domain ID。 |
| `NAV_PROFILE` | `jazzy_native` | 固定值，旧 Foxy 配置已删除。 |
| `AUTO_START` | `false` | 是否启动后立即开始 Frontier 探索。实车建议关闭，由网页开启。 |
| `ENABLE_VISION` | `true` | 启动 Gemini2 SDK 视觉避障。 |
| `START_WEB` | `true` | 启动 Vite 网页。 |
| `USE_RVIZ` | `true` | 启动 RViz2。 |
| `SHOW_NAVI_GUI` | `false` | 打开下位机 NAVI OpenCV 调试窗；关闭不影响后台解析。 |
| `SKIP_BUILD` | `false` | 跳过 colcon；仅在源码完全没变且已有有效 install 时使用。 |
| `CAR_SYSTEM_PYTHON` | `/usr/bin/python3` | Jazzy 构建解释器；默认固定为 Ubuntu 系统 Python。 |
| `LOG_DEFAULT_INTERVAL_SEC` | `3.0` | 网页 SLAM Log 默认间隔，记录默认仍关闭。 |

## 6. 网页操作

启动完成后访问：

```text
本机：http://127.0.0.1:5173
局域网：http://机器人IP:5173
视频：http://机器人IP:8080/video_feed
ROSBridge：ws://机器人IP:9090
```

### 6.1 手动选点导航

1. 等待网页显示 ROS、地图和 Pose 已接收。
2. 在地图上单击目标点，网页立即请求 `/web/preview_path`，只画路线、不动车。
3. 点击“开始导航”，目标才交给 `/navigate_to_pose`。
4. 如果自动建图正在运行，系统先停止 Frontier 并取消它的目标，再提交人工目标。
5. 点击“清除标记”只清除标记和预览路线，不取消已经执行的导航。

### 6.2 自动建图

1. 默认按钮为“自动建图 OFF”，车辆不会自行探索。
2. 点击后，网页经 `frontier_web_bridge` 调用 `/control_exploration` 启动原生 C++ 探索。
3. Explorer 读取 `/map`、TF 和全局/局部 costmap，持续选择可达 Frontier。
4. 再次点击会停止探索并取消当前 Frontier 导航目标。
5. 多个浏览器读取同一个瞬态本地 `/auto_mapping/status`，不会各自发送初始化命令。

控制面板中的“建图 ON/OFF”只是网页遥控速度档位，不等于“自动建图 ON/OFF”，也不会
向 STM32 发送旧的 `0x08` 建图模式帧。

### 6.3 Baseline 与安全门

视觉避障启用时，必须先完成深度 Baseline 采集。Baseline 未完成时，网页遥控、Nav2 和
自动建图都被安全锁阻止。采集时车前地面应为空、车体静止，采集完成后再接管 MOVE。

## 7. 导航与视觉避障关系

Nav2 和视觉节点不能同时直接写串口：

```text
Nav2 /cmd_vel_nav ----+
                       +-> safety_fusion_node -> /cmd_vel_safe -> chassis_node
网页 /cmd_vel_web ----+
深度障碍 ------------+
```

- Nav2 负责全局路线、局部绕行和目标收敛。
- 深度相机负责 2D 雷达看不到的近地低障碍，并向 costmap 发布 `/depth_obstacle_scan`。
- `safety_fusion_node` 对当前唯一速度源做动态减速、差速修正和危险停车。
- 相机光心在车体中心前方 `0.30 m`，虚拟障碍会转换到 `base_link` 后再进入 costmap。
- 导航二档上限为直行 `0.20 m/s`、原地角速度 `0.209 rad/s`，同时受外轮速度包络限制。

关闭网页“串口运动”后，Nav2、预览、Frontier 和视觉仍继续计算，但 MOVE 帧不会写入 STM32。

## 8. 冻结建图基线

以下三个文件由一键脚本和验证脚本进行哈希保护。Jazzy 适配只把扫描 launch 中的
OccupancyGrid 节点入口更新为系统实际提供的 `cartographer_occupancy_grid_node`，没有修改
Cartographer Lua 参数、激光滤波、TF、话题或分辨率：

| 文件 | SHA-256 |
|---|---|
| `cartographer_2d_v9_tightened.lua` | `00dfd1c721f0fe8c61ac6f2b417001920694e4fc77e895fb4a1f194330c910d9` |
| `cartographer_scan_v2_launch.py` | `5650100fbdaf7fd40bdb6cc8dfaa1d642b7fa44aa914028c48817af5be5a9106` |
| `laser_filter.yaml` | `8583a2ca7e99a29b13f2fc339df468e621562d61f0adfa1e7e1828254705b306` |

一键脚本和验证脚本都会检查这些哈希。哈希不一致时脚本会停止，不能为了启动而删除检查。

## 9. 首次 Jazzy 实车验收

升级后的第一次测试按以下顺序进行：

1. 架空驱动轮，执行 `./validate_auto_mapping_jazzy.sh --build`。
2. 启动 `./open_all_log.sh`，确认 RViz、网页、视频和地图正常更新。
3. 检查 `ros2 action list | grep navigate_to_pose` 和
   `ros2 service list | grep control_exploration`。
4. 不发送运动，观察 `/map`、TF 和 `/odom` 静止是否稳定。
5. 低速直行、左转、右转，确认现实和 RViz 方向一致。
6. 网页只点目标不确认，确认出现预览线且车不动。
7. 确认导航后，观察 `/cmd_vel_nav -> /cmd_vel_safe` 与 STM32 下行帧。
8. 放置低矮软障碍，确认视觉先减速/修正，Nav2 路线仍持续更新。
9. 开启自动建图，确认 Frontier 目标出现；再手动导航，确认 Frontier 被暂停。
10. 正常 Ctrl+C，确认 Log 版三个 Final 文件完整。

## 10. 快速排查

Nav2 未激活：

```bash
ros2 lifecycle get /controller_server
ros2 lifecycle get /smoother_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /behavior_server
ros2 lifecycle get /bt_navigator
ros2 action list | grep navigate_to_pose
```

自动建图按钮无响应：

```bash
ros2 node list | grep -E 'frontier_explorer|frontier_web_bridge'
ros2 service list | grep control_exploration
ros2 topic echo /auto_mapping/status --once
```

网页只有路径但车不动：

```bash
ros2 topic hz /cmd_vel_nav
ros2 topic hz /cmd_vel_safe
ros2 topic echo /robot/control_state --once
```

地图不更新或突然跳变时，保留本次完整会话目录，尤其是 `runtime_stack.log`。不要先调 Nav2
参数；Nav2 不发布 `map -> odom`，建图异常应从雷达、NAVI、TF 和 Cartographer 日志排查。

## 11. 重要注意事项

- STM32 和雷达是两根串口线；脚本会自动区分，但首次升级后必须核对启动摘要。
- 不要同时运行旧工作空间的 `source install/setup.bash`，否则同名包可能覆盖当前 Jazzy 安装。
- 不要把 `auto_mapping_v1` 内的旧绝对路径直接复制到参数文件；当前 launch 会动态注入路径。
- 不要删除 ASCII 构建路径机制，也不要把 `build/install/log` 提交到 Git。
- 物理急停优先级最高，网页软件急停不能替代硬件急停回路。
- `STM32/` 和 `SLAM_Log/` 不上传 GitHub。
